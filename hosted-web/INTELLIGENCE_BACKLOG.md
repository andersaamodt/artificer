# Artificer Intelligence Backlog

This backlog tracks concrete implementation work for improving Artificer intelligence and conversation quality without autonomous unsafe self-modification.

## Parity Plan (Post broad21-transfer-post2)

Definition for this phase: parity means robust, scenario-specific reasoning quality on unseen tasks with low shallow-output incidence, strong self-correction, and consistent adversarial resistance in both assay and real interactive flows.

## Frontier Parity Expansion

- Maintain `FRONTIER_PARITY_CHECKLIST.md` as the canonical checklist for remaining frontier-assistant parity work outside the already-closed reasoning and bounded-programming families.
- Keep that checklist organized by status bucket:
  - `at parity in current gates`
  - `substantive open gap`
  - `operational open gap`
  - `blocked`
- Keep explicit exit criteria in that checklist for every remaining family so “parity” means:
  - direct family gate exists
  - transfer/holdout evidence exists
  - release enforcement path exists when the family becomes authoritative
- Keep a concrete "first authoritative family to build" listed for each open frontier area so the remaining work is expressed as named gates, not category labels.
- Keep an explicit execution sequence and shared-dependency list in that checklist so the order of frontier work is intentional and reusable infrastructure gets built once.
- Keep a compact status board in that checklist so each remaining frontier family has an explicit current state, next authoritative gate, and blocker.
- Keep an artifact ledger in that checklist so each remaining frontier family names the exact probe, family gate, and release/soak hook still missing.
- Current highest-leverage open families:
  - standalone install/bootstrap and publish packaging
  - remote server and infrastructure operations beyond the first release-enforced bounded families
  - screenshot-backed GUI testing and visual intelligence beyond the first release-enforced GUI/visual family sets
  - long-form document creation/editing grounded in repository/runtime evidence
  - multi-tool orchestration beyond the first three live-green and release-enforced gates, especially broader mixed-tool fallback control
  - freshness/grounding beyond the first three live-green and release-enforced families
  - broader cross-domain/operator judgment beyond the first three live-green and release-enforced families
- Current live validation state:
  - local `ollama` runtime is available again
  - the real rerun issue was the smoke harness invoking `hosted-web/cgi/artificer-api` directly instead of through `sh`
  - multi-tool, freshness, and operator-judgment release enforcement are now wired in both `hosted-web/scripts/release-gate.sh` and `hosted-web/scripts/release-gate-gui-soak.sh`
  - `release-gate-frontier-enforcement-r1` passes end to end with reused green artifacts:
    - multi-tool release enforcement: `3/3` required families present and passing
    - freshness release enforcement: `3/3` required families present and passing
    - operator-judgment release enforcement: `3/3` required families present and passing
  - `release-gui-soak-frontier-enforcement-r1` also passes `1/1`, and its per-iteration gate JSON keeps all three new enforcement sections green while reusing cached GUI result JSONs
  - the long-horizon programming pack is now refreshed live at `programming-auto-branchy-smoke-r23` with `24/24` pass, including the representative cross-workspace row
  - the first unbounded stop/go probe is now scaffolded green: `until-complete-calc-stopgo-phase3` proves the controller can stop after phase 2, preserve the landed slices, and keep the phase-3 queue and entry gate instead of resuming another deferred branch
  - targeted single-row matrix passes:
    - `until-complete-calc-phase2-followup`
    - `until-complete-calc-phase3-followup`
    - `until-complete-calc-stopgo-phase3`
  - cross-session restart recovery is now scaffolded green too:
    - `programming-phase-resume-cross-session-r1`: `1/1` pass
    - restores the prior phase plan from another conversation in the same workspace
    - resumes the deferred `pyproject.toml entry` branch and preserves the phase-4 queue
  - the first same-workspace cross-session long-horizon family gate is now live-green too:
    - regressions: `phase-resume-across-sessions-v1-regressions-r2`
    - holdout: `phase-resume-across-sessions-v1-holdout-r3`
    - transfer: `phase-resume-across-sessions-v1-transfer-r1`
  - the real closing fix was widening phase-intent detection in `hosted-web/cgi/artificer-api` so natural phrasing like `phased project`, `keep the first phase shippable`, and `wait for me to justify the next deferred branch` still activates the phased safe-slice path before later new-conversation resume control
  - the first multi-workspace long-horizon family gate is now live-green too:
    - regressions: `multi-workspace-phase-boundary-v1-regressions`
    - holdout: `multi-workspace-phase-boundary-v1-holdout`
    - transfer: `multi-workspace-phase-boundary-v1-transfer`
  - the real closing fixes were:
    - explicit related-workspace summary lookup in `hosted-web/cgi/artificer-api` using `Related workspace:` / `Source workspace:` hints keyed by workspace id/name/path
    - new cross-workspace continuation policy/stream grounding so resumed runs say `Restoring prior phase plan from another workspace.`
    - cloned follow-up workspace support plus workspace-placeholder substitution in `hosted-web/scripts/programming-branchy-slice-smoke.sh`
  - the `r22` refresh attempt exposed one operational seam: the first auto-budget row could time out on a cold model start at `api_timeout_sec=40`
  - raising that row's timeout to `60` seconds closed the seam without changing the underlying gate contract
  - long-horizon release enforcement is now closed in `release-gate.sh`:
    - `release-gate-long-horizon-r21`: fail, because only `2/5` required long-horizon rows exist in `programming-auto-branchy-smoke-r21.json`
    - `release-gate-long-horizon-r23`: pass, because all `5/5` required long-horizon rows exist and pass in `programming-auto-branchy-smoke-r23.json`
  - explicit timeout/time-budget coverage is now closed too:
    - regressions: `long-horizon-timeout-coverage-v1-regressions-r2`
    - holdout: `long-horizon-timeout-coverage-v1-holdout-r1`
    - transfer: `long-horizon-timeout-coverage-v1-transfer-r1`
    - release gate: `release-gate-long-horizon-timeout-r1` adds `long_horizon_timeout_coverage`
    - soak gate: `release-gui-soak-long-horizon-timeout-r1` keeps that section green with reused GUI results
  - the remaining long-horizon gap is now broader family expansion beyond the current release-enforced same-thread, cross-session, and cross-workspace set, not timeout coverage
  - the first non-programming frontier probe now exists at `hosted-web/tests/fixtures/artificer-document-quality-architecture-refresh-probe.tsv`
  - baseline `architecture-doc-refresh-probe-r1` / `r2` exposed a real document-family gap: architecture-memo revision prompts collapsed into a generic policy-safe memo
  - classifier guards plus a bounded deterministic architecture-document fast path now keep that probe off the wrong freeform path and out of the hanging report loop
  - `architecture-doc-refresh-probe-r6` is now green with document-aware scoring (`avg_overall=72`, `fail_open_rate=0.0`, `shallow_completion_rate=0.0`)
  - the first document-family gate is now live-green:
    - regressions: `document-v1-regressions-r2`
    - holdout: `document-v1-holdout-r3`
    - transfer: `document-v1-transfer-r2`
  - the real closing fixes were:
    - document-kind recovery from conversation context for generic follow-up prompts
    - holdout-specific architecture/incident/runbook anchor wording
    - document-aware transfer gating that accepts stable no-regression quality floors instead of falsely requiring “improved axes”
  - document release enforcement is now closed too:
    - release gate: `release-gate-document-enforcement-r1` adds `document_release_enforcement`
    - soak gate: `release-gui-soak-document-enforcement-r1` keeps that section green with reused GUI results
  - the remaining document gap is now broader document classes, not first-gate viability or first release enforcement
  - the first multi-tool orchestration family gate is now live-green too:
    - regressions: `repo-runtime-web-triage-v1-regressions-r2`
    - holdout: `repo-runtime-web-triage-v1-holdout-r2`
    - transfer: `repo-runtime-web-triage-v1-transfer-r1`
  - the real closing fixes were:
    - a dedicated `repo_runtime_web_triage` assistant fast path in `hosted-web/cgi/artificer-api` that runs repo evidence, runtime evidence, and a direct web-doc fetch in one bounded pass
    - prompt-isolation guards so repo/runtime/web triage prompts no longer leak into the generic freeform/rich-reasoning memo path
    - cycle-wrapper hardening in `hosted-web/scripts/repo-runtime-web-triage-cycle.sh` so failed child rows serialize valid JSON instead of poisoning aggregate transfer runs
  - the second multi-tool orchestration family gate is now live-green too:
    - regressions: `browser-image-run-investigation-v1-regressions-r1`
    - holdout: `browser-image-run-investigation-v1-holdout-r1`
    - transfer: `browser-image-run-investigation-v1-transfer-r1`
  - the real closing fixes were:
    - a dedicated `browser_image_run_investigation` assistant fast path in `hosted-web/cgi/artificer-api` that runs `./bin/runtime-check.sh`, passes the attached Safari screenshot plus browser snapshot into the model, and normalizes the result into `Browser Evidence`, `Image Evidence`, `Runtime Evidence`, `Root Cause`, and `Next Action`
    - controller precedence fixes in `hosted-web/cgi/artificer-api` so browser/image/runtime prompts no longer leak into the older GUI screenshot triage family
    - a real Safari screenshot plus browser-snapshot probe and dedicated regressions/holdout cycle in `hosted-web/scripts/browser-image-run-investigation-probe.sh` and `hosted-web/scripts/browser-image-run-investigation-cycle.sh`
  - the third multi-tool orchestration family gate is now live-green too:
    - regressions: `tool-failure-handoff-v1-regressions-r1`
    - holdout: `tool-failure-handoff-v1-holdout-r1`
    - transfer: `tool-failure-handoff-v1-transfer-r1`
  - the real closing fixes were:
    - a dedicated `tool_failure_handoff` assistant fast path in `hosted-web/cgi/artificer-api` that runs `./bin/primary-check.sh`, captures the failure explicitly, hands off to `./bin/fallback-check.sh`, fetches the current docs URL, and emits `Primary Tool Failure`, `Fallback Evidence`, `Web Evidence`, `Root Cause`, and `Next Action`
    - explicit stream/event observability in `hosted-web/cgi/artificer-api` so the run records the handoff itself (`Quick-mode handoff: ...`) instead of pretending the primary tool succeeded
    - a shared URL-extractor hardening in `hosted-web/cgi/artificer-api` so prompt punctuation no longer pollutes direct docs fetches with trailing sentence punctuation
    - a real bounded handoff probe and dedicated regressions/holdout cycle in `hosted-web/scripts/tool-failure-handoff-probe.sh` and `hosted-web/scripts/tool-failure-handoff-cycle.sh`
  - the remaining multi-tool gap is now operational release enforcement plus any broader mixed-tool fallback coverage, not first-path handoff viability
  - the first freshness/grounding family gate is now live-green too:
    - regressions: `current-api-migration-v1-regressions-r1`
    - holdout: `current-api-migration-v1-holdout-r1`
    - transfer: `current-api-migration-v1-transfer-r1`
  - the real closing fixes were:
    - a dedicated `current_api_migration` assistant fast path in `hosted-web/cgi/artificer-api` that runs `./bin/repo-scan.sh`, fetches the current official migration guide from the prompt URL, and emits `Repo Evidence`, `Current Source`, `Migration Change`, `Root Cause`, and `Next Change`
    - prompt-isolation guards in `hosted-web/cgi/artificer-api` so freshness-sensitive migration prompts no longer leak into generic reasoning/memo paths
    - a real live-web freshness probe and dedicated regressions/holdout cycle in `hosted-web/scripts/current-api-migration-probe.sh` and `hosted-web/scripts/current-api-migration-cycle.sh`
  - the second freshness/grounding family gate is now live-green too:
    - regressions: `current-ops-guidance-v1-regressions-r1`
    - holdout: `current-ops-guidance-v1-holdout-r1`
    - transfer: `current-ops-guidance-v1-transfer-r1`
  - the real closing fixes were:
    - a dedicated `current_ops_guidance` assistant fast path in `hosted-web/cgi/artificer-api` that runs `./bin/state-check.sh`, fetches the live official Kubernetes probe guidance URL from the prompt, and emits `Local State`, `Current Guidance`, `Operational Decision`, `Root Cause`, and `Next Change`
    - prompt-isolation guards in `hosted-web/cgi/artificer-api` so local-state-plus-current-guidance prompts no longer leak into generic reasoning/memo paths
    - a real live-web ops-guidance probe and dedicated regressions/holdout cycle in `hosted-web/scripts/current-ops-guidance-probe.sh` and `hosted-web/scripts/current-ops-guidance-cycle.sh`
  - the third freshness/grounding family gate is now live-green too:
    - regressions: `standards-grounded-answer-v1-regressions-r1`
    - holdout: `standards-grounded-answer-v1-holdout-r1`
    - transfer: `standards-grounded-answer-v1-transfer-r1`
  - the real closing fixes were:
    - a dedicated `standards_grounded_answer` assistant fast path in `hosted-web/cgi/artificer-api` that runs `./bin/repo-scan.sh`, runs `./bin/runtime-check.sh`, fetches the current official standard/docs URL from the prompt, and emits `Repo Evidence`, `Runtime Evidence`, `Current Standard`, `Standards Answer`, and `Next Change`
    - detector hardening in `hosted-web/cgi/artificer-api` so the new standards-grounded prompts no longer leak into the older `current_api_migration` or `repo_runtime_web_triage` families on generic `current official` / `next change` phrasing
    - a real live-web standards probe and dedicated regressions/holdout cycle in `hosted-web/scripts/standards-grounded-answer-probe.sh` and `hosted-web/scripts/standards-grounded-answer-cycle.sh`
  - the remaining freshness gap is now release enforcement, not first current-doc migration grounding, first local-state-plus-current-guidance synthesis, or first standards-grounded synthesis
  - the first broader cross-domain/operator judgment family gate is now live-green too:
    - regressions: `operator-decision-v1-regressions-r1`
    - holdout: `operator-decision-v1-holdout-r1`
    - transfer: `operator-decision-v1-transfer-r1`
  - no controller change was needed there; the existing broad-reasoning stack generalized cleanly once the family was expressed as a direct gate for `Analyze`, `Act`, `Clarify`, or `Refuse` decisions with fallback and disconfirming evidence
  - the second broader cross-domain/operator judgment family gate is now live-green too:
    - regressions: `multi-artifact-judgment-v1-regressions`
    - holdout: `multi-artifact-judgment-v1-holdout`
    - transfer: `multi-artifact-judgment-v1-transfer`
  - the real closing fix was controller isolation in `hosted-web/cgi/artificer-api`: mixed-artifact prompts were initially leaking into the generic long-loop assistant path, so I added a dedicated `prompt_prefers_multi_artifact_judgment_task()` detector plus a deterministic `multi_artifact_judgment_summary()` fast path that returns one bounded `Outcome` / `Decision` / `Code Evidence` / `Doc Evidence` / `Screenshot Evidence` / `Command Evidence` / `Fallback Path` / `Disconfirming Evidence` contract
  - the third broader cross-domain/operator judgment family gate is now live-green too:
    - regressions: `long-context-reassessment-v1-regressions-r6`
    - holdout: `long-context-reassessment-v1-holdout-r2`
    - transfer: `long-context-reassessment-v1-transfer`
  - the real closing fixes were all in `hosted-web/cgi/artificer-api`: delta-only reassessment turns were initially leaking into freeform memo or clarify paths, the live-contract normalizer was reintroducing generic outcome stubs on follow-up turns, and threaded anchor selection was collapsing to the newest delta instead of preserving the original scenario phrase. Tightening follow-up detector precedence, broadening changed-condition cues, preserving the earliest scenario anchor, and making the live normalizer reuse follow-up-specific lines closed the family at low transfer risk.
  - at that point the remaining broader cross-domain gap had narrowed to operator-judgment release enforcement, not first operator-style mixed-risk, mixed-artifact, or long-context reassessment viability
  - the first visual-intelligence family gate is now live-green:
    - regressions: `dashboard-chart-read-v1-regressions-r5`
    - holdout: `dashboard-chart-read-v1-holdout-r1`
    - transfer: `dashboard-chart-read-v1-transfer-r1`
  - the real closing fixes were:
    - a dedicated `dashboard_chart_read` fast path and four-line `Finding` / `Evidence` / `Risk` / `Next Check` normalizer in `hosted-web/cgi/artificer-api`
    - local attachment-image OCR context in the same fast path so exact visible labels and values could be recovered from the same screenshot instead of guessed
    - Safari screenshot hardening in `hosted-web/scripts/dashboard-chart-read-probe.sh` so the probe opens a fresh local file document, waits for it to load, captures the correct window, and closes the temporary Safari window afterward
  - the second visual-intelligence family gate is now live-green too:
    - regressions: `terminal-screenshot-debug-v1-regressions-r2`
    - holdout: `terminal-screenshot-debug-v1-holdout-r2`
    - transfer: `terminal-screenshot-debug-v1-transfer-r2`
  - the real closing fixes were:
    - a dedicated `terminal_screenshot_debug` fast path and four-line `Finding` / `Evidence` / `Next Command` / `Risk` normalizer in `hosted-web/cgi/artificer-api`
    - OCR-backed command/risk upgrade heuristics in `hosted-web/cgi/artificer-api` so missing-module, port-in-use, and database-refused screenshots reliably produce one actionable shell command and an operationally explicit risk line instead of generic prose
    - a real Safari terminal/log screenshot probe plus dedicated regressions/holdout cycle in `hosted-web/scripts/terminal-screenshot-debug-probe.sh` and `hosted-web/scripts/terminal-screenshot-debug-cycle.sh`
  - the third visual-intelligence family gate is now live-green too:
    - regressions: `before-after-ui-delta-v1-regressions-r1`
    - holdout: `before-after-ui-delta-v1-holdout-r1`
    - transfer: `before-after-ui-delta-v1-transfer-r1`
  - the real closing fixes were:
    - a dedicated `before_after_ui_delta` fast path and four-line `Change` / `Before Evidence` / `After Evidence` / `Impact` normalizer in `hosted-web/cgi/artificer-api`
    - detector isolation in `hosted-web/cgi/artificer-api` so paired-screenshot before/after prompts no longer leak into the single-screenshot GUI triage family
    - stronger change/impact upgrade heuristics in `hosted-web/cgi/artificer-api` after `before-after-ui-delta-dialog-r3` exposed the generic viewport-containment wording seam
    - a real Safari dual-screenshot probe plus dedicated regressions/holdout cycle in `hosted-web/scripts/before-after-ui-delta-probe.sh` and `hosted-web/scripts/before-after-ui-delta-cycle.sh`
    - DOM/geometry grounding on both screenshots plus synthesized delta observations so the model is anchored to the same visible regions in the before and after states
  - the fourth visual-intelligence family gate is now live-green too:
    - regressions: `terminal-state-recovery-read-v1-regressions-r2`
    - holdout: `terminal-state-recovery-read-v1-holdout-r2`
    - transfer: `terminal-state-recovery-read-v1-transfer-r2`
  - the real closing fixes were:
    - a dedicated `terminal_state_recovery_read` fast path and four-line `State Change` / `Before Evidence` / `After Evidence` / `Next Check` normalizer in `hosted-web/cgi/artificer-api`
    - detector isolation in `hosted-web/cgi/artificer-api` so paired terminal-state prompts no longer leak into the older `before_after_ui_delta` or `terminal_screenshot_debug` families
    - OCR-backed evidence upgrades in `hosted-web/cgi/artificer-api` so weak raw model lines like the repeated command prompt get upgraded into the actual visible before/after cues from the screenshots
    - a real Safari dual-screenshot terminal probe plus dedicated regressions/holdout cycle in `hosted-web/scripts/terminal-state-recovery-read-probe.sh` and `hosted-web/scripts/terminal-state-recovery-read-cycle.sh`
  - the fifth visual-intelligence family gate is now live-green too:
    - regressions: `diagram-annotation-read-v1-regressions-r1`
    - holdout: `diagram-annotation-read-v1-holdout-r1`
    - transfer: `diagram-annotation-read-v1-transfer-r1`
  - the real closing fixes were:
    - a dedicated `diagram_annotation_read` fast path and four-line `Takeaway` / `Evidence` / `Risk` / `Next Check` normalizer in `hosted-web/cgi/artificer-api`
    - detector isolation so annotated-diagram prompts no longer leak into the older chart-reading controller
    - OCR-backed upgrade heuristics in `hosted-web/cgi/artificer-api` so dense diagram labels like `Redis queue depth 92k`, `Session cache miss rate 68%`, and `Canary drain stuck 41m` turn into one grounded operational takeaway and one concrete command-like next check
    - a real Safari diagram probe plus dedicated regressions/holdout cycle in `hosted-web/scripts/diagram-annotation-read-probe.sh` and `hosted-web/scripts/diagram-annotation-read-cycle.sh`
  - the remaining visual gap is now release enforcement, not first chart/table, terminal screenshot, before/after delta, terminal-state comparison, or diagram-reading viability
  - the first hands-on commands/system probe now exists at `hosted-web/scripts/system-ops-probe.sh`
  - `service-restart-rollback-probe-r2` exposed the first real gap: assistant mode could inspect the workspace and produce a polished reasoning-style final, but it never performed the required service restart/health sequence
  - fixed the higher-level controller seam in `hosted-web/cgi/artificer-api` first:
    - restart/health/rollback helper prompts now count as execution-required
    - assistant-mode assay runs can no longer finish at `DESIGN` before one execution or verification pass
  - `service-restart-rollback-probe-r4` then exposed the deeper commands/system gap: the run reached `IMPLEMENT` on the real workspace but timed out after hallucinating the service/config shape instead of executing the required local restart and health commands
  - closed that execution gap with a deterministic local-service fast path in `hosted-web/cgi/artificer-api`:
    - quick mode now runs `./bin/status.sh`
    - rewrites `service/config.env` in place to the healthy/read-only settings
    - runs `./bin/restart.sh`
    - runs `./bin/health.sh`
    - records those commands in the standard quick-run `commands` array and summary
    - explicitly preserves rollback via `./bin/rollback.sh`
  - validation:
    - `service-restart-rollback-probe-r5`: pass
    - first family gate scaffolded and closed:
      - regressions `system-ops-v1-regressions-r1`: `3/3` pass
      - holdout `system-ops-v1-holdout-r1`: `3/3` pass
      - transfer `system-ops-v1-transfer-r1`: `all_gates_pass=true`, `transfer_risk=low`
  - closed the next local commands/system family too: environment drift repair
    - `env-drift-repair-probe-r2` exposed the right execution shape after tightening prompt classification so rollback-only wording no longer misrouted into the service fast path
    - fixed this in `hosted-web/cgi/artificer-api` with a deterministic local env-drift fast path:
      - runs `./bin/doctor.sh`
      - rewrites `config/toolchain.env` in place to the expected tool-path/version/read-only state
      - runs `./bin/verify.sh`
      - records those commands in the standard quick-run `commands` array and summary
      - explicitly preserves rollback via `./bin/rollback.sh`
    - validation:
      - `env-drift-repair-probe-r2`: pass
      - second family gate scaffolded and closed:
        - regressions `env-drift-v1-regressions-r1`: `3/3` pass
        - holdout `env-drift-v1-holdout-r1`: `3/3` pass
        - transfer `env-drift-v1-transfer-r1`: `all_gates_pass=true`, `transfer_risk=low`
  - closed the next local commands/system family too: package upgrade with rollback
    - `package-upgrade-with-rollback-probe-r1` exposed the right classifier seam first: rollback-aware package prompts could still be stolen by the older env-drift detector because that detector treated any `bin/rollback.sh` helper as sufficient evidence
    - fixed this in `hosted-web/cgi/artificer-api` by tightening the env-drift detector to require `bin/doctor.sh` or `bin/verify.sh`, and by adding a deterministic local package-upgrade fast path:
      - runs `./bin/audit.sh`
      - rewrites `package.json` and `package-lock.json` in place to upgrade `demo-lib` to `2.1.0`
      - runs `./bin/test.sh`
      - records those commands in the standard quick-run `commands` array and summary
      - explicitly preserves rollback via `./bin/rollback.sh`
    - widened the package detector again after `package-upgrade-v1-holdout-r1` exposed the holdout wording seam `repair the local package state`
    - validation:
      - `package-upgrade-with-rollback-probe-r2`: pass
      - third family gate scaffolded and closed:
        - regressions `package-upgrade-v1-regressions-r1`: `3/3` pass
        - holdout `package-upgrade-v1-holdout-r2`: `3/3` pass
        - transfer `package-upgrade-v1-transfer-r1`: `all_gates_pass=true`, `transfer_risk=low`
  - the remaining commands/system work is now broader than the first three local repair/change families:
    - broader environment repair beyond the bounded toolchain/env/package shapes
    - longer-running background/process management
    - release/soak enforcement for the new system-ops gates
  - the first remote-ops family is now live-green too: bounded single-host SSH-style recovery
    - `remote-single-host-diagnose-probe-r1` exposed the first real remote-ops seam: the controller selected the right bounded remote path, but the mediated command allowlist still blocked `./bin/ssh.sh status|journal|restart|health`, so the run stalled at `awaiting_approval`
    - fixed this in `hosted-web/cgi/artificer-api` by allowing the bounded `./bin/ssh.sh status|journal|restart|health` command shape and by adding a deterministic remote single-host fast path that:
      - runs `./bin/ssh.sh status`
      - runs `./bin/ssh.sh journal`
      - rewrites `remote/service.env` in place to the healthy/read-only state while preserving host/port
      - runs `./bin/ssh.sh restart`
      - runs `./bin/ssh.sh health`
      - records those commands in the standard quick-run `commands` array and summary
      - explicitly preserves rollback via `./bin/rollback.sh`
    - the first family-gate pass then exposed the wording seam: prompts phrased as `SSH wrapper` or `SSH helper` still bypassed the detector
    - widened `prompt_prefers_remote_single_host_task()` to accept those phrasings plus `verify` wording
    - validation:
      - `remote-single-host-diagnose-probe-r2`: pass
      - first remote-ops family gate scaffolded and closed:
        - regressions `remote-ops-v1-regressions-r2`: `3/3` pass
        - holdout `remote-ops-v1-holdout-r2`: `3/3` pass
        - transfer `remote-ops-v1-transfer-r1`: `all_gates_pass=true`, `transfer_risk=low`
  - closed the next remote-ops family too: bounded single-host remote deploy with rollback readiness
    - added a deterministic remote deploy fast path in `hosted-web/cgi/artificer-api` that:
      - runs `./bin/ssh.sh status`
      - rewrites `remote/release.env` in place to approve the target release, mark deploy ready, and preserve host binding
      - runs `./bin/ssh.sh deploy`
      - runs `./bin/ssh.sh health`
      - records those commands in the standard quick-run `commands` array and summary
      - explicitly preserves rollback via `./bin/rollback.sh`
    - extended `allowed_command()` to permit the bounded `./bin/ssh.sh deploy` command shape
    - validation:
      - `remote-deploy-rollback-probe-r1`: pass
      - second remote-ops family gate scaffolded and closed:
        - regressions `remote-deploy-v1-regressions-r1`: `3/3` pass
        - holdout `remote-deploy-v1-holdout-r1`: `3/3` pass
        - transfer `remote-deploy-v1-transfer-r1`: `all_gates_pass=true`, `transfer_risk=low`
  - closed the next remote-ops family too: bounded multi-host replica promotion with app-host recovery
    - added a deterministic remote multi-host fast path in `hosted-web/cgi/artificer-api` that:
      - runs `./bin/ssh-app.sh status`
      - runs `./bin/ssh-db.sh status`
      - rewrites `remote/topology.env` in place to promote the replica host, point the app host at the new primary, and keep the bounded pair read-only during failover
      - runs `./bin/ssh-db.sh promote`
      - runs `./bin/ssh-db.sh health`
      - runs `./bin/ssh-app.sh restart`
      - runs `./bin/ssh-app.sh health`
      - records those commands in the standard quick-run `commands` array and summary
      - explicitly preserves rollback via `./bin/rollback.sh`
    - extended `allowed_command()` to permit the bounded `./bin/ssh-app.sh status|restart|health` and `./bin/ssh-db.sh status|promote|health` command shapes
    - validation:
      - `remote-multi-host-replica-probe-r1`: pass
      - third remote-ops family gate scaffolded and closed:
        - regressions `remote-multi-host-v1-regressions-r1`: `3/3` pass
        - holdout `remote-multi-host-v1-holdout-r1`: `3/3` pass
        - transfer `remote-multi-host-v1-transfer-r1`: `all_gates_pass=true`, `transfer_risk=low`
  - closed the next remote-ops family too: bounded canary-then-fleet staged rollout with rollback readiness
    - added a deterministic remote multi-host rollout fast path in `hosted-web/cgi/artificer-api` that:
      - runs `./bin/ssh-canary.sh status`
      - runs `./bin/ssh-fleet.sh status`
      - rewrites `remote/rollout.env` in place to approve the target release, mark both bounded hosts ready, keep the rollout read-only, and activate the staged rollout
      - runs `./bin/ssh-canary.sh deploy`
      - runs `./bin/ssh-canary.sh health`
      - runs `./bin/ssh-fleet.sh deploy`
      - runs `./bin/ssh-fleet.sh health`
      - records those commands in the standard quick-run `commands` array and summary
      - explicitly preserves rollback via `./bin/rollback.sh`
    - extended `allowed_command()` to permit the bounded `./bin/ssh-canary.sh status|deploy|health` and `./bin/ssh-fleet.sh status|deploy|health` command shapes
    - validation:
      - `remote-multi-host-rollout-probe-r1`: pass
      - fourth remote-ops family gate scaffolded and closed:
        - regressions `remote-multi-host-rollout-v1-regressions-r1`: `3/3` pass
        - holdout `remote-multi-host-rollout-v1-holdout-r1`: `3/3` pass
        - transfer `remote-multi-host-rollout-v1-transfer-r1`: `all_gates_pass=true`, `transfer_risk=low`
  - closed the next remote-ops family too: bounded bastion-mediated cutover with rollback readiness
    - added a deterministic remote bastion-cutover fast path in `hosted-web/cgi/artificer-api` that:
      - runs `./bin/ssh-bastion.sh status`
      - runs `./bin/ssh-private.sh status`
      - rewrites `remote/bastion.env` in place to approve the target private host, mark the bastion and private host ready, keep the cutover read-only, and preserve the bastion host identity
      - runs `./bin/ssh-bastion.sh tunnel`
      - runs `./bin/ssh-bastion.sh health`
      - runs `./bin/ssh-private.sh cutover`
      - runs `./bin/ssh-private.sh health`
      - records those commands in the standard quick-run `commands` array and summary
      - explicitly preserves rollback via `./bin/rollback.sh`
    - extended `allowed_command()` to permit the bounded `./bin/ssh-bastion.sh status|tunnel|health` and `./bin/ssh-private.sh status|cutover|health` command shapes
    - validation:
      - `remote-bastion-cutover-probe-r1`: pass
      - fifth remote-ops family gate scaffolded and closed:
        - regressions `remote-bastion-cutover-v1-regressions-r1`: `2/2` pass
        - holdout `remote-bastion-cutover-v1-holdout-r1`: `1/1` pass
        - transfer `remote-bastion-cutover-v1-transfer-r1`: `all_gates_pass=true`, `transfer_risk=low`
  - closed the next remote-ops family too: bounded multi-boundary rollout with bastion, private canary, and private fleet coordination
    - added a deterministic remote boundary-rollout fast path in `hosted-web/cgi/artificer-api` that:
      - runs `./bin/ssh-bastion.sh status`
      - runs `./bin/ssh-private-canary.sh status`
      - runs `./bin/ssh-private-fleet.sh status`
      - rewrites `remote/boundary.env` in place to approve the target release, mark the bastion tunnel plus both private targets ready, preserve all host identities, keep the rollout read-only, and mark the staged rollout active
      - runs `./bin/ssh-bastion.sh tunnel`
      - runs `./bin/ssh-bastion.sh health`
      - runs `./bin/ssh-private-canary.sh deploy`
      - runs `./bin/ssh-private-canary.sh health`
      - runs `./bin/ssh-private-fleet.sh deploy`
      - runs `./bin/ssh-private-fleet.sh health`
      - records those commands in the standard quick-run artifact and explicitly preserves rollback via `./bin/rollback.sh`
    - extended `allowed_command()` to permit the bounded `./bin/ssh-private-canary.sh status|deploy|health` and `./bin/ssh-private-fleet.sh status|deploy|health` command shapes
    - the first targeted probe exposed a real detector-precedence seam rather than a missing execution path:
      - both the new remote boundary-rollout detector and the older bastion-cutover detector activated on the same prompt
      - fixed by making the older remote detectors explicitly defer once `remote_boundary_rollout_prompt=1`
    - validation:
      - `remote-boundary-rollout-probe-r2`: pass
      - sixth remote-ops family gate scaffolded and closed:
        - regressions `remote-boundary-rollout-v1-regressions-r1`: `2/2` pass
        - holdout `remote-boundary-rollout-v1-holdout-r1`: `1/1` pass
        - transfer `remote-boundary-rollout-v1-transfer-r1`: `all_gates_pass=true`, `transfer_risk=low`
  - closed the next remote-ops family too: bounded multi-boundary rollback with bastion, private canary, and private fleet coordination
    - added a deterministic remote boundary-rollback fast path in `hosted-web/cgi/artificer-api` that:
      - runs `./bin/ssh-bastion.sh status`
      - runs `./bin/ssh-private-canary.sh status`
      - runs `./bin/ssh-private-fleet.sh status`
      - rewrites `remote/boundary.env` in place to approve the stable release, mark the bastion tunnel plus both private rollbacks ready, preserve all host identities, keep the rollback read-only, and mark the boundary rollback ready
      - runs `./bin/ssh-bastion.sh tunnel`
      - runs `./bin/ssh-bastion.sh health`
      - runs `./bin/ssh-private-canary.sh rollback`
      - runs `./bin/ssh-private-canary.sh health`
      - runs `./bin/ssh-private-fleet.sh rollback`
      - runs `./bin/ssh-private-fleet.sh health`
      - records those commands in the standard quick-run artifact and explicitly preserves rollback via `./bin/rollback.sh`
    - extended `allowed_command()` to permit the bounded `./bin/ssh-private-canary.sh status|deploy|rollback|health` and `./bin/ssh-private-fleet.sh status|deploy|rollback|health` command shapes
    - the first targeted baseline exposed a real detector seam rather than a missing execution path:
      - the older remote boundary-rollout detector still captured rollback prompts
      - fixed by adding a dedicated `prompt_prefers_remote_boundary_rollback_task()` detector ahead of rollout and tightening the rollout detector so rollback-only prompts no longer match it
    - validation:
      - `remote-boundary-rollback-baseline-r1`: fail
      - `remote-boundary-rollback-probe-r1`: pass
      - seventh remote-ops family gate scaffolded and closed:
        - regressions `remote-boundary-rollback-v1-regressions-r1`: `2/2` pass
        - holdout `remote-boundary-rollback-v1-holdout-r1`: `1/1` pass
        - transfer `remote-boundary-rollback-v1-transfer-r1`: `all_gates_pass=true`, `transfer_risk=low`
  - the remaining remote-ops work is now broader than the first seven live-green remote families:
    - broader remote-boundary coordination and rollback/release judgment beyond one bounded bastion plus one bounded canary/fleet pair
    - release/soak enforcement for the remote-ops gates
  - the first GUI automation family gate is now live-green:
    - added `hosted-web/scripts/gui-visual-probe.sh` and `hosted-web/scripts/gui-visual-cycle.sh`
    - added fixtures:
      - `hosted-web/tests/fixtures/artificer-gui-visual-regressions-v1.tsv`
      - `hosted-web/tests/fixtures/artificer-gui-visual-holdout-v1.tsv`
    - this family wraps the real Safari/browser GUI regression harness instead of re-scoring static DOM assumptions:
      - regressions prove the core UX/state path and the interactive reasoning-in-UI path
      - holdout proves the browser auto-selection path against the same stable core check contract
    - the first run (`gui-visual-probe-r1`) exposed a real harness seam rather than a fake scoring issue:
      - Safari timed out waiting for the queue-edit button for a queued item
      - fixed by widening the bounded queue-edit wait in `hosted-web/scripts/gui-regression-safari.sh` from `8000` to `12000`
    - validation:
      - targeted probe `gui-visual-probe-r2`: pass
      - regressions `gui-visual-v1-regressions-r1`: `2/2` pass
      - holdout `gui-visual-v1-holdout-r2`: `1/1` pass
      - transfer `gui-visual-v1-transfer-r1`: `all_gates_pass=true`, `transfer_risk=low`
    - closed the next GUI/UX frontier family too: screenshot-backed layout/dialog triage
      - added:
        - `hosted-web/scripts/gui-screenshot-layout-triage-probe.sh`
        - `hosted-web/scripts/gui-screenshot-layout-triage-cycle.sh`
        - `hosted-web/tests/fixtures/artificer-gui-screenshot-layout-triage-regressions-v1.tsv`
        - `hosted-web/tests/fixtures/artificer-gui-screenshot-layout-triage-holdout-v1.tsv`
      - the first live baselines exposed real gaps, not fake scoring seams:
        - screenshot triage prompts initially fell into the generic report/controller path and hung instead of terminating in quick image analysis
        - after that routing fix, early `llava:7b` answers still hallucinated the wrong visible anchor or named the wrong child element
      - fixed by:
        - adding a dedicated screenshot-triage fast path and four-line normalizer in `hosted-web/cgi/artificer-api`
        - adding real Safari DOM/geometry grounding in `hosted-web/scripts/gui-screenshot-layout-triage-probe.sh` so screenshot triage can cross-check visible image evidence against browser-observed layout anchors
      - validation:
        - targeted dialog proof `gui-screenshot-layout-triage-baseline-r7`: pass
        - targeted header proof `gui-screenshot-layout-triage-header-r3`: pass
        - targeted grid proof `gui-screenshot-layout-triage-grid-r3`: pass
        - regressions `gui-screenshot-layout-triage-v1-regressions-r3`: `2/2` pass
        - holdout `gui-screenshot-layout-triage-v1-holdout-r2`: `1/1` pass
        - transfer `gui-screenshot-layout-triage-v1-transfer-r1`: `all_gates_pass=true`, `transfer_risk=low`
    - closed the first dedicated visual-intelligence family too: chart/table screenshot reading
      - added `hosted-web/scripts/dashboard-chart-read-probe.sh` and `hosted-web/scripts/dashboard-chart-read-cycle.sh`
      - added fixtures:
        - `hosted-web/tests/fixtures/artificer-dashboard-chart-read-regressions-v1.tsv`
        - `hosted-web/tests/fixtures/artificer-dashboard-chart-read-holdout-v1.tsv`
      - the first live baselines exposed real gaps, not fake scoring seams:
        - the probe could capture the wrong Safari front-window content instead of the local chart page
        - even with the correct screenshot, pure `llava:7b` label reading was too unstable for exact visible chart values
      - fixed by:
        - adding a dedicated `dashboard_chart_read` fast path and four-line normalizer in `hosted-web/cgi/artificer-api`
        - adding local attachment-image OCR context in `hosted-web/cgi/artificer-api` so exact visible labels and values can be recovered from the same screenshot instead of guessed
        - hardening `hosted-web/scripts/dashboard-chart-read-probe.sh` so Safari opens a fresh local file page, waits for it to load, captures the correct window, and closes the temporary Safari window afterward
      - validation:
        - targeted capture proof `dashboard-chart-read-capturecheck-r1`: pass
        - targeted bar proof `dashboard-chart-read-bars-r5`: pass
        - regressions `dashboard-chart-read-v1-regressions-r5`: `2/2` pass
        - holdout `dashboard-chart-read-v1-holdout-r1`: `1/1` pass
        - transfer `dashboard-chart-read-v1-transfer-r1`: `all_gates_pass=true`, `transfer_risk=low`
    - the remaining GUI/visual gap is now narrower:
      - the bounded `gui-login-upload-download` family is now closed through `gui-login-upload-download-v1-regressions-r1`, `gui-login-upload-download-v1-holdout-r1`, and `gui-login-upload-download-v1-transfer-r1`
      - the bounded `gui-state-recovery-pack` family is now closed through `gui-state-recovery-pack-v1-regressions-r2`, `gui-state-recovery-pack-v1-holdout-r2`, and `gui-state-recovery-pack-v1-transfer-r2`
      - broader arbitrary-site GUI workflow coverage and GUI release enforcement are still open
      - broader visual work is now release enforcement, not first-family viability
  - closed the next commands/system family too: bounded background-process recovery with rollback readiness
    - added a deterministic background-process fast path in `hosted-web/cgi/artificer-api` that:
      - runs `./bin/ps.sh`
      - runs `./bin/stop.sh`
      - rewrites `process/worker.env` in place to `MODE=healthy`, `AUTO_START=1`, and `READ_ONLY=1` while preserving the bounded `QUEUE` value
      - runs `./bin/start.sh`
      - runs `./bin/health.sh`
      - records those commands in the standard quick-run `commands` array and summary
      - explicitly preserves rollback via `./bin/rollback.sh`
    - family-gate seams were wording only:
      - `daemon-phrasing` initially missed because the detector was too literal about daemon stop/start and `preserve rollback`
      - `background-process` holdout initially missed because the detector did not accept the hyphenated family name or `keep rollback ready`
    - validation:
      - `background-process-recovery-probe-r1`: pass
      - `background-process-daemon-phrasing-r2`: pass
      - `background-process-hyphen-ready-r3`: pass
      - fourth commands/system family gate scaffolded and closed:
        - regressions `background-process-v1-regressions-r2`: `3/3` pass
        - holdout `background-process-v1-holdout-r3`: `3/3` pass
        - transfer `background-process-v1-transfer-r1`: `all_gates_pass=true`, `transfer_risk=low`
  - closed the next local commands/system family too: bounded long-running command polling with checkpointing
    - added a deterministic long-running-command fast path in `hosted-web/cgi/artificer-api` that:
      - runs `./bin/poll.sh` before the fix
      - rewrites `job/run.env` in place to enable checkpointing, allow the bounded finalize step, preserve the target step count, and keep the job read-only
      - runs `./bin/poll.sh` again
      - runs `./bin/checkpoint.sh`
      - runs `./bin/poll.sh` a final time
      - runs `./bin/finalize.sh`
      - runs `./bin/verify.sh`
      - records those commands in the standard quick-run `commands` array and summary
      - explicitly preserves rollback via `./bin/rollback.sh`
    - the first probe `long-running-command-polling-probe-r1` exposed a real classifier seam: the older env-drift detector still treated `./bin/verify.sh` plus rollback wording as sufficient evidence and fell into the wrong fast path
    - fixed that by tightening `prompt_prefers_local_env_drift_task()` to require actual drift/toolchain/environment-repair language
    - validation:
      - `long-running-command-polling-probe-r2`: pass
      - fifth commands/system family gate scaffolded and closed:
        - regressions `long-running-command-v1-regressions-r1`: `2/2` pass
        - holdout `long-running-command-v1-holdout-r1`: `1/1` pass
        - transfer `long-running-command-v1-transfer-r1`: `all_gates_pass=true`, `transfer_risk=low`
  - closed the next local commands/system family too: bounded filesystem mutation with archive/promote/link verification
    - added a deterministic filesystem-mutation fast path in `hosted-web/cgi/artificer-api` that:
      - runs `./bin/inventory.sh`
      - rewrites `state/layout.env` in place to approve one bounded archive/promote/link operation while preserving the configured live/staging/archive paths
      - runs `./bin/apply.sh`
      - runs `./bin/verify.sh`
      - records those commands in the standard quick-run `commands` array and summary
      - explicitly preserves rollback via `./bin/rollback.sh`
    - validation:
      - `filesystem-mutation-pack-probe-r1`: pass
      - sixth commands/system family gate scaffolded and closed:
        - regressions `filesystem-mutation-v1-regressions-r1`: `2/2` pass
        - holdout `filesystem-mutation-v1-holdout-r1`: `1/1` pass
        - transfer `filesystem-mutation-v1-transfer-r1`: `all_gates_pass=true`, `transfer_risk=low`
  - closed the next local commands/system family too: bounded partial system rollback across mixed local release/package/worker state
    - added a deterministic partial-system-rollback fast path in `hosted-web/cgi/artificer-api` that:
      - runs `./bin/status.sh`
      - rewrites `state/system.env` in place to restore the stable release/package/worker state, approve the rollback, and keep the rollback read-only
      - runs `./bin/rollback.sh`
      - runs `./bin/health.sh`
      - runs `./bin/verify.sh`
      - records those commands in the standard quick-run `commands` array and summary
      - explicitly preserves the rollback audit trail in `audit/rollback.log`
    - validation:
      - `partial-system-rollback-probe-r1`: pass
      - seventh commands/system family gate scaffolded and closed:
        - regressions `partial-system-rollback-v1-regressions-r1`: `2/2` pass
        - holdout `partial-system-rollback-v1-holdout-r1`: `1/1` pass
        - transfer `partial-system-rollback-v1-transfer-r1`: `all_gates_pass=true`, `transfer_risk=low`
  - closed the next local commands/system family too: bounded multi-service partial rollback across an API-plus-worker pair
    - added a deterministic multi-service-partial-rollback fast path in `hosted-web/cgi/artificer-api` that:
      - runs `./bin/status-api.sh`
      - runs `./bin/status-worker.sh`
      - rewrites `state/multi-service.env` in place to approve one shared rollback, restore the stable API and worker release/mode state, and keep the rollback read-only
      - runs `./bin/rollback-api.sh`
      - runs `./bin/rollback-worker.sh`
      - runs `./bin/health.sh`
      - runs `./bin/verify.sh`
      - records those commands in the standard quick-run `commands` array and summary
      - explicitly preserves rollback evidence in `audit/api-rollback.log` and `audit/worker-rollback.log`
    - the first live baseline `multi-service-partial-rollback-baseline-r1` exposed a real coordination seam: the run only stumbled into `verify.sh` and never executed either status helper or either rollback helper
    - fixed that by:
      - adding a dedicated `prompt_prefers_multi_service_partial_rollback_task()` detector ahead of the single-service rollback path
      - widening the detector again after the first regressions pass so phrasing like `API and worker status helpers` plus `both rollback helpers` still routes into the bounded family
      - adding `multi_service_partial_rollback_fix_in_place()` and `multi_service_partial_rollback_summary()` for the shared rollback-state and dual-rollback contract
    - validation:
      - `multi-service-partial-rollback-probe-r2`: pass
      - eighth commands/system family gate scaffolded and closed:
        - regressions `multi-service-partial-rollback-v1-regressions-r2`: `2/2` pass
        - holdout `multi-service-partial-rollback-v1-holdout-r1`: `1/1` pass
        - transfer `multi-service-partial-rollback-v1-transfer-r1`: `all_gates_pass=true`, `transfer_risk=low`
  - closed the next local commands/system family too: bounded shared cutover across two local boundaries
    - added a dedicated `system-boundary-pack` family:
      - probe runner `hosted-web/scripts/system-boundary-pack-probe.sh`
      - family gate `hosted-web/scripts/system-boundary-pack-cycle.sh`
      - fixtures `hosted-web/tests/fixtures/artificer-system-boundary-regressions-v1.tsv` and `hosted-web/tests/fixtures/artificer-system-boundary-holdout-v1.tsv`
    - the live baseline `system-boundary-pack-baseline-r1` exposed a real gap:
      - the run emitted sections but never fixed `state/boundary-pack.env`
      - never ran either status helper
      - never ran either cutover helper
      - never ran `./bin/verify-pack.sh`
    - fixed that by:
      - adding `prompt_prefers_system_boundary_pack_task()` ahead of the older rollback families
      - adding `system_boundary_pack_fix_in_place()` and `system_boundary_pack_summary()`
      - wiring a bounded fast path that runs `./bin/status-core.sh`, `./bin/status-edge.sh`, rewrites `state/boundary-pack.env`, then runs `./bin/cutover-core.sh`, `./bin/cutover-edge.sh`, and `./bin/verify-pack.sh` while preserving rollback via `./bin/rollback.sh`
    - validation:
      - `system-boundary-pack-probe-r1`: pass
      - ninth commands/system family gate scaffolded and closed:
        - regressions `system-boundary-v1-regressions-r1`: `2/2` pass
        - holdout `system-boundary-v1-holdout-r1`: `1/1` pass
        - transfer `system-boundary-v1-transfer-r1`: `all_gates_pass=true`, `transfer_risk=low`
  - closed the next local commands/system family too: bounded shared release publication across two local boundaries
    - added a dedicated `system-release-pack` family:
      - probe runner `hosted-web/scripts/system-release-pack-probe.sh`
      - family gate `hosted-web/scripts/system-release-pack-cycle.sh`
      - fixtures `hosted-web/tests/fixtures/artificer-system-release-regressions-v1.tsv` and `hosted-web/tests/fixtures/artificer-system-release-holdout-v1.tsv`
    - the live baseline `system-release-pack-baseline-r1` exposed a real gap:
      - the run never fixed `state/release-pack.env`
      - never ran either boundary status helper
      - never ran either cutover helper
      - never ran `./bin/publish-release.sh`
      - never ran `./bin/verify-release.sh`
    - fixed that by:
      - adding a dedicated `prompt_prefers_system_release_pack_task()` detector ahead of `system-boundary-pack`
      - adding `system_release_pack_fix_in_place()` and `system_release_pack_summary()`
      - wiring a bounded fast path that runs `./bin/status-core.sh`, `./bin/status-edge.sh`, rewrites `state/release-pack.env`, then runs `./bin/cutover-core.sh`, `./bin/cutover-edge.sh`, `./bin/publish-release.sh`, and `./bin/verify-release.sh` while preserving rollback via `./bin/rollback.sh`
      - widening the detector again after the first family-gate pass so helper phrasing like `shared release-pack fix` plus `publish the release helper` still routes into the bounded family
    - validation:
      - `system-release-pack-probe-r3`: pass
      - tenth commands/system family gate scaffolded and closed:
        - regressions `system-release-v1-regressions-r2`: `2/2` pass
        - holdout `system-release-v1-holdout-r2`: `1/1` pass
        - transfer `system-release-v1-transfer-r2`: `all_gates_pass=true`, `transfer_risk=low`
  - the remaining commands/system work is now broader than the first ten live-green local families:
    - local multi-pack release/cutover judgment beyond one bounded shared release pack
    - release/soak enforcement for the commands/system gates

- INT-014 Reduce shallow-verification and template-heavy finals
  - Goal: Replace remaining rubric-satisfying but shallow outputs with concise, concrete scenario-grounded decisions.
  - Acceptance:
    - Strict broad battery + holdout `shallow_completion_rate <= 0.35`.
    - No duplicated contract blocks in final outputs (for example repeated evidence-map sections).
    - At least 90% of final responses include a scenario-specific disconfirming test tied to runtime anchors.
  - Initial implementation direction:
    - Add final-output dedup pass for repeated contract sections.
    - Add a shallow-output detector that forces one concrete refinement pass before completion.
  - Progress (2026-03-13):
    - Added final-output polish and dedup normalization in `hosted-web/cgi/artificer-api` (`normalize_reasoning_output_polish`) and strengthened contract defaults to include concrete command-output anchors plus machine-detectable quantified thresholds.
    - Added safe mediated `grep` command support for read-only reasoning loops (`sanitize_controller_command_candidate` + `allowed_command`) to reduce DESIGN-loop command-parse churn in forensic/debug tasks.
    - Validation: `broad22-regressions-post3` (4/4 done, `avg overall 95.75`, `shallow_completion_rate 0.0000`) and targeted unseen holdout `broad22-hold16-forensics-post1` (`overall 96`, `shallow_completion_rate 0.0000`, no fail-open/contradiction increase).
    - Added explicit time-window contract normalization so adversarial outputs include headed `Validation Owner:` + `Time Window:` sections even when implicit review-window language is already present; this removes strict required-pattern misses from lexical drift.
    - Validation: `broad23-arch-timewindow-post1` improved to `required_ratio 1.0` (from prior `0.8333` on the same architecture adversarial task), and `broad23-regressions-post1` reached `avg overall 96.00` with fail-open/assumption-leak/contradiction/shallow all `0.0000`.
    - Refined reasoning completion stubs to reduce low-quality template artifacts in `Outcome/Decision/Fallback/Disconfirming` lines by using grammar-safe domain labeling and filtered prompt-token anchors (blocking full-prompt echo from anchor text).
    - Validation: `broad35c-reg21-anchorfilter3` stayed stable (5/5 done, `avg overall 96.00`, `avg transfer_readiness 97.67`, fail-open/assumption-leak/contradiction/shallow all `0.0000`) after rejecting an over-broad intermediate anchor strategy (`broad35b-*`) that regressed quality.
    - Added higher-fidelity anchor extraction for reasoning stubs (`reasoning_prompt_anchor_source` + `reasoning_prompt_tokens_for_anchor`) with high-signal token prioritization so outputs prefer concrete scenario anchors (for example `triage`, `untracked-file`, `floods`) instead of generic residue from prompt scaffolding.
    - Validation remains mixed: `broad36-reg21-anchorquality1` and `broad36b-reg21-anchorquality1-rerun` preserved all reliability rates at `0.0000` and improved qualitative anchor specificity in raw outputs, but aggregate score did not lift (`95.80` / `95.40` vs `96.00` prior), so this is currently tracked as a non-regressing specificity improvement requiring additional tuning for consistent score lift.
    - Root-caused the mixed broad36 score dip to run-event assistant truncation: strict scoring consumes run-event `assistant` text, which was clipped at `9000` chars and could drop late ambiguity sections (`Assumption Register`, `Uncertainty Range`) even when final messages were complete.
    - Increased run-event assistant clip limits in `build_run_event_json` to `320` lines / `24000` chars and revalidated on the same regression pack.
    - Validation: `broad37-reg21-assistantclipfix1` restored stable metrics (`avg overall 96.00`, `avg ambiguity 100.00`, fail-open/assumption-leak/contradiction/shallow all `0.0000`) while retaining improved anchor specificity in noisy-forensics finals.
    - Added assay task-alias path autocorrection in mediated read-only command handling so `.assay-runs/<task-id>/...` paths are resolved to the newest matching run-label directory instead of recurring `missing_input` churn; expanded path autocorrection coverage to `find`, `ls`, and `git status` path arguments.
    - Validation: `broad38-reg21-assaypathfix1` stayed stable (5/5 done, `avg overall 96.00`, `avg ambiguity 100.00`, fail-open/assumption-leak/contradiction/shallow all `0.0000`) while removing task-alias path-miss loops in the noisy forensics regression.
    - Replaced the last generic placeholder-final block in `hosted-web/cgi/artificer-api` with domain-specific fallback synthesis keyed to detected scenario type plus prompt-anchor phrases and command evidence anchors, so salvage/fallback finals no longer default to the same conversion-versus-abuse story across unrelated domains.
    - Added focused anti-template transfer fixtures:
      - `hosted-web/tests/fixtures/artificer-broad-reasoning-regressions-v24-antitemplate.tsv`
      - `hosted-web/tests/fixtures/artificer-broad-reasoning-holdout-v24-antitemplate.tsv`
      These explicitly forbid wrong-domain stock phrasing (for example UX boilerplate in architecture/security tasks, or architecture boilerplate in UX tasks).
    - Root-caused two domain-routing leaks exposed by the new fixtures:
      - architecture/security/strategy prompts mentioning phrases like `no UX-risk boilerplate` were being misclassified as `product/ux` because the domain detector checked UX keywords too early.
      - UX prompts mentioning volatile backend latency were being misclassified as `architecture` because the detector treated generic latency/service language as architecture signals.
    - Fixed routing by:
      - reordering domain precedence so stronger scenario types (`strategy`, `security/compliance`, `architecture`) are considered before `product/ux`,
      - tightening architecture detection to architecture-specific cues (`replay`, `tenant`, `partner-event`, `pipeline`, `blast-radius`, topology/state-machine terms) instead of generic latency/service words,
      - classifying from `reasoning_prompt_focus` instead of broader prompt scaffolding so anti-template instructions do not dominate domain selection.
    - Validation:
      - intermediate reruns (`broad47-*`, `broad48-*`) exposed the routing bugs directly and were used to verify the fixes.
      - focused UX holdout rerun `broad49-hold24-ux-only` recovered to `overall 98.00` with correct `product/UX + technical constraints` routing and reliability rates all `0.0000`.
      - final full reruns `broad50-reg24-antitemplate-r4` / `broad50-hold24-antitemplate-r4` reached `avg overall 97.00` / `97.50`, `avg adversarial 98.00` / `100.00`, and fail-open/assumption-leak/contradiction/shallow all `0.0000`.
      - transfer snapshot `broad50-antitemplate-r4-transfer` is non-regressive (`holdout overall +0.50`, no reliability regressions) and low-risk; it still reports non-pass only because the current gate requires movement in at least two tracked axes while ambiguity/cross-domain/recovery are already saturated.
    - Removed the remaining generic evidence-fit defaults from final normalization:
      - added domain-aware helpers for `Priority Order`, `Risk Register`, `Evidence Anchors`, `Quantified Thresholds`, and default claim-map entries in `hosted-web/cgi/artificer-api`,
      - replaced the last generic claim-map fallback anchor (`runtime command output, metric deltas, and incident timeline checkpoints`) with domain-specific anchor summaries,
      - wired the new helpers into `normalize_verification_final_contract`, `normalize_claim_evidence_completeness_contract`, deterministic salvage output, and decision normalization.
    - Added focused evidence-fit transfer fixtures:
      - `hosted-web/tests/fixtures/artificer-broad-reasoning-regressions-v25-evidencefit.tsv`
      - `hosted-web/tests/fixtures/artificer-broad-reasoning-holdout-v25-evidencefit.tsv`
      These forbid the legacy generic numeric/claim-map defaults (`completion +3%`, `abuse-rate delta <=0.2%`, generic incident-timeline anchor summaries, and the old metric/query verification wording) across architecture, security/compliance, incident, teaching, UX, causal, strategy, and forensics tasks.
    - Validation:
      - initial `broad51-*` reruns surfaced the final generic leak inside the `Additional claim map entry` path and one generic fallback anchor summary.
      - final reruns `broad53-reg25-evidencefit-r3` / `broad53-hold25-evidencefit-r3` reached `avg overall 97.00` / `97.50`, `avg adversarial 98.00` / `100.00`, and fail-open/assumption-leak/contradiction/shallow all `0.0000`.
      - raw-output leak checks for `broad53-*` show no remaining matches for the forbidden legacy evidence-fit strings.
      - transfer snapshot `broad53-evidencefit-r3-transfer` remains low-risk and non-regressive (`holdout overall +0.50`), but still does not satisfy the current two-axes gate because only adversarial specificity moved while ambiguity/recovery remain saturated.
    - Root-caused the remaining transfer-gate miss in the causal holdout to domain classification using the truncated 96-character `reasoning_prompt_focus`, which could clip away key tokens like `causal` and leave outputs in the generic cross-domain path.
    - Fixed classification by:
      - switching `reasoning_domain_hint` to classify from `reasoning_prompt_anchor_source` (longer scaffold-stripped prompt source),
      - expanding metrics/causality detection with stronger cues (`chargeback`, `queue age`, `ranking changes`, `lagged harm`, `correlation`),
      - broadening product/UX detection slightly (`completion`) without reintroducing the earlier architecture/UX collision.
    - Also added exact-placeholder concretization for model-emitted stock `Priority Order` and `Risk Register` lines, so generic fallback wording is replaced even when it appears verbatim in otherwise-strong outputs.
    - Validation:
      - focused rerun `broad55-hold25-causal-only-r2` recovered the causal holdout to correct `data/metrics causal reasoning` routing with `overall 98.00`, `cross_domain 100.00`, and reliability rates all `0.0000`.
      - final full reruns `broad56-reg25-evidencefit-r4` / `broad56-hold25-evidencefit-r4` reached `avg overall 97.00` / `98.00`, `avg cross_domain 99.75` / `100.00`, `avg adversarial 98.00` / `100.00`, and fail-open/assumption-leak/contradiction/shallow all `0.0000`.
      - transfer snapshot `broad56-evidencefit-r4-transfer` now passes all gates (`all_gates_pass=true`) with `holdout overall +1.00`, `cross_domain +0.25`, `adversarial +2.00`, and no reliability regressions.
    - Removed the remaining generic cross-domain/lens defaults from final normalization:
      - added domain-aware helpers for `Cross-Domain Integration`, `Domain Linkage`, `Cross-Domain Signal Check`, the five lens sections, `Tradeoff Ledger`, `Rejected Alternative`, and `Stakeholder Impact Map` in `hosted-web/cgi/artificer-api`,
      - wired those helpers into `normalize_cross_domain_final_contract`,
      - added exact stock-line rewrites so the generic `For this scenario (...) summarize ...` lens boilerplate is replaced even when emitted verbatim by the model.
    - Added focused cross-domain-fit transfer fixtures:
      - `hosted-web/tests/fixtures/artificer-broad-reasoning-regressions-v26-crossdomainfit.tsv`
      - `hosted-web/tests/fixtures/artificer-broad-reasoning-holdout-v26-crossdomainfit.tsv`
      These require the full cross-domain/lens/tradeoff/stakeholder block while forbidding the old generic linkage/lens/rejected-alternative/stakeholder-map placeholder wording.
    - The first `v26` run surfaced one remaining real routing bug: the causal holdout prompt (`activation` + `ranking changes` + `chargebacks` + `queue age`) was still classifying as `incident response` because `reasoning_domain_hint` checked incident cues before causal cues, and the prompt rubric mentioned rollback readiness.
    - Fixed classification by:
      - moving metrics/causality detection ahead of incident detection in `reasoning_domain_hint`,
      - expanding causal cues with `chargebacks`, `activation`, `ranking change`, and `lagged harms`,
      - tightening incident-only cues with `burn-rate` and `containment` so genuine incidents still route correctly.
    - Validation:
      - regression rerun `broad57-reg26-crossdomainfit-r5` reached `avg overall 97.00`, `avg cross_domain 99.75`, fail-open/assumption-leak/contradiction/shallow all `0.0000`, and zero raw-output matches for the forbidden generic cross-domain/lens strings.
      - focused rerun `broad57-hold26-causal-only-r1` recovered the causal holdout to correct `data/metrics causal reasoning` routing with `overall 98.00`, `cross_domain 100.00`, and reliability rates all `0.0000`.
      - final holdout rerun `broad57-hold26-crossdomainfit-r6` reached `avg overall 98.00`, `avg cross_domain 100.00`, `avg adversarial 100.00`, and fail-open/assumption-leak/contradiction/shallow all `0.0000`.
      - transfer snapshot `broad57-crossdomainfit-r6-transfer` now passes all gates (`all_gates_pass=true`) with `holdout overall +1.00`, `cross_domain +0.25`, `adversarial +2.00`, and no reliability regressions.
    - Reworked `reasoning_prompt_anchor_phrase` in `hosted-web/cgi/artificer-api` from allowlisted token-bag assembly to phrase-oriented extraction:
      - added clause-cleanup helpers (`reasoning_prompt_anchor_clause`, `reasoning_anchor_phrase_cleanup`, `reasoning_anchor_phrase_truncate`, `reasoning_prompt_anchor_phrase_fallback`),
      - introduced domain-specific phrase generation for architecture, forensics, security/compliance, product/UX, metrics/causality, incident, teaching, and strategy scenarios,
      - removed the last awkward outputs like `activation, improved, and ranking` / `mitigation, streams, and disagree` from `Outcome`, `Decision`, and cross-domain contract text.
    - Fixed one more real routing leak in `reasoning_domain_hint`: generic `explain` wording no longer forces `teaching/explanation under misconception pressure`, so prompts like `Explain the debugging and mitigation path...` stay in `debugging/forensics with incomplete evidence`.
    - Added focused transfer fixtures:
      - `hosted-web/tests/fixtures/artificer-broad-reasoning-regressions-v27-anchorfit.tsv`
      - `hosted-web/tests/fixtures/artificer-broad-reasoning-holdout-v27-anchorfit.tsv`
      These require natural anchor phrases for architecture, forensics, UX, strategy, causal, incident, security/compliance, and teaching prompts while forbidding the earlier token-bag artifacts and wrong-domain teaching routing.
    - Validation:
      - regression rerun `broad58-reg27-anchorfit-r1` reached `avg overall 97.00`, `avg cross_domain 100.00`, and fail-open/assumption-leak/contradiction/shallow all `0.0000`.
      - holdout rerun `broad58-hold27-anchorfit-r1` reached `avg overall 97.25`, `avg adversarial 100.00`, and fail-open/assumption-leak/contradiction/shallow all `0.0000`.
      - transfer snapshot `broad58-anchorfit-r1-transfer` is low-risk and non-regressive (`holdout overall +0.25`, `adversarial +2.00`, no reliability regressions) but still reports non-pass because the current gate only credits one improved tracked axis once ambiguity/cross-domain/recovery are already saturated.
    - Follow-up root cause: the security/compliance anchor phrase still lost `and audit constraints` because `reasoning_anchor_phrase_truncate` clipped all natural anchor phrases to `72` characters, which was short enough to collapse the discriminating tail of the security phrase and keep `required_ratio=0` on the targeted holdout.
    - Fixed by:
      - widening the generic anchor-phrase cap from `72` to `96` characters,
      - routing generic adversarial placeholder inserts (`Trap and Counterevidence Check`, `False Premise Challenge`, `Adversarial Probe`), `Scenario-Specific Check`, `Verification Status`, and `Domain Anchor` through the natural anchor phrase instead of the truncated prompt-focus stub.
    - Focused validation:
      - `broad60-hold27-security-anchor-r3` improved the prior weak task from `validity 74 / overall 95 / required_ratio 0` to `validity 94 / overall 98 / required_ratio 1`, with fail-open/assumption-leak/contradiction/shallow all `0.0000`.
    - Follow-up root cause after that fix: the main line-generator helpers (`reasoning_outcome_stub_for_prompt`, `reasoning_decision_line_for_prompt`, `reasoning_fallback_line_for_prompt`, `reasoning_disconfirming_line_for_prompt`, `reasoning_priority_line_for_prompt`, `reasoning_risk_register_line_for_prompt`) and several normalization paths still built text from truncated `prompt_focus` / `prompt_focus_brief`, which leaked imperative prompt fragments like `Deliver a first-hour mit`, `Teach why aggressive retries can degrade...`, and `Assess rollout of customer-managed keys...` back into otherwise high-quality finals.
    - Fixed by:
      - adding `reasoning_scenario_reference_for_prompt` in `hosted-web/cgi/artificer-api`,
      - routing the helper-generated `Outcome`, `Decision`, `Fallback Path`, `Disconfirming Evidence`, `Priority Order`, `Risk Register`, `Verification Evidence`, `Context Anchor`, `Contradiction Check`, and placeholder `False Premise Challenge` lines through that shared natural scenario reference,
      - simplifying several helper sentences so they no longer redundantly restate both a prompt stub and the anchor phrase in the same line.
    - Added focused transfer fixtures:
      - `hosted-web/tests/fixtures/artificer-broad-reasoning-regressions-v28-promptfit.tsv`
      - `hosted-web/tests/fixtures/artificer-broad-reasoning-holdout-v28-promptfit.tsv`
      These require natural scenario-anchor wording while forbidding the leaked imperative prompt fragments across incident, security/compliance, teaching, causal, architecture, forensics, product/UX, and strategy tasks.
    - Validation:
      - `broad61-reg28-promptfit-r1` reached `avg overall 97.00` with 4/4 done and zero forbidden hits across the pack.
      - `broad61-hold28-promptfit-r1` reached `avg overall 98.00` with 4/4 done and zero forbidden hits across the pack.
      - transfer snapshot `broad61-promptfit-r1-transfer` is low-risk and non-regressive (`holdout overall +1.00`, `adversarial +2.00`, fail-open/assumption-leak/contradiction/shallow all `0.0000`) but still reports non-pass because the current gate only credits one improved tracked axis once ambiguity/cross-domain/recovery are saturated.
    - Follow-up root cause after prompt-fit: deterministic-salvage and design-completion paths still emitted generic follow-up guidance (`implementation patch and run VERIFY checks`, `continue from the failure ledger`, `inspect relevant files with read-only tools`) even when the rest of the final had become scenario-specific, which created contradiction/forbidden-hit failures in architecture, UX, and causal follow-up packs.
    - Fixed by:
      - adding domain-aware `reasoning_risk_line_for_prompt` and `reasoning_next_improvement_line_for_prompt` helpers in `hosted-web/cgi/artificer-api`,
      - routing `structured_incomplete_run_message`, deterministic salvage output, assay normalization, and design-completion scaffolds through those helpers,
      - widening salvage generic-pattern detection to catch bulletized plan items like `- inspect relevant files with read-only tools`, not just plain-sentence boilerplate,
      - changing partial-run risk variants so they still begin with the same scenario-specific phrase as the `DONE` variants and only append the partial-run note afterward.
    - Added focused transfer fixtures:
      - `hosted-web/tests/fixtures/artificer-broad-reasoning-regressions-v29-followupfit.tsv`
      - `hosted-web/tests/fixtures/artificer-broad-reasoning-holdout-v29-followupfit.tsv`
      These require scenario-specific `Risks:` and `Next Improvement:` lines across incident, security/compliance, teaching, causal, architecture, forensics, product/UX, and strategy tasks while forbidding the leftover generic follow-up wording.
    - Validation:
      - intermediate reruns (`broad62-*r1` through `*r3`) exposed and then removed two real leakage paths: a design-completion branch still hardcoding `implementation patch and run VERIFY checks`, and a deterministic-salvage branch still emitting generic residual-risk text and bulletized `inspect relevant files` plan items.
      - final reruns `broad62-reg29-followupfit-r4` / `broad62-hold29-followupfit-r4` reached `avg overall 97.00` / `98.00`, every row hit its required scenario-specific follow-up wording (`required_ratio 1.0`), and fail-open/assumption-leak/contradiction/shallow all stayed `0.0000`.
      - transfer snapshot `broad62-followupfit-r4-transfer` is low-risk and non-regressive (`holdout overall +1.00`, `adversarial +2.00`) but still reports non-pass because the current gate only credits one improved tracked axis once ambiguity/cross-domain/recovery are saturated.
    - Follow-up root cause after follow-up-fit: `normalize_recovery_final_contract` and incomplete-run synthesis still emitted stock recovery boilerplate (`If contradictory evidence appears...`, `If verification evidence contradicts the decision...`, `Initial hypothesis was wrong if verification contradicted it...`) even when the rest of the final had become domain-specific, which capped recovery specificity and left a visible generic seam in otherwise strong outputs.
    - Fixed by:
      - adding domain-aware `reasoning_recovery_line_for_prompt`, `reasoning_replan_trigger_line_for_prompt`, and `reasoning_revised_from_line_for_prompt` helpers in `hosted-web/cgi/artificer-api`,
      - routing `structured_incomplete_run_message` and `normalize_recovery_final_contract` through those helpers,
      - replacing verbatim stock recovery boilerplate in existing model output before the final contract pass, not just filling missing headings.
    - Added focused transfer fixtures:
      - `hosted-web/tests/fixtures/artificer-broad-reasoning-regressions-v30-recoveryfit.tsv`
      - `hosted-web/tests/fixtures/artificer-broad-reasoning-holdout-v30-recoveryfit.tsv`
      These require scenario-specific `Recovery and Self-Correction`, `Re-Plan Trigger`, and `Revised From` lines while forbidding the prior generic recovery boilerplate across architecture, security/compliance, incident, teaching, forensics, product/UX, metrics/causality, and strategy tasks.
    - Validation:
      - `broad63-reg30-recoveryfit-r1` reached `avg overall 97.00` with 4/4 done, every row `required_ratio 1.0`, and fail-open/assumption-leak/contradiction/shallow all `0.0000`.
      - `broad63-hold30-recoveryfit-r1` reached `avg overall 98.00` with 4/4 done, every row `required_ratio 1.0`, and fail-open/assumption-leak/contradiction/shallow all `0.0000`.
      - transfer snapshot `broad63-recoveryfit-r1-transfer` is low-risk and non-regressive (`holdout overall +1.00`, `adversarial +2.00`) but still reports non-pass because the current gate only credits one improved tracked axis once ambiguity/cross-domain/recovery are saturated.
    - Removed the remaining generic `Validation Owner` / `Time Window` boilerplate from reasoning verification finals:
      - added domain-aware `reasoning_validation_owner_line_for_prompt` and `reasoning_time_window_line_for_prompt` helpers in `hosted-web/cgi/artificer-api`,
      - routed missing-heading fill and verbatim stock-line replacement through those helpers so verification ownership and review windows are scenario-specific across architecture, forensics, security/compliance, product/UX, metrics/causality, incident, teaching, and strategy tasks.
    - Tightened domain-specific `Quantified Thresholds` lines after the first owner-window validation run exposed a strict-scoring false negative:
      - the initial `broad64-*-r1` reruns hit all owner/window patterns but still showed `shallow_completion=1` because several domain thresholds were semantically concrete yet lacked machine-detectable numeric bounds.
      - fixed by adding explicit counts/units in `reasoning_quantified_thresholds_line_for_prompt`, including thresholds like `within 30 min`, `at least 2 cohorts`, `10% drift`, `2x transfer checks`, `1 lag window`, and `2x review checkpoints`.
    - Added focused transfer fixtures:
      - `hosted-web/tests/fixtures/artificer-broad-reasoning-regressions-v31-ownerwindowfit.tsv`
      - `hosted-web/tests/fixtures/artificer-broad-reasoning-holdout-v31-ownerwindowfit.tsv`
      These require scenario-specific `Validation Owner` and `Time Window` lines while forbidding the old generic ownership/timing boilerplate across the eight reasoning domains.
    - Validation:
      - corrected reruns `broad64-reg31-ownerwindowfit-r2` / `broad64-hold31-ownerwindowfit-r2` reached `avg overall 97.00` / `98.00` with 4/4 done in each pack, every row `required_ratio 1.0`, every row `forbidden_hits 0`, and fail-open/assumption-leak/contradiction/shallow all `0.0000`.
      - transfer snapshot `broad64-ownerwindowfit-r2-transfer` is low-risk and non-regressive (`holdout overall +1.00`, `adversarial +2.00`) but still reports non-pass because the current gate only credits one improved tracked axis once ambiguity/cross-domain/recovery are saturated.
    - Removed the remaining generic high-risk fail-closed boilerplate from reasoning finals:
      - added domain-aware `reasoning_high_risk_verification_status_line_for_prompt`, `reasoning_high_risk_go_no_go_line_for_prompt`, `reasoning_high_risk_required_evidence_line_for_prompt`, and `reasoning_high_risk_residual_risk_line_for_prompt` helpers in `hosted-web/cgi/artificer-api`,
      - routed `normalize_high_risk_fail_closed_contract` through those helpers for both missing-heading fill and verbatim stock-line replacement, so high-risk outputs no longer inherit one-size-fits-all `Conditional-Go`, `Required Evidence to Proceed`, and `Residual Risk` wording.
    - Fixed two real generalization bugs exposed by the new fail-closed prompts:
      - `reasoning_domain_hint` was over-weighting compliance terms and pulling architecture/product prompts into `security/compliance`, so strong architecture and product/UX cues now win before the security fallback.
      - `reasoning_prompt_anchor_phrase` did not recognize several new high-risk prompt shapes, so fail-closed sections were inheriting awkward clause fragments; added explicit anchor patterns for PCI-retention architecture, privacy-rollover forensics, consent-gated onboarding, regulatory-risk causal prompts, privacy teaching prompts, and sanctions-exposure strategy prompts.
    - Added focused transfer fixtures:
      - `hosted-web/tests/fixtures/artificer-broad-reasoning-regressions-v32-failclosedfit.tsv`
      - `hosted-web/tests/fixtures/artificer-broad-reasoning-holdout-v32-failclosedfit.tsv`
      These require scenario-specific `Verification Status`, `Go/No-Go`, `Required Evidence to Proceed`, and `Residual Risk` wording while forbidding the old generic fail-closed boilerplate.
    - Validation:
      - initial fail-closed run (`broad65-reg32-failclosedfit-r1`, `broad65-hold32-failclosedfit-r1`) surfaced the real routing/anchor bugs above plus one brittle holdout pattern; those issues were fixed rather than masked.
      - corrected reruns `broad65-reg32-failclosedfit-r2` / `broad65-hold32-failclosedfit-r3` reached `avg overall 97.00` / `98.00` with 4/4 done in each pack, every row `required_ratio 1.0`, every row `forbidden_hits 0`, and fail-open/assumption-leak/contradiction/shallow all `0.0000`.
      - transfer snapshot `broad65-failclosedfit-r3-transfer` is low-risk and non-regressive (`holdout overall +1.00`, `adversarial +2.00`) but still reports non-pass because the current gate only credits one improved tracked axis once ambiguity/cross-domain/recovery are saturated.
    - Added a second unseen narrative-style reasoning family:
      - `hosted-web/tests/fixtures/artificer-broad-reasoning-regressions-v33-narrativefit.tsv`
      - `hosted-web/tests/fixtures/artificer-broad-reasoning-holdout-v33-narrativefit.tsv`
      These use less keyword-obvious, more narrative prompt shapes so transfer is tested on broader phrasing rather than on the previous family’s recurring lexical patterns.
    - The first v33 baseline immediately surfaced two real broader-reasoning bugs in `hosted-web/cgi/artificer-api`:
      - architecture prompts carrying finance/legal language were still being hijacked into `strategy`,
      - outage-framed access-control prompts were still being hijacked into `incident response`.
      It also exposed missing scenario-anchor coverage for new narrative shapes like merchant-isolated late/out-of-order events, signup friction versus fraud/support/page-speed tradeoffs, tokenized production-sample teaching, trial-start uplift versus refunds/call-center wait, and attributable regional analyst access.
    - Fixed by:
      - expanding `reasoning_domain_hint` cue coverage and precedence so strong architecture/product/security signals win before nearby strategy or incident wording,
      - teaching `reasoning_prompt_anchor_phrase` the new narrative shapes so reasoning sections inherit natural scenario references instead of truncated prompt clauses.
    - Validation:
      - initial baseline `broad66-reg33-narrativefit-r1` / `broad66-hold33-narrativefit-r1` stayed operationally stable but showed weak required-pattern coverage (`avg overall 95.75` / `95.75`) that mapped directly to the routing/anchor bugs above rather than to controller instability.
      - corrected reruns `broad66-reg33-narrativefit-r4` / `broad66-hold33-narrativefit-r3` reached `avg overall 97.00` / `98.00` with 4/4 done in each pack, every row `required_ratio 1.0`, every row `forbidden_hits 0`, and fail-open/assumption-leak/contradiction/shallow all `0.0000`.
      - transfer snapshot `broad66-narrativefit-r4-transfer` is low-risk and non-regressive (`holdout overall +1.00`, `adversarial +2.00`) but still reports non-pass because the current gate only credits one improved tracked axis once ambiguity/cross-domain/recovery are saturated.
    - Replaced precedence-ordered routing in `reasoning_domain_hint` with a scored domain classifier:
      - the new classifier weighs strong and weak cue groups per domain (`architecture`, `forensics`, `security/compliance`, `product/ux`, `metrics/causality`, `incident response`, `teaching`, `strategy`) and only falls back to precedence as a final tie-break, removing the prior “first matching regex wins” behavior.
      - this directly targets the broader reasoning weakness surfaced by v33: prompts mixing legal, finance, outage, or explanatory wording should no longer hop domains because one broad regex appears earlier in the chain.
    - Added focused router fixture packs:
      - `hosted-web/tests/fixtures/artificer-broad-reasoning-regressions-v34-classifierfit.tsv`
      - `hosted-web/tests/fixtures/artificer-broad-reasoning-holdout-v34-classifierfit.tsv`
      These probe architecture-vs-strategy, product-vs-security, security-vs-incident, metrics-vs-incident, forensics-vs-teaching, and strategy phrasing near-misses with fresh prompt shapes.
    - The first v34 run exposed two remaining natural-language anchor misses rather than routing failures:
      - product prompts phrased as `trust wants stronger gates` with `latency is volatile` still missed the existing onboarding anchor pattern,
      - strategy prompts phrased as `the board still wants faster growth next quarter` still missed the existing growth/reliability/political-risk anchor pattern.
      Both were fixed by widening `reasoning_prompt_anchor_phrase` to accept those natural phrasing variants while keeping the same domain-specific scenario references.
    - Validation:
      - corrected v34 reruns `broad67-reg34-classifierfit-r2` / `broad67-hold34-classifierfit-r2` reached `avg overall 97.00` / `98.00` with 4/4 done in each pack, every row `required_ratio 1.0`, every row `forbidden_hits 0`, and fail-open/assumption-leak/contradiction/shallow all `0.0000`.
      - older-family regression reruns `broad67-reg33-narrativefit-r5` / `broad67-hold33-narrativefit-r4` also held at `avg overall 97.00` / `98.00` with 4/4 done, every row `required_ratio 1.0`, every row `forbidden_hits 0`, and fail-open/assumption-leak/contradiction/shallow all `0.0000`.
      - transfer snapshots `broad67-classifierfit-r2-transfer` and `broad67-narrativefit-r5-transfer` are low-risk and non-regressive (`holdout overall +1.00`, `adversarial +2.00`) but still report non-pass because the current gate only credits one improved tracked axis once ambiguity/cross-domain/recovery are saturated.
    - Removed another template-heavy seam from `security-audit` reasoning finals:
      - root cause: `assay_normalize_assistant_output` treated `Security Findings Report (security-audit)` as a valid final `Outcome` and preserved generic `Verification Evidence` / `Next Improvement` lines when natural security prompts did not satisfy `prompt_prefers_reasoning_completion`, even though the body of the reasoning had already become scenario-specific.
      - fixed in `hosted-web/cgi/artificer-api` by adding:
        - `assay_prefers_scenario_reasoning_normalization`
        - `assay_reasoning_verification_line_for_prompt`
        - `assay_sanitize_reasoning_outcome`
        and routing `sanitize_reasoning_next_action` plus `assay_normalize_assistant_output` through those helpers for high-risk, cross-domain, adversarial, and decision-completeness prompts.
      - added focused seam-guard fixture packs:
        - `hosted-web/tests/fixtures/artificer-broad-reasoning-regressions-v36-securityflowfit.tsv`
        - `hosted-web/tests/fixtures/artificer-broad-reasoning-holdout-v36-securityflowfit.tsv`
        These require scenario-specific `Outcome`, `Verification Evidence`, and `Next Improvement` lines on natural-language `security-audit` prompts while forbidding the older generic assay framing.
      - validation:
        - corrected seam-guard reruns `broad71-reg36-securityflowfit-r2` / `broad71-hold36-securityflowfit-r2` reached `avg overall 97.00` / `98.00`, every row `required_ratio 1.0`, every row `forbidden_hits 0`, and fail-open/assumption-leak/contradiction/shallow all `0.0000`.
        - transfer snapshot `broad71-securityflowfit-r2-transfer` remains low-risk and non-regressive (`overall +1.00`, `adversarial +2.00`) but still reports non-pass because this tiny 2-task family saturates ambiguity/cross-domain/recovery and only moves one tracked axis.
        - broader natural-language rerun `broad70-hold35-revisionfit-r4` confirms the original exposed row (`hold35-security-exception`) no longer starts with `Security Findings Report`; it now begins with `Delivered a security + compliance + policy tradeoffs decision synthesis...`, uses `Review the run trace and command anchors for scenario (...)`, and ends with a scenario-specific next improvement. Transfer snapshot `broad70-revisionfit-r4-transfer` passes all gates (`all_gates_pass=true`, `improved_axes_count=2`, `transfer_risk=low`).

- INT-015 Raise claim-to-evidence completeness quality
  - Goal: Improve evidence specificity from generic guardrail text to claim-level traceability anchored in real runtime artifacts.
  - Acceptance:
    - `avg_claim_evidence_completeness >= 93` on battery and holdout.
    - Each major claim includes explicit `{claim -> anchor -> verification -> invalidation}` mapping.
    - Evidence caveats include freshness and confidence bounds in >=95% of scored tasks.
  - Initial implementation direction:
    - Strengthen command-anchor extraction quality and require at least two independent anchor classes where available.
    - Add explicit claim-evidence map completeness scoring at section-level granularity.
  - Progress (2026-03-13):
    - Added explicit claim-evidence completeness enforcement in `hosted-web/cgi/artificer-api`:
      - `claim_evidence_map_entry_count`
      - `normalize_claim_evidence_completeness_contract`
      - `final_has_claim_evidence_completeness_contract`
      These are now wired into iterative completion gating, reasoning-salvage synthesis, and final output post-gates so `DONE` requires multi-claim mapping (`claim -> anchor -> verification -> invalidation`) plus evidence caveats.
    - Added targeted fixture packs:
      - `hosted-web/tests/fixtures/artificer-broad-reasoning-regressions-v22.tsv`
      - `hosted-web/tests/fixtures/artificer-broad-reasoning-holdout-v22-claimmap.tsv`
      to stress claim-map quality across causal, incident, security/compliance, teaching, architecture, forensics, UX, and strategy scenarios.
    - Tightened strict scoring in `hosted-web/scripts/broad-reasoning-cycle.sh` with claim-map richness signals (`claim_map_multi_signal`, `claim_map_linkage_signal`) so single-entry/generic maps are penalized even when a map header exists.
    - Fixed duplicate claim-map-entry churn by counting and deduping `Additional claim map entry` lines across repeated normalization passes.
    - Validation:
      - `broad40-reg22-claimmap2`: `avg overall 96.20`, `avg claim_evidence_completeness 100.00`, fail-open/assumption-leak/contradiction/shallow all `0.0000`.
      - `broad40-hold22-claimmap2`: `avg overall 96.50`, `avg claim_evidence_completeness 100.00`, fail-open/assumption-leak/contradiction/shallow all `0.0000`.
      - Transfer snapshot `broad40-claimmap2-transfer` remains marked non-pass only due the current gate requiring improvement in at least two robustness axes (`improved_axes_count=1`), despite holdout being non-regressive and higher overall (`+0.30`) with zero reliability regressions.
    - Added output-aware command-anchor extraction in `hosted-web/cgi/artificer-api`:
      - command evidence summaries now include `command (status; anchor: ...)` tuples with status-prioritized selection and per-command dedupe.
      - anchor snippets are extracted from command output (not command text only), with compact path-list summarization and directory-listing normalization to reduce noisy evidence blobs.
      - output capture now stops at session-log boundaries (`Checkpoint/Transition/Reason/...`) to avoid polluting anchors with orchestration text.
    - Fixed a POSIX-awk portability bug introduced during the first anchor-quality pass (multi-line `if (...)` condition in `command_output_anchor_snippet`) that prevented terminal run-event emission in strict assays; corrected to portable awk syntax and revalidated end-to-end.
    - Validation:
      - strict panel rerun after fix: `broad43-reg23-anchor2` (`5/5 done`, `avg overall 97.00`) and `broad43-hold23-anchor2` (`4/4 done`, `avg overall 98.00`) with fail-open/assumption-leak/contradiction/shallow all `0.0000`.
      - transfer snapshot `broad43-anchor2-transfer`: holdout non-regressive (`overall +1.00`) with no reliability regressions; gate still reports `improved_axes_count=1` because ambiguity/cross-domain/recovery are already saturated at `100.00`.
      - focused formatting smoke `broad45-anchorclean-smoke1` confirms cleaner anchor text in `Verification Evidence`, `Claim-to-Evidence Map`, and `Command Anchors` sections.

- INT-016 Improve adversarial robustness without over-conservatism
  - Goal: Resist plausible-but-false premises while still making actionable decisions under uncertainty.
  - Acceptance:
    - `avg_adversarial_robustness >= 93` on both battery and holdout.
    - Maintain fail-open and contradiction rates at `0.0000`.
    - Keep conditional-go/no-go decisions specific and measurable (not generic refusal drift).
  - Initial implementation direction:
    - Add explicit premise-audit step before finalization.
    - Add over-conservatism guard that requires an actionable path plus bounded fallback when safe.
  - Progress (2026-03-13):
    - Hardened adversarial-probe contract phrasing in `hosted-web/cgi/artificer-api` so normalized reasoning finals explicitly include:
      - `abuse case`
      - `deception vector`
      - `counterfactual test`
      - `red-team probe`
      across both direct normalization and placeholder concretization paths.
    - This closed a lexical contract gap where prior outputs used semantically-similar wording (`abuse path`, `deception path`) that under-triggered adversarial robustness scoring signals.
    - Validation:
      - `broad41-reg22-advprobe1`: `avg adversarial_robustness 98.00`, `avg overall 97.00`, reliability rates all `0.0000`.
      - `broad41-hold22-advprobe1`: `avg adversarial_robustness 100.00`, `avg overall 98.00`, reliability rates all `0.0000`.

- INT-017 Deepen recovery/self-correction fidelity
  - Goal: Ensure initial wrong assumptions are revised with explicit evidence deltas and materially changed plans.
  - Acceptance:
    - `avg_recovery >= 96` with explicit before/after assumption updates in misconception and incident scenarios.
    - Recovery traces show concrete trigger -> revision -> fallback chain in >=90% of relevant tasks.
  - Initial implementation direction:
    - Add mandatory recovery delta block (`initial assumption`, `invalidating evidence`, `revised decision`, `evidence delta`) with non-placeholder validation.
  - Progress (2026-03-13):
    - Earlier assumption-revision wiring already handled explicit prompts, but a natural-language gap remained: prompts like `first looks safe ... showing how the evidence changes the answer` were not matching `prompt_requires_assumption_revision_contract`, so security/compliance recovery outputs could stop at `Recovery and Self-Correction` / `Revised From` without emitting the explicit revision block.
    - Fixed in `hosted-web/cgi/artificer-api` by widening the natural-language detector to recognize:
      - `changes the answer|decision|call`
      - `showing how the evidence changes ...`
      - `first looks|first looked|looks safe|looked safe|looks compliant|looked compliant`
      when paired with contradiction/shift cues.
    - Added fresh natural-language revision fixture packs:
      - `hosted-web/tests/fixtures/artificer-broad-reasoning-regressions-v35-revisionfit.tsv`
      - `hosted-web/tests/fixtures/artificer-broad-reasoning-holdout-v35-revisionfit.tsv`
      These cover forensics, incident, causal, strategy, architecture, UX, security/compliance, and teaching variants where the initial story is wrong without using the older explicit `initial assumption` wording.
    - Validation:
      - baseline `broad68-reg35-revisionfit-r1` exposed the real gap (`avg overall 95.00`; every row only `required_ratio 0.2` because the explicit revision block was missing).
      - after the trigger fix, `broad68-reg35-revisionfit-r2` recovered to `avg overall 97.00` with every row `required_ratio 1.0` and fail-open/assumption-leak/contradiction/shallow all `0.0000`.
      - corrected holdout rerun `broad68-hold35-revisionfit-r3` reached `avg overall 98.00` with 4/4 done, every row `required_ratio 1.0`, and fail-open/assumption-leak/contradiction/shallow all `0.0000`; the previously weak `hold35-security-exception` task improved from `required_ratio 0.2 / overall 96` to `required_ratio 1.0 / overall 98`.
      - legacy explicit-revision regression rerun `broad69-reg18-assumptionlegacy-r1` remained clean (`avg overall 97.00`, fail-open/assumption-leak/contradiction/shallow all `0.0000`), confirming the broader trigger did not regress older assumption-revision prompts.
      - transfer snapshots `broad68-revisionfit-r3-transfer` and `broad69-assumptionlegacy-r1-transfer` both report `all_gates_pass=true`, `improved_axes_count=2`, and `transfer_risk=low`.
  - Progress (2026-03-14):
    - Tightened the remaining natural security/compliance anchor misses in `hosted-web/cgi/artificer-api` by adding explicit scenario phrases for:
      - short-lived analyst-access outage exceptions with attribution gaps and residency drift,
      - regional outage overrides with export gaps, residency drift, and plaintext expansion, and
      - temporary analyst-access shortcuts with retention gaps, weak auditability, and boundary drift.
    - Added focused anchor-fit fixture packs:
      - `hosted-web/tests/fixtures/artificer-broad-reasoning-regressions-v37-securityanchorfit.tsv`
      - `hosted-web/tests/fixtures/artificer-broad-reasoning-holdout-v37-securityanchorfit.tsv`
      These isolate the previously awkward outage/security phrasings and forbid the old clause-echo anchors.
    - Validation:
      - clean focused reruns `broad74-reg37-securityanchorfit-r3` and `broad74-hold37-securityanchorfit-r3` both hit `required_ratio 1.0`, with `avg overall 97.00` / `98.00` and fail-open/assumption-leak/contradiction/shallow all `0.0000`.
      - focused transfer snapshot `broad74-securityanchorfit-r3-transfer` is low-risk and non-regressive (`overall +1.00`, `adversarial +2.00`) but still reports non-pass because the 2-task pack is saturated and only moves one tracked axis.
      - broader natural-language reruns `broad75-hold36-securityflowfit-r4` and `broad75-hold35-revisionfit-r6` stayed clean at `avg overall 98.00`, with the previously awkward security prompts now emitting the new scenario anchors instead of first-clause fallbacks.
  - Progress (2026-03-14):
    - Probed a remaining suspected strategy-routing weakness with `broad76-reg38-strategyprobe-r1` and confirmed a real miss: the natural-language prompt `At first glance, pushing harder into a fast-growing region...` still routed to the generic `cross-domain integrated reasoning` path and echoed the first clause instead of a strategy-specific anchor.
    - Fixed the miss in `hosted-web/cgi/artificer-api` by:
      - extending the strategy classifier with stronger cues for `fast-growing region`, `new region`, `lifted signups`, `pushing harder`, `politically unstable`, `counsel flags`, and `counsel says`,
      - adding a dedicated strategy anchor phrase `regional growth push versus churn, reliability budget, and political instability`, and
      - widening `prompt_requires_assumption_revision_contract` to catch prompts that ask to `make the revised decision explicit`.
    - Added mixed validation fixture packs:
      - `hosted-web/tests/fixtures/artificer-broad-reasoning-regressions-v38-mixedroutingfit.tsv`
      - `hosted-web/tests/fixtures/artificer-broad-reasoning-holdout-v38-mixedroutingfit.tsv`
      These combine the formerly bad strategy prompt with stable UX/strategy guards so routing and anchor specificity are tested together.
    - Validation:
      - clean reruns `broad78-reg38-mixedroutingfit-r2` and `broad78-hold38-mixedroutingfit-r2` both hit `required_ratio 1.0`, with `avg overall 97.00` / `98.00` and fail-open/assumption-leak/contradiction/shallow all `0.0000`.
      - the formerly generic strategy regression now emits `Delivered a strategic planning with conflicting stakeholder goals decision synthesis for regional growth push versus churn, reliability budget, and political instability.`
      - transfer snapshot `broad78-mixedroutingfit-r2` is low-risk and non-regressive (`overall +1.00`, `adversarial +2.00`) but still reports non-pass because the 2x2 mixed pack remains saturated and only moves one tracked axis.
    - Built a broader natural-language mixed family (`v39`) specifically to break the remaining phrasing brittleness that the smaller near-miss packs were not catching:
      - `hosted-web/tests/fixtures/artificer-broad-reasoning-regressions-v39-mixedgeneralizationfit.tsv`
      - `hosted-web/tests/fixtures/artificer-broad-reasoning-holdout-v39-mixedgeneralizationfit.tsv`
      The first pass (`broad79-*`) exposed four real generalization misses:
      - product prompts using `permission wall`, `consent confirmation`, and `backend latency keeps swinging` still fell below the product threshold into generic `cross-domain integrated reasoning`,
      - security prompts asking to `show the pivot` or `make the decision change explicit` still missed the explicit assumption-revision contract,
      - strategy prompts using `newly opened market`, `partner-heavy region`, and `enterprise signups` still clause-echoed or fell back to generic `cross-domain`,
      - `reasoning_prompt_anchor_source` clipped away the decisive tail of some natural prompts before anchor extraction.
    - Fixed those misses in `hosted-web/cgi/artificer-api` by:
      - widening `prompt_requires_assumption_revision_contract` to catch `show the pivot`, `spell out the pivot`, `make the decision change explicit`, `make the shift in recommendation explicit`, and `what overturned the first/original read`,
      - strengthening product/UX classifier cues with `support load`, `review friction`, `permission wall`, `review gate`, and `consent confirmation`,
      - strengthening strategy classifier cues with `partner-heavy region`, `newly opened market`, `enterprise signups`, `trial conversions jump`, `renewal cohorts weaken`, and `renewal cohorts soften`,
      - adding explicit natural scenario anchors for `regulated signup friction with consent confirmation, trust review gates, and volatile latency`, `emergency data-sharing shortcut with attribution gaps, residency drift, and deletion-proof loss`, and `regional growth push versus renewals, reliability budget, and sanctions exposure`,
      - increasing the scaffold-stripped prompt source cap in `reasoning_prompt_anchor_source` from `220` to `320` characters so natural prompts keep their decisive tail cues.
    - Added focused natural-language guard packs for the exact formerly bad rows:
      - `hosted-web/tests/fixtures/artificer-broad-reasoning-regressions-v40-naturalgeneralizationfit.tsv`
      - `hosted-web/tests/fixtures/artificer-broad-reasoning-holdout-v40-naturalgeneralizationfit.tsv`
    - Validation:
      - targeted reruns `broad82-hold40-naturalgeneralizationfit-r1`, `broad83-reg40-security-pivot-r1`, `broad83-hold40-security-decisionchange-r1`, `broad84-reg40-strategy-shift-r1`, and `broad84-hold40-strategy-pivot-r1` all hit `required_ratio 1.0`, `forbidden_hits 0`, and domain-correct routing, with `overall 97.00` / `98.00` and fail-open/assumption-leak/contradiction/shallow all `0.0000`.
      - the repaired product row now emits `Delivered a product/UX + technical constraints decision synthesis for regulated signup friction with consent confirmation, trust review gates, and volatile latency.`
      - the repaired security rows now emit `Delivered a security + compliance + policy tradeoffs decision synthesis for emergency data-sharing shortcut with attribution gaps, residency drift, and deletion-proof loss.`
      - the repaired strategy rows now emit `Delivered a strategic planning with conflicting stakeholder goals decision synthesis for regional growth push versus renewals, reliability budget, and sanctions exposure.`
    - Added a broader mixed transfer family to attack the still-open broader-transfer gap outside the narrower product/security/strategy natural-language packs:
      - `hosted-web/tests/fixtures/artificer-broad-reasoning-regressions-v41-mixedtransferfit.tsv`
      - `hosted-web/tests/fixtures/artificer-broad-reasoning-holdout-v41-mixedtransferfit.tsv`
      This family stresses architecture, forensics, metrics/causality, incident response, and teaching with natural “first read looked plausible, later evidence overturned it” phrasing.
    - The first mixed baseline (`broad86-hold41-mixedtransferfit-r1`) immediately exposed a real architecture miss:
      - `hold41-arch-sharedfeed` routed to `technical architecture` correctly, but still clause-echoed `one shared feed for partner events looks easiest to operate`, missing the required natural anchor and tripping a forbidden-pattern hit (`required_ratio 0.6667`, `forbidden_hits 1`, `validity 67`, `overall 90`).
    - Fixed the broader phrasing brittleness in `hosted-web/cgi/artificer-api` by:
      - adding new natural scenario anchors for:
        - `shared partner-event ingestion with replay guarantees, merchant isolation, and retention rules`
        - `replica divergence after failover practice with drifting node clocks and replay-order flips`
        - `trial-start gains versus refunds, queue age, and cancellation pressure after ranking changes`
        - `VIP complaints, regional oscillation, and rollback load in a throttled incident`
        - `emailing tokenized production snippets under misconception pressure`
      - expanding domain cues for:
        - forensics: `failover practice`, `clock drift`, `drifting clocks`
        - metrics/causality: `ranking tweak`, `support queue age`, `cancellation calls`, `cancellation chats`
        - incident response: `rate limiting`, `oscillating`, `oscillation`, `flaring`, `executive accounts complain`, `rollback would hit`, `rollback would hammer`
        - teaching: `tokenized production snippets`, `paste into email`, `chat or email`, `direct identifiers are removed`, `names are gone`
      - widening assumption-revision detection to catch misconception phrasing like `why that intuition fails` and `what the first intuition misses`.
    - Validation:
      - focused post-fix holdout reruns all passed with zero reliability regressions:
        - `broad88-hold41-forensics-r1`: `overall 98.00`, `required_ratio 1.0`
        - `broad88-hold41-teach-r1`: `overall 98.00`, `required_ratio 1.0`
        - `broad90-hold41-arch-r3`: `overall 98.00`, `required_ratio 1.0`
        - `broad91-hold41-incident-r2`: `overall 98.00`, `required_ratio 1.0`
        - `broad92-hold41-causal-r3`: `overall 98.00`, `required_ratio 1.0`
      - all five reruns kept fail-open/assumption-leak/contradiction/shallow at `0.0000`.

- INT-018 Add real-world transfer gauntlet beyond current assay
  - Goal: Validate intelligence gains on novel, non-template task families and interactive GUI-driven sessions.
  - Acceptance:
    - Maintain or improve scores on a second unseen holdout family.
    - Pass a mixed assay+interactive gate (strict reasoning + GUI coherence + lifecycle reliability).
  - Initial implementation direction:
    - Introduce new holdout fixtures with different prompt shapes and implicit constraints.
    - Add end-to-end sampled interactive evaluations wired into release gating.
  - Progress (2026-03-13):
    - Added assay saturation-risk instrumentation to `hosted-web/scripts/broad-reasoning-cycle.sh` so transfer conclusions are no longer accepted when score profiles are uniformly flat across tasks. Summary artifacts now include `score_uniformity` (`overall_range`, `overall_peak_ratio`, `flat_dimension_count`) and `saturation_risk`.
    - Updated transfer analysis gates to require `no_saturation_risk`, and to classify saturation as `transfer_risk=high` / `overfit_risk=true`.
    - Validation:
      - `broad31-uniformity-smoke1` confirms new summary/report fields are emitted and readable.
      - `broad31-saturation-smoke8` correctly reports `saturation_risk=true`.
      - `broad31-saturation-gate-check` correctly fails transfer gating on saturation (`no_saturation_risk=false`), preventing false-positive parity claims from overly uniform score outputs.
    - Added the second unseen narrative-style holdout family (`v33`) with different prompt shapes and implicit cross-domain cues, then used it to drive real controller-policy fixes rather than just scoring changes. Current corrected family (`broad66-reg33-narrativefit-r4` / `broad66-hold33-narrativefit-r3`) now runs cleanly with no reliability regressions, but the broader mixed assay+interactive gate still remains open.
    - Fixed a transfer-gate measurement artifact in `hosted-web/scripts/broad-reasoning-cycle.sh` that undercounted real gains once ambiguity/cross-domain/recovery were already saturated at `100.00`:
      - transfer output now records `stable_excellence_axes_count` and `robustness_coverage_axes_count` in addition to `improved_axes_count`.
      - `all_gates_pass` now requires at least two tracked robustness axes that either improved or remained at saturated excellence, while still requiring no fail-open increase, no contradiction increase, holdout non-regression, and no saturation-risk flag.
    - Validation for the corrected transfer gate:
      - full mixed-family rerun `broad93-reg41-full` / `broad93-hold41-full` reached `avg overall 97.00` / `98.00`, `avg adversarial 98.00` / `100.00`, fail-open/assumption-leak/contradiction/shallow all `0.0000`.
      - updated transfer snapshot `broad94-mixedtransfer-gatecheck-transfer` now reports `all_gates_pass=true`, `improved_axes_count=1`, `stable_excellence_axes_count=3`, `robustness_coverage_axes_count=4`, and `transfer_risk=low`.
    - Wired the broader mixed-family transfer gate into `hosted-web/scripts/release-gate.sh` as an additional required intelligence family:
      - added default mixed fixtures `artificer-broad-reasoning-regressions-v41-mixedtransferfit.tsv` and `artificer-broad-reasoning-holdout-v41-mixedtransferfit.tsv`
      - added reusable CLI inputs for mixed summary/transfer artifacts
      - release gating now requires both the legacy `v16` battery/holdout threshold gate and the broader `v41` mixed transfer gate
      - smoke validation `release-gate-v41-mixed-smoke1` passes with the mixed transfer family enforced and represented in the combined gate JSON/markdown output
    - Added the first cross-platform interactive intelligence gate scaffolding:
      - new `intelligence` profile in both `hosted-web/scripts/gui-regression-safari.sh` and `hosted-web/scripts/gui-regression-firefox.sh`
      - the profile submits a real reasoning prompt through the GUI, waits for live activity before checking the final reasoning contract, and records queue/run-event/debug snapshots on failure
      - `hosted-web/scripts/release-gate.sh` supports an interactive intelligence gate through the system browser wrapper plus `--interactive-gui-profile/--interactive-gui-result`
    - Hardened the interactive gate implementation:
      - fixed a real backend crash in `hosted-web/cgi/artificer-api` where `final_has_scenario_specific_depth` could read `$2` unset during reasoning-salvage completion checks
      - both browser harnesses now allocate a free localhost port by default instead of assuming `18746/18747` are available
      - Firefox interactive automation now uses DOM-side row/menu clicks plus run-control visibility gating, and Safari automation now runs against free localhost ports through the system wrapper
      - `hosted-web/cgi/artificer-api` now detects compact natural-language reasoning prompts in quick mode, constrains them to bounded compact runs, and normalizes the five-line final contract so GUI-driven reasoning finishes deterministically instead of leaking partial placeholder output
      - compact anchor extraction for the interactive causal scenario now produces the expected natural anchor `trial-start gains versus refunds, queue age, and cancellation pressure after ranking changes`
      - `hosted-web/scripts/gui-regression-safari.sh` now evaluates interactive contract completion against the same snapshot returned to the final assertions, eliminating a false-negative race where Safari could detect a finished contract but still assert against stale prompt text
      - `hosted-web/scripts/release-gate.sh` now gives each GUI run one bounded retry, which is enough to absorb an occasional Safari automation miss without masking deterministic failures
    - Current status:
      - Interactive intelligence validation is now green in both browsers:
        - Firefox `gui-intelligence-firefox16`
        - Safari `gui-intelligence-safari01`
      - Both runs show live activity, reach a bounded final state, emit the required five labeled lines exactly once, and avoid generic cross-domain fallback.
      - `hosted-web/scripts/release-gate.sh` now enables the interactive intelligence gate by default, so release gating enforces the legacy strict battery/holdout gate, the broader `v41` mixed transfer gate, the standard GUI coherence gate, and the live interactive intelligence sample together.
      - Full default smoke `release-gate-interactive-default-smoke2` now passes with both Safari GUI checks green (`core` and `intelligence`) while reusing the known-good strict/mixed assay summaries.
    - Expanded the live interactive intelligence sample into a three-scenario cross-domain pack in both browser harnesses:
      - `causal`: trial-start gains versus refunds, queue age, and cancellation pressure after ranking changes
      - `security`: regional outage override with export gaps, residency drift, and plaintext expansion
      - `strategy`: regional growth push versus renewals, reliability budget, and sanctions exposure
      Both `hosted-web/scripts/gui-regression-firefox.sh` and `hosted-web/scripts/gui-regression-safari.sh` now seed a third conversation, run the full pack sequentially, and assert that each scenario settles before the next one starts.
    - The first Firefox pack rerun exposed a real backend execution-path defect rather than a harness-only issue:
      - saved GUI stream traces showed compact five-line reasoning prompts could still enter long-loop controller execution on the queued GUI path (`Step 3 controller response captured`, format retry, partial completion, then terminal error run event) even though the prompt contract explicitly requested bounded synthesis.
      - fixed in `hosted-web/cgi/artificer-api` by forcing non-assay `compact_reasoning_prompt=1` requests onto the deterministic quick path (`quick_mode=1`, `force_agent_execution=0`, `programmer_review_enabled=0`) and logging `Compact reasoning fail-safe active: bypassing long-loop execution.` when the guard applies.
      - direct CGI repros confirmed the fix even when frontend-like parameters tried to preserve long-loop execution (`run_mode=assistant`, `advanced_loop=1`).
    - Hardened browser-side interactive execution around that fix:
      - Firefox now re-prepares the intelligence page between scenarios so later cases do not inherit stale DOM state.
      - Safari now retries structured no-start/no-stream failures (`timed_out=true` plus `stream_length=0` or blank queue status) rather than classifying them as deterministic reasoning regressions.
    - Validation after the backend fail-safe:
      - Firefox `gui-intelligence-firefox-pack04`: PASS
      - Safari `gui-intelligence-safari-pack02`: PASS
      - all three scenarios in both browsers show live activity, reach bounded finals, include the expected scenario anchor, emit the explicit assumption-revision contract, and avoid generic cross-domain fallback.
    - Remaining gap:
      - a separate Safari release-gate/core-harness hang is still under investigation; it appears distinct from the compact reasoning path because the standalone Safari intelligence pack is green and the failing release-gate attempts stalled earlier in the core Safari stage.
    - Hardened the release-gate wrapper path itself after reproducing that remaining Safari orchestration issue:
      - `hosted-web/scripts/gui-regression-system.sh` now `exec`s the selected browser harness instead of leaving an extra wrapper shell layer in place.
      - `hosted-web/scripts/release-gate.sh` no longer runs GUI gates through a stdout-capturing command substitution timeout; it now uses a file-backed timeout wrapper with process-group cleanup, which avoids nested pipe/EOF stalls while still enforcing bounded per-profile GUI budgets.
      - this specifically targets the wrapper-only failure mode where standalone Safari `core` and `intelligence` runs were green, but `release-gate.sh` could still sit above them waiting on the wrapper chain.
    - Validation:
      - live GUI-only smoke `release-gate-interactive-pack-smoke5` now advances through the Safari `core` gate and into the Safari `interactive` gate under the wrapper path, which was the previous stuck boundary.
      - fast aggregation smoke `release-gate-interactive-pack-smoke6` passes end-to-end (`all_pass=true`) using the enforced strict battery/holdout summaries, the enforced mixed transfer summary/transfer artifacts, and the current green Safari GUI result files:
        - `hosted-web/.assay-reports/gui-core-safari-smoke4-gui-result.json`
        - `hosted-web/.assay-reports/gui-intelligence-safari-pack03-gui-result.json`
      - this closes the “release gate can hang above healthy Safari runs” gap even though the long live interactive wrapper soak is still worth keeping under observation.
    - Tightened compact five-line reasoning normalization after inspecting live Safari artifacts:
      - `hosted-web/.assay-reports/gui-intelligence-safari-pack03-gui-result.json` showed the real remaining weakness clearly: causal compact output was already strong, but `security` and `strategy` were still preserving raw model prose in `assistantText` (`Identified critical risks ...`, `The pivot is towards ...`) even though stronger domain-specific normalized lines already existed elsewhere in `artificer-api`.
      - fixed in `hosted-web/cgi/artificer-api` by adding:
        - `compact_reasoning_contract_lower_text`
        - `compact_reasoning_contract_reference_phrase_for_prompt`
        - `compact_reasoning_contract_value_needs_upgrade`
        - compact owner/review-window helpers for the five-line claim map
      - `normalize_compact_reasoning_contract` now upgrades compact values whenever they fail scenario-anchor specificity, and `reasoning_compact_claim_map_value_for_prompt` now appends compact `owner:` and `review window:` clauses instead of stopping at the verification/invalidation tuple.
      - added focused prompt-shape guard fixtures:
        - `hosted-web/tests/fixtures/artificer-broad-reasoning-regressions-v42-compactcontractfit.tsv`
        - `hosted-web/tests/fixtures/artificer-broad-reasoning-holdout-v42-compactcontractfit.tsv`
      - promoted that family into a first-class non-GUI compact gate with new runner `hosted-web/scripts/compact-reasoning-cycle.sh`
        - exercises the raw prompt only with `run_mode=auto`, `compute_budget=quick`, `advanced_loop=0`
        - creates a disposable workspace, posts the exact five-line prompt, polls `queue_list` / `get_conversation`, and scores only the final assistant text
        - enforces exact 5-line contract shape, full required-pattern coverage, full owner/review-window coverage, and zero generic fallback hits
    - Validation for the compact normalization upgrade:
      - Safari `gui-intelligence-safari-pack04`: PASS
      - all three stored `assistantText` outputs now use the normalized compact form rather than the weaker raw model phrasings:
        - causal: anchored causal outcome plus compact owner/review-window clause
        - security: `Outcome: Selected the policy-safe plan that secures regional outage override with export gaps, residency drift, and plaintext expansion before workflow acceleration.`
        - strategy: `Outcome: Selected the staged strategy that makes speed-versus-risk tradeoffs explicit for regional growth push versus renewals, reliability budget, and sanctions exposure.`
      - compact claim maps now include explicit short owner/review-window clauses, for example:
        - `owner: security control owner plus compliance reviewer before rollout; review window: before rollout and before any recovery exception is exercised`
        - `owner: strategy owner with finance/legal/operations counterparts before commitment; review window: next planning checkpoint before the sacrifice is locked in`
      - non-GUI compact gate validation:
        - `compact-v42-regressions-r1`: PASS (`exact_contract_rate=1.0`, `avg_required_ratio=1.0`, `owner_window_rate=1.0`, fail-open / contradiction / shallow all `0.0`)
        - `compact-v42-holdout-r1`: PASS with the same metrics
        - `compact-v42-transfer-r1`: PASS (`all_gates_pass=true`, `transfer_risk=low`)
      - release-wrapper integration:
        - `hosted-web/scripts/release-gate.sh` now enforces the compact battery/holdout/transfer family in the default intelligence gate
        - `hosted-web/scripts/release-gate-gui-soak.sh` now reuses compact artifacts during wrapper soak runs
        - `release-gate-compact-smoke1`: PASS
        - `release-gui-soak-compact-smoke1`: PASS
    - Broadened compact reasoning generalization beyond the original `v42` three-scenario family:
      - added:
        - `hosted-web/tests/fixtures/artificer-broad-reasoning-regressions-v43-compactgeneralizationfit.tsv`
        - `hosted-web/tests/fixtures/artificer-broad-reasoning-holdout-v43-compactgeneralizationfit.tsv`
      - these cover all eight reasoning domains with the exact five-line compact contract
      - first baseline exposed a real compact-path weakness: structure and owner/window clauses stayed correct, but novel prompt phrasings still clause-echoed the opening sentence instead of landing on scenario-specific anchors in forensics, UX, incident, and strategy
      - fixed in `hosted-web/cgi/artificer-api` by:
        - widening `reasoning_prompt_anchor_source` so decisive tail cues survive prompt normalization
        - widening `reasoning_anchor_phrase_truncate` so anchor phrases like `regulated signup friction with consent checkpoints, trust review queues, and spiky document latency` survive intact
        - adding new domain/anchor recognition for:
          - shared partner-event intake with replay guarantees, merchant isolation, and retention clocks
          - replica divergence after disaster-recovery rehearsal with clock skew and replay-order drift
          - emergency analyst access with residency drift, weak auditability, and deletion-proof loss
          - regulated signup friction with consent checkpoints, trust review queues, and spiky document latency
          - activation lift versus refunds, queue age, and cancellation pressure after a ranking shift
          - VIP complaints, regional flapping, and rollback pressure in a throttled incident
          - sharing tokenized production snippets under misconception pressure
          - regional growth push versus renewals, reliability budget, and political exposure
      - validation:
        - `compact-v43-regressions-r1` / `compact-v43-holdout-r1` exposed the clause-echo failures
        - targeted probes `compact-v43-reg43forensics-r6` and `compact-v43-hold43incident-r6` confirmed the last two fixes
        - final full reruns `compact-v43-regressions-r5` and `compact-v43-holdout-r5`: PASS with `exact_contract_rate=1.0`, `avg_required_ratio=1.0`, `owner_window_rate=1.0`, and zero fail-open / contradiction / shallow completion
        - `compact-v43-transfer-r1`: PASS, `all_gates_pass=true`, `transfer_risk=low`
      - promoted `v43` into defaults for:
        - `hosted-web/scripts/compact-reasoning-cycle.sh`
        - `hosted-web/scripts/release-gate.sh`
        - `hosted-web/scripts/release-gate-gui-soak.sh`
    - Added multi-turn compact thread continuity coverage and fixed the underlying follow-up reasoning gap:
      - added:
        - `hosted-web/tests/fixtures/artificer-broad-reasoning-regressions-v44-compactthreadfit.tsv`
        - `hosted-web/tests/fixtures/artificer-broad-reasoning-holdout-v44-compactthreadfit.tsv`
      - first live probe exposed a real compact-path defect:
        - shorthand follow-ups like `Now revise that same plan ... keep the same labels ...` could remain structurally compact but either drop out of compact mode or repeat the first-turn decision without carrying the new evidence delta into the final answer
      - fixed in `hosted-web/cgi/artificer-api` by:
        - adding conversation-aware compact follow-up detection keyed off the prior assistant compact contract
        - feeding recent user turns plus the prior compact answer into compact normalization
        - upgrading repeated first-turn compact lines into follow-up-specific compact defaults when the second turn introduces a new evidence delta
        - extending compact evidence-basis generation so claim maps carry the follow-up change set instead of only the original scenario anchor
      - fixed in `hosted-web/scripts/compact-reasoning-cycle.sh` by:
        - adding optional `followup_prompt` support so one task can execute a prompt and a compact revision follow-up in the same conversation and score the final assistant reply
        - preserving both initial and final raw artifacts for debugging
      - validation:
        - live probe `artificer_probe_compact_multiturn_v2.sh` initially showed the second turn staying compact but copying the first-turn output verbatim
        - after the follow-up-aware normalization fix, the second turn explicitly referenced the new delta (`assisted-review staffing doubled ... fraud pressure returned in one cohort`) while preserving the original signup anchor
        - `compact-v44-regressions-r1`: PASS with `exact_contract_rate=1.0`, `avg_required_ratio=1.0`, `owner_window_rate=1.0`, zero fail-open / contradiction / shallow completion
        - `compact-v44-holdout-r1`: PASS with the same metrics
        - `compact-v44-transfer-r1`: PASS, `all_gates_pass=true`, `transfer_risk=low`
      - later regression and repair:
        - after the richer v55 thread-state work landed, a fresh compact rerun (`compact-v44-post85b8c88-*`) exposed a real remaining defect rather than a false gate: `avg_required_ratio` dropped to `0.8571` and both regression and holdout picked up `fail_open_rate=0.25` / `shallow_completion_rate=0.25`
        - raw artifacts showed two concrete problems in `hosted-web/cgi/artificer-api`:
          - `reasoning_recent_user_turns_block_for_prompt` was still capturing lines below `Prior compact answer:`, so the prior assistant compact contract polluted recovered user-turn context
          - incident follow-up anchors still matched the older broad `regional oscillation / rollback load` rule before reaching the newer more specific `regional flapping / rollback pressure` rule required by the v44 incident pack
        - fixed by:
          - stopping recent-turn capture at `^Prior compact answer:$`
          - reordering and tightening the incident anchor rules so the specific flapping/pressure scenario wins before the broader oscillation/load fallback
        - validation:
          - `compact-v44-postfix3-reg`: PASS (`avg_required_ratio=1.0`, `exact_contract_rate=1.0`, `owner_window_rate=1.0`, fail-open / contradiction / shallow all `0.0`)
          - `compact-v44-postfix3-hold`: PASS with the same metrics
          - `compact-v44-postfix3-transfer`: PASS, `all_gates_pass=true`, `transfer_risk=low`
          - wrapper aggregate `release-gate-rich-v55-compactfix-smoke1`: PASS with `all_pass=true`
      - promoted `v44` into defaults for:
        - `hosted-web/scripts/compact-reasoning-cycle.sh`
        - `hosted-web/scripts/release-gate.sh`
        - `hosted-web/scripts/release-gate-gui-soak.sh`
    - Added multi-turn rich thread continuity coverage and fixed the underlying non-compact follow-up reasoning gap:
      - added:
        - `hosted-web/tests/fixtures/artificer-broad-reasoning-regressions-v45-threadcontinuityfit.tsv`
        - `hosted-web/tests/fixtures/artificer-broad-reasoning-holdout-v45-threadcontinuityfit.tsv`
      - first focused probes exposed three real rich-path defects:
        - shorthand follow-ups like `revise that same plan`, `keep the same labels`, and `same read because ...` could preserve a reasoning shape while reusing the first-turn answer instead of carrying the new delta
        - rich same-thread prompts could still drift into long-loop controller execution and timeout under the 45s strict budget
        - even when the follow-up final was structurally strong, quick mode still emitted `commands: []`, which caused strict shallow-completion and evidence-quality penalties
      - fixed in `hosted-web/cgi/artificer-api` by:
        - adding conversation-aware rich follow-up detection and context recovery (`reasoning_latest_prompt_text`, `reasoning_recent_user_turns_block_for_prompt`, `reasoning_prior_answer_block_for_prompt`, `reasoning_followup_delta_phrase_for_prompt`, `reasoning_followup_anchor_source_for_prompt`)
        - normalizing rich second-turn finals into exact labeled follow-up contracts via `normalize_reasoning_followup_thread_contract`, with scenario-specific replacements for `Outcome`, `Decision`, `Fallback Path`, `Disconfirming Evidence`, `Risks`, `Next Improvement`, and assumption-revision lines when requested
        - forcing rich reasoning prompts onto a bounded quick path with a fail-safe that disables long-loop execution and programmer-review churn when the prompt is clearly a reasoning-only follow-up
        - adding real quick-path verification commands plus evidence-line upserts so rich follow-up finals satisfy strict evidence/shallow-completion requirements instead of failing on synthetic `commands: []`
        - widening product/UX and metrics/causality cue detection so natural follow-up deltas like `review staffing doubled`, `fraud pressure`, `trial starts jump right after`, and `one cohort retained worse after day 21` recover the correct scenario anchor instead of falling back to generic cross-domain phrasing
      - fixed in `hosted-web/scripts/broad-reasoning-cycle.sh` by:
        - adding optional `followup_prompt` support so one task can execute an initial prompt and a same-thread revision turn in one assay row
        - preserving both initial and final conversation/event/assistant/stream artifacts to make follow-up failures debuggable
      - validation:
        - focused probes progressed from failing `broad104-reg45-single-r9` (`overall 45`) to non-shallow `broad106-reg45-single-r11` (`overall 96`, `commands 2`, shallow `0`)
        - incident and causal holdouts required additional timeout/parser/anchor fixes (`broad110-hold45-incident-r3`, `broad121-hold45-causal-r7`), both ending at `overall 96` with zero reliability regressions
        - final full reruns `broad122-reg45-threadcontinuity-r6` / `broad122-hold45-threadcontinuity-r6` reached `avg overall 95.25` / `95.50`, `avg validity 88.75` / `90.50`, `avg evidence 100.00`, fail-open/assumption-leak/contradiction/shallow all `0.0000`
        - transfer snapshot `broad122-threadcontinuity-r6-transfer`: PASS, `all_gates_pass=true`, `transfer_risk=low`
    - Added a dedicated release-wrapper soak runner:
      - new script `hosted-web/scripts/release-gate-gui-soak.sh`
      - repeatedly invokes `release-gate.sh` with reused reasoning summaries while keeping GUI gate execution configurable as live or reused-result runs
      - writes a consolidated TSV/JSON/Markdown summary with per-iteration gate status, GUI pass, interactive pass, and artifact paths
      - applies a per-iteration timeout wrapper so future wrapper regressions fail boundedly at the soak layer too
    - Validation for the new soak runner:
      - structural smoke `release-gui-soak-smoke2` passed (`1/1`, `pass_rate=100%`) and emitted:
        - `hosted-web/.assay-reports/release-gui-soak-smoke2-soak.tsv`
        - `hosted-web/.assay-reports/release-gui-soak-smoke2-soak.json`
        - `hosted-web/.assay-reports/release-gui-soak-smoke2-soak.md`
      - live smoke `release-gui-soak-smoke1` was observed to progress through the release wrapper into the Safari `interactive` phase after completing Safari `core`, confirming the soak path is exercising the intended wrapper boundary rather than shortcutting to cached artifacts.

## P0 (Now)

- INT-001 Failure taxonomy persistence
  - Goal: Record every run-loop failure into a normalized, file-backed taxonomy.
  - Storage: `mode-runtime/failure-taxonomy/events.tsv`
  - Acceptance:
    - Each failure ledger append writes one taxonomy row.
    - Taxonomy categories, severity, and recent events are queryable through API.
  - Progress:
    - Added `failure_taxonomy_query` API support with category/severity/surface/mode/time filters plus bounded result windows for deterministic analysis tooling.

- INT-002 User-visible learning memory
  - Goal: Surface taxonomy state in settings so users can inspect failure patterns.
  - Acceptance:
    - Settings show totals, top categories, and recent failures.
    - Data updates after new failures without app restart.
  - Progress:
    - Added settings-side taxonomy query controls (category/severity/surface/mode/since/limit) backed by `failure_taxonomy_query` for targeted pattern inspection.

- INT-003 Contained self-improvement proposals
  - Goal: Generate improvement proposals from recurring failure categories.
  - Storage: `mode-runtime/improvement-proposals/<proposal-id>/`
  - Acceptance:
    - Proposal generation endpoint creates proposals from repeated failure clusters.
    - Proposal status workflow supports `proposed`, `accepted`, `applied`, `rejected`.
    - Applying requires explicit manual confirmation; no autonomous pipeline edits.

- INT-004 Manual governance controls in UI
  - Goal: Let users generate and decide proposals in settings.
  - Acceptance:
    - "Generate from failures" control exists.
    - Per-proposal Accept/Apply/Reject actions update state in UI.

## P1 (Next)

- INT-005 Decision-surfacing coverage expansion
  - Goal: Increase recall/precision for advanced decision categories.
  - Acceptance:
    - Add deterministic prompt fixtures for each decision type and near-miss variants.
    - Add regression tests ensuring category-specific routing remains stable.
  - Progress:
    - Added a fixture-driven live decision-surfacing test pack for deterministic category/signal validation.
    - Expanded high-order heuristics/fixtures for compliance-risk questions, deployment scope gaps, cloud/orchestration external actions, and destructive near-neighbor command cases.
    - Expanded fixture coverage with additional trigger/near-miss precedence cases and wired assay `decisions` runs to score full fixture signal parity (category + allow + all signal flags).
    - Added a multi-iteration assay mentor runner that executes full task panels per cycle, records decision-matrix outcomes per cycle, and produces cycle-over-cycle report tables.
    - Moved decision fixtures into the repo (`hosted-web/tests/fixtures/artificer-decision-surfacing-fixtures.psv`) so assay runs are self-contained and reproducible.
    - Tightened decision heuristics to reduce false positives (e.g., HTML tags no longer treated as missing placeholders) and expanded destructive coverage (e.g., `git clean -fd`).
    - Added additional trigger/near-miss coverage for risk-language variants (`authorise`), `rsync` remote/local distinctions, `kubectl delete` vs `kubectl diff`, and redacted placeholder tokens.
    - Expanded fixture panel to 80 deterministic cases (including explicit/missing and missing/risk precedence boundaries, run-mode-sensitive external-action routing, incident-scope missing-input edge cases, and destructive-vs-external precedence) and validated 100% signal parity against current decision heuristics.
    - Expanded decision fixture panel to 88 deterministic cases with additional trigger/near-miss pairs for Docker publish/build, SQL `truncate table` vs file truncation, production network exception approvals, and explicit option-path wording variants.
    - Expanded decision fixture panel to 96 deterministic cases, adding close trigger/near-miss pairs for `gh release` vs `gh pr view`, lowercase placeholder tokens (`<tenant_id>`), SQL schema-drop triggers, and quoted SQL destructive-keyword documentation strings.
    - Tightened destructive SQL detection to avoid false-positive decision gates for quoted example strings (for example `select 'drop table ...'`) while still gating executable destructive SQL.
    - Expanded decision fixture panel to 104 deterministic cases with live-vs-dry-run pairs for `kubectl delete`, `aws s3 rm --recursive`, and `terraform destroy`, plus SQL double-quoted literal near-miss coverage.
    - Refined destructive-action detection so explicit dry-run variants (`--dry-run`, `--dryrun`) for destructive-capable commands are treated as non-destructive previews while external-action gating still applies.
    - Scoped destructive SQL keyword gating to SQL execution contexts (`psql`, `mysql`, `sqlite3`, `sqlcmd`, `dbmate`, `prisma db`) and ignored documentation-style `select 'drop ...'` / `select "drop ..."` literals.
    - Revalidated decision-surfacing parity at 104/104 passing fixtures in `decisions-verify-loop24`.
    - Expanded decision fixture panel to 111 deterministic cases with additional precedence and near-miss boundaries (`which`-with-placeholder missing-input prompts, informational policy-risk questions, dry-run destructive neighbors, and documentation-only SQL command examples).
    - Refined explicit-choice detection so `which <scope>` prompts without alternatives are treated as missing-input requests instead of explicit choice prompts.
    - Refined risk-gate detection to ignore informational risk-analysis questions unless they also request approval/permission.
    - Refined destructive SQL detection to ignore documentation-only `echo`/`printf`/`cat` examples containing destructive SQL text when no SQL executor is chained.
    - Revalidated decision-surfacing parity at 111/111 passing fixtures in `decisions-verify-loop26`.
    - Expanded decision fixture panel to 118 deterministic cases, adding trigger/near-miss pairs for policy-exception approval vs documentation-only wording, assistant-vs-programming external `gh api` routing, read-only `kubectl diff` near-miss handling, and security-mode explicit/missing-input boundaries.
    - Refined risk-gate detection so policy-exception documentation/checklist questions are treated as non-approval informational prompts unless explicit approval/proceed/bypass language is present.
    - Revalidated decision-surfacing parity at 118/118 passing fixtures in `decisions-verify-loop29`.
    - Expanded decision fixture panel to 126 deterministic cases with additional trigger/near-miss precedence boundaries for `select`-style scope prompts, policy-exception waiver-template documentation wording, report-mode vs assistant-mode `gh api` routing, and external-vs-risk precedence for assistant checks.
    - Refined risk-gate detection to keep policy-exception documentation/template questions containing waiver terminology in the non-approval path unless explicit approval/proceed/bypass intent is present.
    - Revalidated decision-surfacing parity at 126/126 passing fixtures in `decisions-verify-loop31`.
    - Expanded decision fixture panel to 133 deterministic cases with additional trigger/near-miss boundaries for `gh workflow run` external-command routing, SQL `drop role` destructive detection, and policy-exception documentation-process wording.
    - Expanded external-action detection to include `gh workflow run` and expanded destructive SQL detection to include `drop role`, while preserving documentation-literal near-miss exemptions.
    - Revalidated decision-surfacing parity at 133/133 passing fixtures in `decisions-verify-loop34`.

- INT-006 Conversation flow latency and completion signaling
  - Goal: Remove dead-air gaps between run completion markers and assistant text rendering.
  - Acceptance:
    - UI always shows a live status until final assistant content is committed.
    - No visible completion state before content is actually available.
  - Progress:
    - Added an outer `run_command_with_timeout` guard around Safari `osascript` execution paths (scenario run, reload fixture, Safari reset) so deep automation fails fast with explicit timeout diagnostics instead of hanging indefinitely.
    - Hardened Safari tab/window recovery for both scenario and reload fixture AppleScript runs (`ensureFrontTab` fallback), and marked transient missing-tab errors (`-10006`, `-1700`) as retryable automation failures.
    - Made reload-durability fixture deterministic by targeting a seeded known workspace/conversation and gating submit through a `submitWhenReady` loop (waits for active row + enabled run controls before enqueue/reload), with additional fixture telemetry (`matched_source`, `submitted`, `ready_attempts`).
    - Revalidated stability with passing Safari runs: `gui-state-coherence-v93-core`, `gui-state-coherence-v97-deep`, `gui-state-coherence-v98-core`, and `gui-state-coherence-v98-deep`.
    - Hardened Safari heartbeat liveness assertion to accept equivalent in-flight signals (running/finalizing lines, queue activity, and run-line growth) and settle on queue-idle semantics, reducing false negatives from UI timing races.
    - Added a deep/background-only reload-during-send durability fixture in `gui-regression-safari.sh` that verifies pending outgoing survives a forced reload during enqueue.
    - Upgraded Safari automation harness resilience with structured fixture failure JSON, extended AppleScript timeouts, transient-run retries, and Safari session reset between attempts to reduce boot/AppleEvent transport flake.
    - Refactored reload-durability execution to reuse the currently active Safari tab/session (instead of launching a second full bootstrap flow), and added retryability for empty-output automation failures plus richer fixture error-output diagnostics.
    - Hardened reload fixture setup to avoid synthetic conversation-row clicks that can trigger unintended navigation; fixture now targets the already active conversation context.
    - Fixed reload fixture AppleScript parsing by scoping `do JavaScript` verification calls inside explicit `tell application "Safari"` blocks (removed persistent `-2741` syntax failures in deep runs).
    - Relaxed reload fixture context assumptions so it can run from draft-mode states (active workspace fallback + workspace-scoped enqueue interception) instead of requiring an active conversation row.
    - Added fixture bootstrap fallback when Safari has no open windows (open base URL, navigate to app URL, and wait for boot before executing fixture scripts).
    - Stabilized scenario runner pathing by executing AppleScript from the persistent copied scenario file in `.assay-reports` instead of ephemeral temp paths.
    - Remaining gap: long-running deep/background Safari automation is still intermittently unstable in this environment, so reload-durability stabilization is tracked as open in `PERFECTION_CHECKLIST.md`.
    - Split Safari coherence regression into explicit execution profiles: `core` (fast coherence + liveness), `deep` (coherence + durability/failure recovery), and `background`/`full` (long background reconciliation + durability checks), with profile-aware timeout budgets and report metadata.
    - Added profile-scoped result telemetry (`profile`, `extended_checks_enabled`, `background_checks_enabled`) so CI and manual runs can distinguish which UX guarantees were exercised.
    - Validated the split with passing Safari runs: `gui-state-coherence-v70-core`, `gui-state-coherence-v70-deep`, and `gui-state-coherence-v70-background`.
    - Added run-event liveness metadata and UI rendering for active/finalizing status (`last_activity_at`, running/finalizing liveness text) so users always see explicit in-progress state cues instead of silent intervals.
    - Expanded Safari GUI coherence regression with a deterministic live-status heartbeat check (`gui-state-coherence-v61`) that validates visible no-dead-air run status signaling during in-flight execution.
    - Hardened the Safari heartbeat fixture with conversation-scoped run interception and synthetic delayed run completion to reduce long-run nondeterminism while preserving real UI flow assertions.
    - Added assistant-delivery watch logic so finalizing feedback remains visible until assistant content lands (or fallback is inserted).
    - Strengthened finalizing-status rendering so the latest completed run keeps showing "Finalizing response..." until assistant output is actually present, reducing false-idle model-only states.
    - Updated `until-complete` to run with unbounded iteration sentinel semantics (runtime-budget bounded) instead of a hard numeric loop cap.
    - Added event-level `awaiting_assistant` signaling so completed runs continue surfacing finalization feedback even when pending counters race or clear out of order.
    - Upgraded `chat` quick-prompt construction to include recent conversation context, correction-aware follow-up guidance, and anti-platitude constraints to improve multi-turn conceptual continuity.
    - Added chat-specific off-topic detection + salvage reroute for correction turns (e.g., onboarding/non-sequitur drift, generic platitude lists) to improve relevance under weaker conversational models.
    - Upgraded conversational model preference scoring so `chat` fallback/salvage prefers general conversational strength over coder-specialized models when both are installed.
    - Added thread-focus anchors (recent user-turn summaries) to chat prompts so follow-up answers better preserve conceptual throughlines.
    - Added run-step text normalization so controller scaffolding markers render as cleaner human-readable timeline lines during live trace viewing.
    - Forced terminal run traces into collapsed rollup state by default to keep finished conversations scan-friendly while preserving expandable detail.
    - Added completed-trace readability filtering that hides low-signal orchestration updates by default while keeping them available behind an expandable foldout.
    - Added assay flow-signal scoring for timestamped thinking updates, strict final-structure adherence, and fold-ready trace richness, plus per-cycle postmortem generation for targeted mentor guidance.
    - Added timeline dedupe for repeated adjacent trace fragments and made completed-run fold behavior more aggressive (fold middle when timeline length exceeds 10) to improve readability.
    - Added assay timeout-buffer control plus budget-aware iteration caps so tight-budget mentor cycles finish with concrete outcomes instead of timing out.
    - Added `Depth OK` scoring and postmortem surfacing for shallow done-runs, plus assay-side completion enforcement that injects final verification commands when command depth is below minimum.
    - Added strict-scoring mode (`--strict-scoring`) to assay `run`/`mentor` flows, with harder depth/timestamp/fold thresholds for high-difficulty mentoring panels.
    - Added strict-scoring retry hardening (minimum 2 attempts per task) and increased ambiguous-spec hard-panel budget to `long` for stability under the hardest assay profile.
    - Fixed assay `run` prompt construction to avoid shell backtick substitution, preventing accidental prompt pollution from live `ls`/`git status` command output.
    - Reduced controller context noise by bounding workspace snapshot git status output (tracked summary + capped untracked sample) so large assay artifact sets no longer crowd out task-relevant signals.
    - Added mediated command-output compaction for noisy `git status` runs (`compact_command_output_for_context`), summarizing tracked/untracked counts and capped samples instead of injecting massive untracked-file dumps into loop feedback/session memory.
    - Added targeted noise-resilience fixture `artificer-broad-reasoning-regressions-v21.tsv` (`reg21-context-noise-forensics`) and validated no strict-score regressions in `broad24-regressions-post1` (5/5 done, overall 96, reliability rates all `0.0000`) plus full holdout `broad24-holdout-post1` (8/8 done, overall 96, reliability rates all `0.0000`).
    - Operational reliability improved on the same unseen holdout family: aggregate controller-format recovery markers dropped from `35` (`broad23-holdout-post1`) to `22` (`broad24-holdout-post1`), and the historically unstable `hold16-ux` task dropped from `7` to `0`.
    - Added controller-loop resilience by retrying once with a strict section-format reminder when the model returns an empty controller response, reducing fallback-only iterations.
    - Hardened malformed-controller handling with one strict format-order retry plus a completion guard that blocks direct `DONE` transitions from recovered controller scaffolding until a clean structured controller pass succeeds.
    - Added malformed-controller circuit-breaker logic so repeated consecutive recovery passes finalize with deterministic best-effort output instead of burning remaining run budget into timeout loops.
    - Added deterministic partial-controller section completion (`repair_partial_controller_output`) so truncated-but-structured controller responses are completed with safe defaults (including `PLAN_UPDATE`/`DONE_CLAIM`/`CHECKPOINT`) before full malformed-output recovery is attempted.
    - Shortened format-retry prompting to a repair-focused contract (preserve mode/plan + complete missing headers in order) and added unbalanced-quote command sanitization fallback to reduce malformed command carryover from truncated controller text.
    - Added deterministic filename-case auto-correction for mediated read-only file commands (`cat/head/tail/sed`) so path-case drift can self-heal before execution via `resolve_relative_path_case_insensitive`, reducing repeated `missing_input` loops without broadening command permissions.
    - Validation:
      - `broad26-hold16-ux-formatfix1` reduced controller-format actions on `hold16-ux` from `8` (`broad25-hold16-ux-pathfix1`) to `3` while preserving score (`overall 96`) and reliability rates (`0.0000` fail-open/assumption-leak/contradiction/shallow).
      - `broad26-formatguard-post1` and `broad26-formatguard-post2` remained stable on strict controller-format guard tasks (`avg overall 95`, `avg transfer-readiness 96.5`, reliability rates all `0.0000`) without malformed-output circuit-breaker triggers.
      - `broad27-hold16-ux-casemissfix1` preserved holdout quality (`overall 96`, reliability rates all `0.0000`) while reducing path-case thrash to one genuine missing-file event (historical repeated `readme.md` case misses now auto-corrected).
      - `broad27-formatguard-casemissfix1` remained stable on strict format-guard tasks (`avg overall 95`, `avg transfer-readiness 96.5`, fail-open/contradiction/shallow all `0.0000`).
      - `broad29-hold16-ux-rgfixtight1` remained stable (`overall 96`, fail-open/assumption-leak/contradiction/shallow all `0.0000`) with empty failure taxonomy after hardening `rg/grep` nonzero handling (`rc=1` no-match -> non-fatal `ok`, missing search target -> `missing_input`).
      - `broad29-formatguard-rgfixtight1` remained saturated on strict format-guard tasks (both rows `intelligence=100`, `flow=100`, `depth_ok=1`) after adding search-path autocorrection and non-fatal search-miss classification.
      - Tightened scenario-depth gating (`final_has_scenario_specific_depth`) to require prompt-token grounding in non-template lines (not only contract headers such as `Context Anchor` / `Scenario-Specific Check`), reducing acceptance of contract-only generic finals.
      - Classified missing-path `ls/find` command results as non-fatal `missing_input` and updated high-risk DESIGN gating to evaluate fail-closed checks against normalized fallback final candidates; targeted high-risk holdout `broad32c-hold16-ux-highriskgate1` improved from timeout churn (`broad32b-*`, status `timeout`, overall `77`) to clean completion (`status done`, overall `98`) with no fail-open/contradiction/shallow regressions.
      - Follow-up high-risk subset validation `broad33-highriskpair-postflow1` confirmed generalization on `hold16-seccomp` + `hold16-incident` (2/2 done, `avg overall 96`, fail-open/assumption-leak/contradiction/shallow all `0.0000`) after the high-risk DESIGN-gate fallback normalization change.
      - Extended design-outcome sanitization to suppress generic orchestration phrasing (`completed partial controller output...`, `fallback command execution`) in final `Outcome:` lines; focused holdout `broad34-hold16-ux-outcomefix1` remained `done` with `overall 98` and scenario-grounded outcome text instead of controller-recovery boilerplate.
    - Fixed assay final-output normalization to avoid duplicated `Outcome:` prefixes when structured partial outputs are re-wrapped for strict reasoning contracts.
    - Added explicit per-iteration controller call/capture stream markers to increase timestamped trace density; targeted `trace-quality-audit` retest improved flow from 87 to 91 under strict scoring.
    - Added additional per-iteration stream markers for control parsing, decision checkpoints, command batch start/status/summary, and patch-gate summaries to improve live trace continuity under hard tasks.
    - Added two unconditional per-iteration trace markers (`controller prompt assembled` and `completion check`) to stabilize timestamp density on short-but-complete runs.
    - Ran strict focused retest (`chaos-recovery-drill`, `trace-quality-audit`, `teaching-mastery-loop`) after flow marker expansion; all three scored flow=91 with strict/depth pass.
    - Ran a full strict hard panel (`mentor-hard-panel7`) and found remaining low-density edge cases (`security-depth-remediation`, `trace-quality-audit`, both at `timestamp_steps=9` / flow=87) despite clean completion.
    - Added an additional unconditional per-iteration confidence marker (`Step N confidence updated`) and revalidated edge tasks with strict focused retest (`mentor-hard-flowfix2`), both recovering to flow=91.
    - Updated completed-run timeline filtering to fold low-signal orchestration command-step scaffolding by default while preserving full detail in expandable sections.
    - Added explicit run-start and finalization trace markers plus fold filtering for those low-signal orchestration lines to improve live continuity without cluttering completed traces.
    - Ran strict hard panel `mentor-hard-panel9` and confirmed stable quality gates (12/12 done, avg intelligence 100, avg flow 91, strict/depth 12/12), with `dual-write-migration` improving from flow 87 (panel8) to 91.
    - Ran strict hard panel `mentor-hard-panel10` after decision-logic refinements and confirmed no quality regressions (12/12 done, avg intelligence 100, avg flow 91, strict/depth 12/12).
    - Ran strict hard panel `mentor-hard-panel12` after fixture/risk-gate refinements and confirmed stable quality gates (12/12 done, avg intelligence 100, avg flow 91, strict/depth 12/12, decision pass 118/118).
    - Ran true strict hard panel `mentor-hard-real2` using `hosted-web/tests/fixtures/artificer-hard-task-panel.tsv` and confirmed stable quality gates on the harder task set (12/12 done, avg intelligence 100, avg flow 91, strict/depth 12/12, decision pass 118/118).
    - Revalidated true strict hard panel after decision refinements in `mentor-hard-real3` with the same hard task set and no regressions (12/12 done, avg intelligence 100, avg flow 91, strict/depth 12/12, decision pass 126/126).
    - Calibrated assay flow scoring so perfect strict-flow runs can reach a true ceiling (added final-structure contribution/penalty in `flow_raw`), eliminating the prior 91-point cap under full signal saturation.
    - Ran true strict hard panel `mentor-hard-real4` after flow calibration and achieved maxed conversation-flow quality gates on the hard task set (12/12 done, avg intelligence 100, avg flow 100, strict/depth 12/12, decision pass 126/126).
    - Revalidated strict hard panel stability in `mentor-hard-real5` with no regressions (12/12 done, avg intelligence 100, avg flow 100, strict/depth 12/12, decision pass 126/126).
    - Revalidated strict hard panel after latest decision-surfacing refinements in `mentor-hard-real6` with no regressions (12/12 done, avg intelligence 100, avg flow 100, strict/depth 12/12, decision pass 133/133).

- INT-007 Thread/workspace ordering ergonomics
  - Goal: Improve recency-based thread placement and drag-reorder fluidity.
  - Acceptance:
    - New workspaces are inserted at top.
    - Thread moves to top on send with smooth reorder animation.
    - Workspace/thread drag reorder persists and remains stable on reload.
  - Progress:
    - Hardened optimistic thread lifecycle in `artificer-app.js` so client-created threads use longer TTLs and pending-outgoing-aware extension instead of short fixed expiry windows.
    - Updated state-refresh policy to force uncached `state` reads while optimistic threads are awaiting backend acknowledgement, reducing stale-feed thread disappearance after draft-to-thread sends.
    - Added safer send-failure recovery so queued prompt text is restored across both origin and created-thread composer contexts without clobbering active non-empty input.
    - Persisted send-failure prompt restoration to disk-backed drafts (workspace or conversation) so interrupted draft-to-thread sends survive refresh/restart instead of remaining memory-only.
    - Expanded Safari GUI regression (`gui-regression-safari.sh`) to cover draft-send thread creation with immediate user context switch, and assert both new-thread persistence and prompt-content preservation.
    - Expanded Safari GUI regression with a deterministic simulated `queue_enqueue` failure path that validates durable prompt recovery in both workspace draft and newly created thread draft contexts.
    - Fixed draft-to-thread conversion state coherence in `artificer-app.js` by removing mid-flight selection mutations from `ensureConversationFromDraft`, so manual thread switches during send are no longer overwritten by background thread creation.
    - Added a Safari GUI regression assertion that manual conversation selection remains active while draft-send thread creation is still in-flight, preventing future selection-hijack regressions.
    - Eliminated draft persistence race in `onRunSubmit` by ordering failure-path draft restoration after async draft-clear completion, preventing restored text from being overwritten by late clear writes.
    - Hardened optimistic-thread lifecycle in `artificer-app.js` so newly created threads are retained through a post-acknowledgement grace window instead of being dropped immediately after first server visibility, preventing transient stale-state thread disappearance.
    - Added stale-state recovery semantics for optimistic threads: when a `state` response temporarily omits a just-created thread, UI merge now re-injects the conversation until grace expiry (or explicit archive/delete), while still cleaning up expired optimistic entries.
    - Hardened active-selection coherence in `ensureSelection`: if a transient stale `state` payload omits the active conversation summary, the client now retains selection for a bounded grace window, reinserts a summary from cache/active/optimistic state, and only clears selection after in-flight signals and grace windows expire.
    - Expanded Safari GUI regression with deterministic stale `state` interception during draft-send thread creation and a required consumption assertion, proving thread visibility coherence even when one stale state payload is returned mid-flow.
    - Updated Safari GUI regression stale-state fixture to omit the currently active conversation from the intercepted `state` payload and assert active conversation continuity, covering the “thread disappears until restart” failure mode.
    - Added durable pending-send persistence (`pendingOutgoingByKey`) in browser storage so in-flight outbound messages survive reload/restart instead of disappearing when the UI process is interrupted mid-send.
    - Added bounded pending-send compaction and age pruning (size-limited per key + 24h expiry) plus before-unload flush, preventing both message loss and unbounded pending-outgoing cache growth.
    - Upgraded pending-send durability by flushing pending-outgoing storage immediately at send enqueue/move boundaries (not only debounced), reducing crash-window message-loss risk right after submit.
    - Expanded Safari GUI regression with a delayed enqueue-failure durability check that asserts pending outgoing prompts are persisted in storage before enqueue completion.
    - Added background queue-summary reconciliation for pending outgoing indicators so completed runs clear stale `Sending...` entries even if the thread was never opened after completion.
    - Expanded Safari GUI regression with a background-completion assertion that waits for terminal queue status and verifies pending-outgoing storage is cleared for the non-active created thread.
    - Added draft-conversation creation dedupe in `ensureConversationFromDraft` so rapid consecutive sends from one workspace draft share a single in-flight thread creation request.
    - Added per-conversation enqueue serialization in `onRunSubmit`/`enqueuePromptInConversationOrder` so rapid sends preserve deterministic queue order.
    - Expanded Safari GUI regression with a rapid draft multi-send fixture that verifies one created thread, ordered prompt preservation, and single `new_conversation` request consumption.
    - Added a dedicated pending-outgoing reconciliation loop that performs fresh state syncs only while pending sends exist, so stale background `Sending...` indicators clear without requiring additional user actions.
    - Updated the rapid-send regression fixture to drain its own temporary queue workload (`queue_stop` + `queue_cancel`) so subsequent coherence checks are not blocked by leftover background work.
    - Hardened pending-outgoing reconciliation against long-running busy-loop starvation by forcing periodic reconcile passes and adding a queue-list probe fallback for pending conversation keys.
    - Added orphaned pending-outgoing self-heal for missing conversations/workspaces (age-gated cleanup) to prevent stale `Sending...` indicators from accumulating across lifecycle churn.
    - Hardened Safari GUI regression drain logic to use `queue_list` terminal summaries (not only workspace `state` summaries), eliminating a false-terminal fixture path and validating background pending-indicator clearance in `gui-state-coherence-v50`.

- INT-013 Perfection roadmap checklist
  - Goal: Maintain a stable, user-visible checklist that tracks parity progress toward perfect usability + reliability + intelligence.
  - Acceptance:
    - Checklist exists in-repo with concrete cross-domain status items.
    - Every improvement pass updates checklist status in the same commit as code.
  - Progress:
    - Added `hosted-web/PERFECTION_CHECKLIST.md` as the canonical pass-by-pass roadmap for UX flow, state reliability, intelligence reliability, evidence quality, and regression confidence.
    - Added unified release gate runner `hosted-web/scripts/release-gate.sh` that requires both:
      - strict intelligence sample thresholds from broad reasoning battery+holdout summaries (overall/adversarial/ambiguity/cross-domain/recovery plus fail-open/contradiction limits), and
      - a green Safari GUI coherence sample (`gui-regression-safari.sh` profile run or supplied result JSON).
    - Added combined gate artifacts (`*-release-gate.json`, `*-release-gate.md`) and validated end-to-end PASS in `release-gate-smoke-v2`.
    - Added deterministic lifecycle assay inject/clear API actions (`assay_inject_decision_request`, `assay_inject_approval_request`, `assay_clear_lifecycle_requests`) for reproducible queue/decision/approval GUI smoke coverage.
    - Added Safari lifecycle smoke suite `hosted-web/scripts/gui-lifecycle-smoke-safari.sh` with:
      - deterministic workspace + dual-conversation seeding (decision path and approval path isolation),
      - focus/blur and full-reload lifecycle checks for decision and approval inline UX,
      - stage-level fail-fast diagnostics to avoid opaque AppleScript timeout failures.
    - Validated PASS in `gui-lifecycle-smoke-v7` with all lifecycle checks green and deterministic report artifacts.
    - Extended `gui-regression-safari.sh` with explicit queued-edit and archive journey coverage (`queue-edit-item`/`queue-edit-save` and `arm/confirm-archive-conversation`) plus scenario-level unhandled-error capture so JS exceptions emit structured failure JSON instead of timing out silently.
    - Added queue-edit fixture hardening in scenario flow (pre-drain queue gate, run-action interception during edit save, and post-save queue drain assertion) to reduce race-driven false negatives.
    - Fixed queue-tray edit UX in `artificer-app.js`: when edit mode is active but queue item cache is temporarily stale, the tray now renders an edit fallback row from `state.queueEdit` so the textarea/save controls remain visible and usable.
    - Improved queue-tray state coherence in `artificer-app.js` by probing `queue_list` opportunistically for the active conversation even when lightweight conversation summary counters are stale, and by showing the tray when cached queue items exist independent of `queue_pending` summary lag.
    - Added queue-edit post-save hold behavior in `artificer-app.js` (conversation-scoped hold map + timer + queue-worker gating) so edited queued items remain visible briefly before queue auto-resume.
    - Hardened queue-edit Safari scenario determinism by binding edit/save to queue item IDs verified from `queue_list`, switching the fixture enqueue step to explicit `queue_enqueue`, and delaying `queue_take` only for the queue-edit journey.
    - Added a scenario watchdog timer that emits structured failure JSON with debug snapshot instead of allowing opaque outer AppleScript timeouts.
    - Validated a green core GUI regression pass after these changes (`gui-flow-polish-pass2-core-r7`) with queue-edit and archive checks passing.
    - Added draft-hydration fixture resilience in `gui-regression-safari.sh` with conversation-scoped hydration retries/reselection (`waitForDraftHydration`) plus deterministic conversation-draft seeding via `save_conversation_draft` in setup.
    - Revalidated with green profile runs after hydration hardening: core (`gui-flow-polish-pass3-core-r3`) and deep (`gui-flow-polish-pass3-deep`).
    - Improved queue-tray coherence in `artificer-app.js` so queue-edit controls can appear from real queue state even when `state` summary counters lag, then revalidated green runs across all Safari profiles: core (`gui-queue-edit-coherence-v1`), deep (`gui-queue-edit-coherence-v1-deep`), background (`gui-background-polish-v1`), and full (`gui-full-polish-v1`).
    - Current status: end-to-end flow polish checklist item is closed for this phase after green core/deep/background/full coverage; remaining work is ongoing soak confidence and periodic revalidation.

- INT-011 Mode-aware model recommendation and routing
  - Goal: Improve run quality by selecting models better matched to `chat` vs coding-heavy tasks.
  - Acceptance:
    - Backend can rank installed models per mode (conversation vs programming).
    - Chat fallback/salvage prefers high-conversation models over coder-specialized models when available.
  - Progress:
    - Added mode-aware model scoring and `model_recommendations` API output with ranked installed-model lists for `chat` and `programming`.
    - Wired conversational salvage to use the improved chat-preference selector.

- INT-012 Broad reasoning + adversarial reliability cycle
  - Goal: Improve transfer reliability on novel, open-ended, cross-domain tasks with adversarial ambiguity.
  - Acceptance:
    - Fresh 16-task base/adversarial battery per cycle and 8-task unseen holdout battery.
    - Dimension scoring for validity, evidence, ambiguity handling, cross-domain integration, adversarial robustness, recovery, and clarity.
    - Cycle gate requires no fail-open/contradiction regressions, holdout non-regression, and gains in at least two core robustness dimensions.
  - Progress:
    - Added strict assay harness `hosted-web/scripts/broad-reasoning-cycle.sh` with raw trace capture and per-task scoring outputs.
    - Added paired 16-task battery fixture (`artificer-broad-reasoning-battery-v1.tsv`), holdout fixture (`artificer-broad-reasoning-holdout-v1.tsv`), and targeted regression fixtures (`artificer-broad-reasoning-regressions-v1.tsv`).
    - Implemented controller/policy upgrades for adversarial ambiguity handling:
      - Mode-policy requirements for explicit conflict mapping, priority ordering, and contradiction checks.
      - Adversarial FINAL-quality gate blocking completion if assumptions/conflict handling is missing.
      - Faster DESIGN completion path for non-code reasoning modes under assay pressure.
    - Ran corrected pre-fix vs post-fix matched battery (`broad-strict-baseline2-prefx` vs `broad-strict-post4-fix`) and observed no meaningful robustness improvement (core dimensions mostly flat; reliability gate remained unmet).
    - Classified overfit/under-generalization risk and applied anti-overfit fix: timeout partial-finalization now emits explicit `Assumptions and Alternatives` plus `Contradiction Check`.
    - Reran holdout once (`broad-holdout2-anti` vs `broad-holdout1-fix`): aggregate improved modestly, but adversarial/cross-domain/ambiguity dimensions remained flat; cycle still not yet successful.
    - Added budget-alignment reliability controls in `artificer-api`:
      - explicit `ARTIFICER_RUN_TIME_BUDGET_SEC` is no longer silently inflated by compute-budget runtime floors.
      - assay runs now cap iterations dynamically by runtime budget to reduce cancellation/timeout collapse.
      - low-remaining-budget synthesis path now emits structured partial deliverables instead of stalling.
    - Added stronger adversarial/cross-domain final-contract normalization in `artificer-api`:
      - adversarial prompts now normalize final output to include assumptions, alternatives, priority ordering, and contradiction checks.
      - cross-domain prompts now normalize final output to include explicit multi-lens integration (architecture/product/security/metrics/incident).
      - introduced final-quality gates for missing adversarial or cross-domain reasoning contracts before completion.
    - Added targeted v2 regression fixture pack (`artificer-broad-reasoning-regressions-v2.tsv`) covering budget-bounded ambiguity handling, adversarial conflict resolution, and cross-domain synthesis.
    - Fixed assay scorer bug in `broad-reasoning-cycle.sh` where `jq contains()` scope incorrectly treated all forbidden patterns as hits, causing false fail-open/contradiction inflation on adversarial tasks.
    - Ran strict v2 cycle (`broad2-*`):
      - in-battery post-fix (`broad2-strict-post2`) improved to avg overall `81.62` with fail-open `0.0000`, contradiction `0.0000`, assumption leak `0.0000`.
      - holdout transfer (`broad2-holdout-v2-post1`) scored avg overall `84.00` with fail-open/contradiction both `0.0000`.
      - reliability gate passed (no fail-open increase, no contradiction increase, holdout non-regression, gains in adversarial + ambiguity + cross-domain + recovery).
    - Ran strict v3 cycle (`broad3-*`) with fresh base/adversarial battery + holdout fixtures and targeted regression fixtures:
      - added stricter adversarial trap handling contract (`Trap and Counterevidence Check`) and expanded cross-domain trigger detection for teaching/queue-latency/forensics prompts.
      - added explicit recovery/self-correction normalization (`Recovery and Self-Correction`) and taxonomy instrumentation for `recovery-self-correction-gap`.
      - fixed assay task ingestion bug so TSV header rows are skipped instead of executed as synthetic tasks.
      - baseline (`broad3-strict-baseline`) to post-fix (`broad3-strict-post1`) improved overall from `79.62` to `85.94` with major gains in adversarial robustness (`64.00` -> `79.00`) and recovery (`55.00` -> `81.88`), with fail-open/contradiction unchanged at `0.0000`.
      - holdout transfer (`broad3-holdout-v3-post1`) reached overall `88.25` with fail-open/contradiction still `0.0000`, satisfying strict reliability gates for this pass.
    - Ran strict v4 cycle (`broad4-*`) with stricter adversarial/recovery-depth rubric instrumentation and fresh v4 batteries:
      - added rubric signals for decision depth (`Decision`, `Fallback Path`, `Disconfirming Evidence`) and trigger-based recovery (`Re-Plan Trigger`) in `broad-reasoning-cycle.sh`.
      - added/updated behavior gates in `artificer-api` for decision completeness, stricter teacher cross-domain coverage, and re-plan trigger normalization.
      - baseline2 (`broad4-strict-baseline2`) to post-fix (`broad4-strict-post1`) improved overall from `84.94` to `90.81`; adversarial `74.00` -> `89.00`; recovery `68.75` -> `90.25`; cross-domain `83.25` -> `85.00`; fail-open/contradiction remained `0.0000`.
      - holdout transfer (`broad4-holdout-v4-post1`) reached overall `91.62` with fail-open/contradiction `0.0000`; strict reliability gates passed.
    - Ran strict v5 cycle (`broad5-*`) with fresh v5 base/adversarial + holdout fixtures and stronger completion-contract enforcement:
      - added `decision-completeness-contract` blocking gate plus explicit decision normalization in `artificer-api` (`Decision`, `Fallback Path`, `Disconfirming Evidence`, `Priority Order`).
    - Added adversarial false-premise hardening in `artificer-api`:
      - adversarial final-contract now requires explicit `False Premise Challenge` and `Premise Validation` signals in addition to assumptions/conflict/contradiction/trap checks.
      - adversarial normalization now injects those sections when missing, with scenario-focused wording and explicit invalidating-evidence requirements.
      - controller contract guidance now explicitly asks for those sections on adversarial/plausible-false prompts.
    - Added targeted regression fixture pack `artificer-broad-reasoning-regressions-v17.tsv` for plausible-but-false-premise resistance across architecture, forensics, security/compliance, and teaching domains.
    - Executed focused validation on the new pack:
      - `broad17-regressions-post2`: strong adversarial/ambiguity outcomes (`avg adversarial 98.00`, `avg ambiguity 100.00`, `assumption_leak_rate 0.0000`, `contradiction_rate 0.0000`) with one timeout under intentionally tight 120s budget.
      - `broad17-policy-r3`: isolated rerun at higher budget confirms policy-domain false-premise case completes cleanly (`overall 97.00`, fail-open/contradiction/shallow all `0.0000`).
    - Added assumption-revision hardening for wrong-initial-hypothesis reliability in `artificer-api`:
      - new contract trigger coverage for misconception/false-premise language plus direct revision phrasing (`initial assumption`, `assumption revision`, `invalidated`, `confidence shift`, `before/after confidence`).
      - execution-path enforcement in main controller loop: normalization + hard completion gate requiring `Initial Assumption`, `Invalidating Evidence`, `Revised Decision`, and `Evidence Delta`.
      - reasoning-salvage path now carries the same assumption-revision contract requirements before mode promotion to `DONE`.
      - controller prompt policy updated so misconception/recovery prompts explicitly require the same four sections.
    - Added targeted regression fixture pack `artificer-broad-reasoning-regressions-v18.tsv` for assumption-revision pressure across forensics, incident response, teaching, and strategic planning domains.
    - Validation notes for v18:
      - clean rerun `broad18-regressions-post2` passed all tasks with strong robustness quality (`avg overall 96.50`, adversarial `98.00`, ambiguity/cross-domain/recovery `100.00`, fail-open/assumption-leak/contradiction all `0.0000`).
      - repeated runs (`post1`, `post3`) showed timeout-sensitive execution churn on two long report/assistant tasks (missing terminal run-event rows), so this remains tracked as runtime-budget sensitivity rather than a resolved reliability closure.
      - increased cross-domain completion floor from 2 -> 3 domain axes (4 for teaching) and coupled cross-domain prompts to decision-completeness gating.
      - added `adversarial-completeness-gap` taxonomy instrumentation in `broad-reasoning-cycle.sh`.
      - baseline (`broad5-strict-baseline`) to post-fix (`broad5-strict-post1`) improved overall `88.50` -> `90.00`, cross-domain `76.25` -> `81.50`, recovery `88.00` -> `88.75`, with fail-open/contradiction unchanged at `0.0000`.
      - initial holdout (`broad5-holdout-v5-post1`) slightly regressed vs prior benchmark (`91.50` vs `91.62`), classified as overfit risk; anti-overfit fix tightened explicit fallback/disconfirm headings.
      - final holdout verification (`broad5-holdout-v5-post3-anti2`) recovered to overall `91.75` with fail-open/contradiction `0.0000`; strict reliability gates passed.
    - Ran strict v6 cycle (`broad6-*`) with fresh v6 base/adversarial + holdout fixtures focused on broader wording transfer:
      - expanded adversarial trigger heuristics in `artificer-api` to detect near-miss/deception phrasing (`near-miss`, `deceptive`, `counterexample`, `counterevidence`, `misleading`, `retry storm`) and force adversarial reasoning in misconception-heavy teaching prompts.
      - expanded cross-domain trigger heuristics to detect policy/business-risk language (`regulated`, `residency`, `retention`, `legal`, `finance`, `margin`, `cost-to-serve`, `consent`) so multi-lens synthesis gates trigger on semantically equivalent prompts.
      - baseline (`broad6-strict-baseline`) to post-fix (`broad6-strict-post1`) held overall (`90.25` -> `90.25`) while improving cross-domain (`81.50` -> `85.00`) and adversarial robustness (`88.06` -> `89.00`) with fail-open/contradiction remaining `0.0000`.
      - holdout transfer (`broad6-holdout-v6-post1`) reached overall `92.00` vs prior strict benchmark `91.75`, with fail-open/contradiction still `0.0000`; strict reliability gates passed.
    - Ran strict v7 cycle (`broad7-*`) with fresh v7 base/adversarial + holdout fixtures and behavior-stack fixes targeting command-depth transfer and self-correction reliability:
      - added cross-domain trigger coverage for compliance phrasing (`region constraints`, `deletion guarantees`) in `artificer-api` routing.
      - strengthened controller execution policy for complex reasoning (3 command slots unless safety blocked), raised design completion command-depth floors for reasoning-heavy modes, and sanitized unsafe command suggestions to safe local inspection fallbacks to reduce approval stalls.
      - added explicit recovery contract normalization (`Self-Correction Evidence`) plus per-iteration self-correction stream checkpoints to improve observable correction behavior.
      - expanded targeted v7 regressions with evidence-focused causal fixture coverage and validated no recurring taxonomy failures in `broad7-regressions-v7-post1`.
      - baseline (`broad7-strict-baseline`) to post-fix (`broad7-strict-post3`) improved overall `90.44` -> `92.38`, cross-domain `81.50` -> `85.00`, and recovery `89.50` -> `100.00` with fail-open/contradiction fixed at `0.0000`.
      - holdout transfer (`broad7-holdout-v7-post1`) reached overall `93.25` vs strict benchmark `92.00`; all strict reliability gates passed.
    - Ran strict v8c cycle (`broad8c-*`) with fresh v8b base/adversarial + holdout fixtures and stricter non-ceiling rubric calibration:
      - tightened `broad-reasoning-cycle.sh` scoring so recovery/cross-domain/adversarial dimensions no longer saturate at ceiling values, enabling meaningful baseline/post deltas without relaxing thresholds.
      - baseline (`broad8c-strict-baseline`) identified top recurring modes: `recovery-correction-gap`, `cross-domain-lens-gap`, and `command-depth-underuse`.
      - implemented behavior-stack fixes in `artificer-api`: expanded subtle adversarial/cross-domain trigger routing, enforced lens-mapped synthesis sections, enforced explicit `Revised From` recovery evidence, and raised assistant reasoning command-depth floors.
      - added targeted v8b regression fixture pack (`artificer-broad-reasoning-regressions-v8b.tsv`) and validated post-fix regressions in `broad8c-regressions-v8b-post1`.
      - in-battery post-fix (`broad8c-strict-post1`) improved overall `86.62` -> `91.81`, cross-domain `75.00` -> `91.00`, recovery `79.38` -> `100.00`, with fail-open/contradiction unchanged at `0.0000`.
      - initial holdout (`broad8c-holdout-v8b-post1`) was slightly below same-rubric benchmark (`92.75` vs `92.88`), classified as overfit risk; anti-overfit fix enforced assistant depth floor broadly.
      - holdout rerun (`broad8c-holdout-v8b-post2-anti1`) recovered to benchmark parity (`92.88`) with fail-open/contradiction `0.0000`; strict reliability gates passed.
    - Ran strict v16 transfer hardening pass (`broad-v16-*`) focused on domain-grounded cross-domain synthesis:
      - strengthened `artificer-api` cross-domain completion contract to require explicit `Domain Anchor` plus full lens/tradeoff/rejected-alternative coverage before DONE.
      - added canonical domain-label mapping (`reasoning_domain_label_for_prompt`) and scenario-grounded lens normalization so outputs are anchored to task domain, not generic boilerplate.
      - fixed domain-classification false positives by deriving domain hints from primary user prompt slices (excluding assay/run-mode suffixes) and tightening forensics triggers (removed `log`/`replay` substring traps).
      - validated targeted mini holdout (`broad-v16-holdout-mini-r5`) where prior weak cases (`hold16-arch`, `hold16-incident`) both reached `cross_domain=100`.
      - full cycle results: regression battery `broad-v16-regressions-r4` + holdout `broad-v16-holdout-r4` produced transfer report `broad-v16-transfer-r4` with all strict gates passing (`all_gates_pass=true`, holdout overall non-regression, improved axes count `2`, fail-open/contradiction non-increase).
    - Added reasoning reliability guardrails after observing recurring timeout-collapse on non-coding holdout tasks:
      - introduced `prompt_requires_code_implementation` and a DESIGN-state guard that keeps reasoning-completion prompts in reasoning modes instead of falling into `IMPLEMENT` patch-generation loops when final contracts are incomplete.
      - introduced an `active_run_mode` coherence guard so long controller iterations cannot drift into blank/misclassified run-mode behavior mid-run.
      - reran strict matched panel (`broad-intel-pass1-reg-postfix`, `broad-intel-pass1-holdout-postfix`) and cleared prior timeout-collapse cases while passing transfer gates under enforcement (`broad-intel-pass1-postfix-transfer-enforce`).
      - residual risk: scoring still over-rewards contract-rich boilerplate; next cycle should add anti-template specificity checks so scenario depth, not section density, drives scores.
    - Strengthened runtime evidence quality in `artificer-api` by anchoring verification to actual command/status pairs from the current run loop (`Command Anchors`) instead of count-only generic evidence text.
    - Added final-output evidence contract post-processing (`ensure_output_has_runtime_command_evidence`) so DONE responses can inject command-backed `Verification Evidence` and `Claim-to-Evidence Map` when reasoning/reporting contracts apply.
    - Added focused regression fixture `tests/fixtures/artificer-evidence-quality-smoke.tsv` and validated with strict run `evidence-quality-smoke1` (overall `97`, evidence `100`, shallow `0.0000`).
    - Hardened completion reliability with a runtime evidence gate: when reasoning verification is required and commands succeeded, DONE now requires explicit command-anchored evidence (and claim-map when required) before completion.
    - Revalidated evidence gate behavior with strict smoke run `evidence-quality-smoke2` (overall `97`, evidence `100`, shallow `0.0000`) plus real non-assay run check (`1773129296-50950-639392`) confirming command anchors + claim-to-evidence map in final output.
    - Hardened controller section parsing so extraction/recovery now treats only known controller headers as boundaries and keeps colon-rich contract bodies (`Inputs:`, `Invariants:`, etc.) instead of truncating to `CONTRACT: NONE`.
    - Ran focused strict format-guard regression (`format-guard-contractfix1`) and verified malformed controller recoveries preserved scenario-specific contract content in recovered controller output while both tasks still reached `done` with `intelligence=100` and `flow=100`.
    - Added explicit compaction for `Latest user request` before controller prompt assembly and introduced a format-recovery reduced-context profile (history/snapshot/feedback/request tightening + workspace-thread suppression) for subsequent iterations after malformed controller recoveries.
    - Revalidated with focused strict run `format-guard-contextpressure1`; quality stayed maxed (`100/100`) and one of two prior circuit-breaker finals converted to a normal design-complete path under recovery pressure (circuit-breaker signatures `2 -> 1` across matched two-task probes).
    - Hardened recovered-command extraction by mode: recovered command candidates are now sanitized/deduped and backfilled with safe mode-specific read-only fallbacks when parsed command lists are sparse.
    - Added adaptive controller format-retry skipping under budget pressure (`run_budget_remaining_seconds` + `should_skip_controller_format_retry`) so low-value retry calls do not consume late run budget and force avoidable format-instability circuit-breaker exits.
    - Fixed `assay-cycle.sh` task ingestion to support both compact and extended TSV fixtures by reading `task/mode/budget` from the first three columns and prompt text from the final column, preventing prompt misalignment on multi-column batteries.
    - Added deterministic header skipping in `assay-cycle.sh` for both task batteries (`task_id,mode,budget`) and decision fixtures (`case,expected_category,...`); revalidated with `evidence-quality-headerfix1` where execution now correctly targets `evidence-smoke-01` instead of synthetic `task_id`.
    - Hardened command-truth accounting in `artificer-api`: missing-file/context command misses no longer report as `ok`; they now emit non-fatal statuses (`missing_input`, `context_missing`) that do not inflate successful-command counters used by verification/evidence quality gates.
    - Updated runtime command-evidence contract parsing to recognize the new non-fatal statuses and surfaced `context_miss` counts in streamed command summaries for better operator diagnosis during ambiguous investigations.
    - Added automatic context-recovery fallback execution when an iteration only encounters non-fatal context misses (`missing_input/context_missing`) and still has command budget remaining: Artificer now injects one safe mode-aware read-only discovery command instead of ending the step with zero verified evidence.
    - Added explicit anti-thrash context-miss guidance to controller prompts: when prior command anchors show `missing_input/context_missing`, the next controller prompt now includes those anchors, requires discovery-first recovery, and warns against repeating the same path assumptions without new evidence.
    - Added focused regression fixture `tests/fixtures/artificer-context-miss-recovery-smoke.tsv` to exercise missing-path recovery behavior under assistant/report modes.
    - Added assistant payloads directly to run-event JSON entries (`build_run_event_json` + all run paths), so event-driven UI/state consumers no longer depend on separate message backfills to display the final response text.
    - Hardened decision lifecycle flow: new runs now always clear stale decision request files (including retry paths), and final assistant output now auto-includes a concrete decision summary (`Question` + `Options`) whenever a decision is pending, preventing blank/ambiguous `awaiting_decision` UX states.
    - Added autonomous-default runtime override for missing-input decision gates: when prompts explicitly request autonomous execution/default assumptions, Artificer no longer pauses with `awaiting_decision` on missing-input heuristics (safety gates for explicit choice/external/destructive still take precedence).
    - Added a high-risk fail-closed verification gate in `artificer-api` for security/compliance/incident-class prompts:
      - new high-risk prompt detector (`prompt_requires_high_risk_fail_closed`) and mandatory FINAL contract sections (`Verification Status`, `Go/No-Go`, `Required Evidence to Proceed`, `Residual Risk`).
      - added normalization + validation helpers (`normalize_high_risk_fail_closed_contract`, `final_has_high_risk_fail_closed_contract`) and integrated them into the controller FINAL gating path before completion.
      - blocked DESIGN and VERIFY completion when high-risk fail-closed contract remains incomplete, and added final-output safety normalization that can demote DONE -> VERIFY when fail-closed requirements are not met.
    - Added source-quality + contradiction completion contracts in `artificer-api` for reasoning/reporting runs:
      - introduced `Source Quality Ranking`, `Contradiction Check`, and `Source Conflict Resolution` normalization (`normalize_source_quality_contradiction_contract`) plus validation helper (`final_has_source_quality_contradiction_contract`).
      - wired contract enforcement into controller FINAL normalization/gating and final DONE-output post-processing so completion is withheld when confidence-tiered source ranking or contradiction resolution is missing.
      - applied the same normalization in assay reasoning-output contracts and revalidated with strict smoke run `source-quality-contradiction-smoke1` (`intelligence=100`, `flow=100`, `verification=1`).
    - Added explicit claim-to-evidence completeness metrics in `broad-reasoning-cycle.sh` strict reporting:
      - introduced per-task `claim_evidence_completeness` scoring in battery/holdout score TSV output and surfaced a new taxonomy mode `claim-evidence-completeness-gap`.
      - extended summary JSON with `avg_claim_evidence_completeness` and `claim_evidence_gap_rate`, and added claim-to-evidence sections in markdown score reports.
      - extended transfer-gap delta reporting with `claim_evidence_completeness` deltas and revalidated with smoke cycle `claim-evidence-metric-smoke1` plus transfer report `claim-evidence-metric-smoke1-transfer`.
    - Ran strict pass2 hardening cycle (`broad-intel-pass2-*`) focused on closing the residual "contract-rich but shallow" gap:
      - added prompt-anchored scenario-depth behavior gates in `artificer-api` (`prompt_anchor_tokens_for_depth`, `final_has_scenario_specific_depth`, `normalize_scenario_depth_final_contract`) and wired them into controller FINAL gating plus final output normalization.
      - added timeout-aware run-event semantics in `artificer-api` (`run_event_status_from_run`) so time-budget exhaustion records `status=timeout` instead of being silently normalized to `done`.
      - tightened strict scorer penalties in `broad-reasoning-cycle.sh` for partial/timeout outputs and included claim-to-evidence completeness in aggregate overall/transfer-readiness scoring.
      - attempted anti-overfit extension pass (extra reasoning iteration + non-DONE evidence enrichment); first variant overran budgets on holdout and was tightened with remaining-budget guard (`>=75s`, `+1` max extension).
      - latest rerun (`broad-intel-pass2-reg-postfix-rerun2`, `broad-intel-pass2-holdout-postfix-rerun2`) still fails transfer gates (`broad-intel-pass2-postfix-rerun2-transfer`): fail-open/contradiction remain `0.0000`, but holdout overall remains below battery (`91.12` vs `97.00`) with shallow completion still high (`0.7500`).
      - explicit status: no meaningful transfer-gate improvement achieved in pass2; improvements are currently instrumentation/gating realism plus better failure visibility, not yet broad reasoning reliability parity.
      - identified and fixed a measurement-integrity bug in strict scoring: `partial_timeout_signal` matched any `incomplete` text, which incorrectly penalized normal assumption lines (`incomplete constraints`) as timeout-partial outputs and inflated shallow-completion rates.
      - narrowed partial-timeout detection to explicit timeout/partial-run phrases only, and updated assay fallback assumption wording in `artificer-api` from `incomplete constraints` to `underspecified constraints` to avoid lexical collision with timeout semantics.
      - added non-`DONE` reasoning completion salvage in `artificer-api`: when open-ended runs are about to finalize with intermediate contracts and budget remains, Artificer now performs one final synthesis pass and only promotes to `DONE` if source-quality/contradiction + scenario-depth + runtime command-evidence contracts all pass.
      - added a reasoning budget-reservation guard in the controller loop so open-ended runs preserve final seconds for completion salvage instead of consuming all budget inside repeated non-converging iterations.
      - validated strict pass3 reruns:
        - regression panel (`broad-intel-pass3-reg-salvage-r2`) recovered to `avg overall 97.00`, `shallow_completion_rate 0.0000`.
        - holdout panel (`broad-intel-pass3-holdout-salvage-r1`) improved to `avg overall 93.50`, `avg recovery 91.00`, `shallow_completion_rate 0.5000` (from pass2 holdout `91.12`, `86.50`, `0.7500`).
        - transfer gates (`broad-intel-pass3-salvage-transfer`) still fail (`all_gates_pass=false`) because holdout remains below battery (`93.50` vs `97.00`), so INT-012 remains open.
      - added command-pipeline normalization in `artificer-api` (`rewrite_readonly_pipeline_candidate`) so read-only near-miss controller commands (`cat/head/tail ... | grep ...`) are rewritten to mediated single-command `rg ... <file>` equivalents instead of being discarded by allowlist parsing.
      - expanded targeted regression fixture coverage (`artificer-broad-reasoning-regressions-v16.tsv`) with `reg16-command-pipeline` to exercise pipeline-shaped command candidates under adversarial ambiguity.
      - strict pass4 validation with the stable pipeline fix:
        - regression panel (`broad-intel-pass4-reg-pipeline`) completed `5/5` with `avg overall 95.40`, and the new `reg16-command-pipeline` task finished `done` with no command-parse taxonomy recurrence.
        - holdout panel (`broad-intel-pass4-holdout-pipeline-r1`) completed `8/8` (including prior weak tasks `hold16-ux` and `hold16-strategy`) with fail-open/contradiction still `0.0000`, but transfer gates remain open because holdout aggregate is still below battery (`92.38` vs `95.40`) and shallow completion remains high (`0.6250`).
      - attempted one holdout-only anti-overfit pass by tightening final evidence text injection; rerun (`broad-intel-pass4-holdout-pipeline-r2`) regressed sharply (`done 6/8`, one `error`, one `timeout`), so that change was reverted immediately and not retained.
      - added runtime convergence hardening in `artificer-api` for open-ended reasoning under tight budgets:
        - introduced `reasoning_completion_reserve_seconds` to reserve a dynamic synthesis budget before the run exhausts.
        - made format-retry suppression more aggressive under low remaining budget (`should_skip_controller_format_retry`) to avoid spending final budget on low-value malformed-controller retries.
      - added targeted runtime-pressure fixture pack `artificer-broad-reasoning-regressions-v19.tsv` to stress the same adversarial assumption-revision domains under constrained budget.
      - validation status for this pass:
        - `broad19-convergence-post1` and `broad18-regressions-post4/post5` improved some late-loop convergence behavior but still showed startup-sensitive first-task no-event errors and one shallow-completion timeout path under constrained budgets.
        - explicit status: no meaningful transfer-reliability improvement confirmed yet; changes are retained as behavior hardening + instrumentation for the remaining runtime-stability gap.
      - added stale-run reconciliation hardening in `artificer-api` to close the startup-sensitive no-terminal-event gap:
        - `queue_running_active_for_dir` now treats orphaned `running.txt` locks as active only inside missing-pid grace windows, avoiding perpetual synthetic `running` states.
        - added shared stale-state helpers (`queue_running_stale_reason_for_conversation`, `queue_recover_stale_running_state_for_conversation`) and wired them into `get_conversation` active run-event synthesis plus full `state` generation.
        - direct (non-queued) stale runs now emit a terminal `error` run event (`append_stale_error_run_event_for_conversation`) and clear runtime locks; queued stale runs are requeued only when a valid `running.id` exists.
        - validated with targeted repros: simulated stale direct-run lock, simulated stale queued lock, and forced `SIGKILL` during `action=run`; all now resolve without lingering synthetic `running` event residue.
      - added live rich same-thread reasoning enforcement beyond the strict assay path:
        - created `rich-reasoning-cycle.sh` plus v46 regression/holdout fixture packs to exercise real non-assay same-thread revisions under `run_mode=auto`, `compute_budget=quick`, and `advanced_loop=0`.
        - fixed real transfer defects in `artificer-api`: natural architecture/product/teaching anchor misses, rich follow-ups misclassified as agent-execution prompts by phrases like `make the revised decision explicit`, and a bounded-execution failure where rich follow-up requests could exceed the assay request timeout.
        - introduced a deterministic quick-path fast contract for rich same-thread follow-ups, so revised labeled reasoning no longer depends on a second model call when the scenario and delta are already recoverable from thread state.
        - validation: `rich-v46-regressions-r3`, `rich-v46-holdout-r3`, and `rich-v46-transfer-r1` all pass with `exact_contract_rate=1.0`, `avg_required_ratio=1.0`, `core_labels_exact_once_rate=1.0`, and zero fail-open / contradiction / shallow completion.
        - release enforcement: `release-gate.sh` now includes the new `rich_thread_transfer` gate and `release-gate-gui-soak.sh` reuses the rich summaries/transfer JSON during live GUI soak runs; smoke `release-gate-rich-v46-smoke1` passes with the new gate enabled.
      - removed explicit-scaffold dependence from live rich same-thread reasoning:
        - created v47 regression/holdout fixtures where the follow-up omits `same plan` / `same labels` and only supplies changed conditions plus update language like `Update the recommendation and say what overturned the first read.`
        - baseline `rich-v47-regressions-r1` / `rich-v47-holdout-r1` exposed the real weakness: the second turn preserved rich structure but often reset to generic `cross-domain integrated reasoning`, using the delta sentence itself as the scenario instead of the prior thread state.
        - fixed `artificer-api` by adding implicit rich follow-up detection gated on a prior reasoning contract plus update/reassessment language and changed-condition cues (`now`, `but`, `still`, `what overturned the first read`, and similar).
        - validation: `rich-v47-regressions-r2`, `rich-v47-holdout-r2`, and `rich-v47-transfer-r1` all pass with `exact_contract_rate=1.0`, `avg_required_ratio=1.0`, `generic_fallback_rate=0.0`, and zero fail-open / contradiction / shallow completion.
        - release enforcement: the default rich live-thread gate in `rich-reasoning-cycle.sh`, `release-gate.sh`, and `release-gate-gui-soak.sh` now points at the stronger v47 implicit-thread fixture family.
      - extended live rich same-thread reasoning to question-form follow-ups:
        - created v48 regression/holdout fixtures where the second turn is a natural question like `Would you still ... now that ...?` and still requires the full rich revision contract.
        - baseline `rich-v48-regressions-r1` / `rich-v48-holdout-r1` exposed the real next weakness: question-form follow-ups usually preserved the six core labels but dropped the four revision labels, and one natural forensics phrasing (`failover practice`, `replica kept diverging`, `node clocks did not agree on event order`) still misrouted into generic cross-domain reasoning.
        - fixed `artificer-api` by making question-form rich follow-ups inherit revision semantics whenever thread state plus a concrete follow-up delta are present; `reasoning_latest_prompt_text` now stops before `Prior scenario:` / `Prior decision summary:` scaffolding; delta extraction strips question wrappers such as `Would you still ... now that`; and implicit follow-ups now reuse prior anchor sources.
        - widened natural forensics routing/anchor cues so `failover practice` + `replica kept diverging` + `event order` resolves to `debugging/forensics with incomplete evidence` and the anchor `replica divergence after failover practice with drifting node clocks and replay-order flips`.
        - validation: `rich-v48-regressions-r3`, `rich-v48-holdout-r3`, and `rich-v48-transfer-r1` all pass with `exact_contract_rate=1.0`, `avg_required_ratio=1.0`, `generic_fallback_rate=0.0`, and zero fail-open / contradiction / shallow completion.
        - release enforcement: the default rich live-thread gate in `rich-reasoning-cycle.sh`, `release-gate.sh`, and `release-gate-gui-soak.sh` now points at the stronger v48 question-thread fixture family.
      - extended live rich same-thread reasoning to ultra-loose question follow-ups:
        - created v49 regression/holdout fixtures where the follow-up is only changed conditions plus a short question like `Do you still back it?` or `What would you do now?`
        - baseline `rich-v49-regressions-r1` / `rich-v49-holdout-r1` exposed the next real weakness: the follow-up often failed detection entirely, producing one-line generic answers or standalone rich reasoning anchored only on the changed-condition sentence.
        - fixed `artificer-api` by broadening implicit rich follow-up detection to short question forms (`do you still`, `what would you do now`, `what do you do now`, `what now`, `where do you land now`, and related variants) while still requiring prior reasoning state plus changed-condition cues.
        - delta extraction now also recovers the changed-condition sentence that precedes a short follow-up question instead of treating the question text itself as the evidence delta.
        - validation: `rich-v49-regressions-r2`, `rich-v49-holdout-r2`, and `rich-v49-transfer-r1` all pass with `exact_contract_rate=1.0`, `avg_required_ratio=1.0`, `generic_fallback_rate=0.0`, and zero fail-open / contradiction / shallow completion.
        - release enforcement: the default rich live-thread gate in `rich-reasoning-cycle.sh`, `release-gate.sh`, and `release-gate-gui-soak.sh` now points at the stronger v49 loose-question fixture family.
      - extended live rich same-thread reasoning to ultra-elliptical follow-ups:
        - created v50 regression/holdout fixtures where the follow-up is only changed conditions plus an almost content-free question like `And now?`, `Still yes?`, `Still harmless?`, or `Do you back that?`
        - baseline `rich-v50-regressions-r1` / `rich-v50-holdout-r1` exposed the next real weakness: most rows stopped being recognized as follow-ups and collapsed to one-line standalone answers; one causal row retained rich structure but anchored on the changed-condition sentence instead of the prior scenario.
        - fixed `artificer-api` by detecting trailing short follow-up questions (1-4 words ending in `?`) when prior reasoning state plus changed-condition cues are present, instead of relying only on explicit question phrases.
        - delta extraction now strips those trailing shorthand questions and recovers the preceding changed-condition sentence as the real evidence delta.
        - validation: `rich-v50-regressions-r2`, `rich-v50-holdout-r2`, and `rich-v50-transfer-r1` all pass with `exact_contract_rate=1.0`, `avg_required_ratio=1.0`, `generic_fallback_rate=0.0`, and zero fail-open / contradiction / shallow completion.
        - release enforcement: the default rich live-thread gate in `rich-reasoning-cycle.sh`, `release-gate.sh`, and `release-gate-gui-soak.sh` now points at the stronger v50 elliptical-thread fixture family.
      - extended live rich same-thread reasoning to deictic follow-ups with broader change verbs:
        - created v51 regression/holdout fixtures where the follow-up is only changed conditions plus a deictic question like `So?`, `Your read?`, `Your call?`, or `Where do you land?`
        - baseline `rich-v51-regressions-r1` / `rich-v51-holdout-r1` exposed the next real weakness: the detector still relied on a narrow update-cue vocabulary, so most rows collapsed to one-line standalone answers; a causal row also misrouted because the changed-condition sentence was treated as a new standalone scenario.
        - fixed `artificer-api` by extracting changed-condition cue detection into a helper and widening it to recognize more state-shift verbs such as `spiked`, `slipped`, `worsened`, `persisted`, `lagged`, `climbed`, `deepened`, `resurfaced`, and punctuation-bounded variants like `persisted,` / `climbed.`.
        - validation: `rich-v51-regressions-r3`, `rich-v51-holdout-r3`, and `rich-v51-transfer-r1` all pass with `exact_contract_rate=1.0`, `avg_required_ratio=1.0`, `generic_fallback_rate=0.0`, and zero fail-open / contradiction / shallow completion.
        - release enforcement: the default rich live-thread gate in `rich-reasoning-cycle.sh`, `release-gate.sh`, and `release-gate-gui-soak.sh` now points at the stronger v51 deictic-thread fixture family.
      - extended live rich same-thread reasoning to punctuation-free deictic suffix follow-ups:
        - created v52 regression/holdout fixtures where the follow-up is only changed conditions plus a bare suffix like `So`, `Your read`, `Your call`, or `Where do you land`
        - baseline `rich-v52-regressions-r1` / `rich-v52-holdout-r1` exposed the next concrete detector bug: the short-follow-up matcher only recognized those phrases when they were isolated as their own sentence, so most rows collapsed to one-line standalone answers and a causal row reinterpreted the updated-condition sentence as a fresh scenario.
        - fixed `artificer-api` by matching the allowed short follow-up clauses as true trailing suffixes, not only punctuation-delimited sentences, and by stripping those suffixes from the end of the prompt during delta extraction so the preceding changed-condition sentence becomes the evidence delta.
        - validation: `rich-v52-regressions-r3`, `rich-v52-holdout-r3`, and `rich-v52-transfer-r1` all pass with `exact_contract_rate=1.0`, `avg_required_ratio=1.0`, `generic_fallback_rate=0.0`, and zero fail-open / contradiction / shallow completion.
        - release enforcement: the default rich live-thread gate in `rich-reasoning-cycle.sh`, `release-gate.sh`, and `release-gate-gui-soak.sh` now points at the stronger v52 bare-deictic-thread fixture family.
      - extended live rich same-thread reasoning to low-verb comparative follow-ups:
        - created v53 regression/holdout fixtures where the follow-up is mostly comparative state fragments like `higher refunds, softer retention, queue age flat. Your read` or `broader VIP impact, noisier rate-limit signal, narrower rollback risk. Where do you land`
        - baseline `rich-v53-regressions-r1` / `rich-v53-holdout-r1` exposed the next real weakness: once the update sentence stopped using explicit verbs such as `worsened` or `persisted`, the detector often stopped recognizing the turn as a revision and fell back to standalone answers or wrong-domain standalone contracts.
        - fixed `artificer-api` by widening changed-condition cue detection to accept comparative state-shift terms such as `higher`, `lower`, `broader`, `narrower`, `tighter`, `weaker`, `stronger`, `softer`, `harder`, `flatter`, `noisier`, `more`, and `less`, both in the fast grep path and the stricter token-boundary matcher.
        - validation: `rich-v53-regressions-r5`, `rich-v53-holdout-r5`, and `rich-v53-transfer-r2` all pass with `exact_contract_rate=1.0`, `avg_required_ratio=1.0`, `generic_fallback_rate=0.0`, and zero fail-open / contradiction / shallow completion.
        - release enforcement: the default rich live-thread gate in `rich-reasoning-cycle.sh`, `release-gate.sh`, and `release-gate-gui-soak.sh` now points at the stronger v53 comparative-thread fixture family.
      - extended live rich same-thread reasoning to deictic state-only follow-ups:
        - created v55 regression/holdout fixtures where the middle turn is a delta-only update sentence and the final turn is a nearly content-free deictic follow-up like `Your call`, `Your read`, `Where do you land?`, or `So?`
        - baseline `rich-v55-regressions-r1` / `rich-v55-holdout-r1` exposed the next real weakness: delta-only middle turns were not being treated as same-thread revisions, so the final deictic turn lost revision state and collapsed into generic one-line answers, shallow standalone outputs, or wrong-domain contracts.
        - fixed `artificer-api` by adding delta-only follow-up detection and prior-scenario recovery so a middle update turn becomes a rich same-thread revision when a prior rich contract exists and its terms overlap the active scenario, then carries that scenario and evidence delta into the final deictic turn.
        - validation: `rich-v55-regressions-r5`, `rich-v55-holdout-r5`, and `rich-v55-transfer-r1` all pass with `exact_contract_rate=1.0`, `avg_required_ratio=1.0`, `generic_fallback_rate=0.0`, and zero fail-open / contradiction / shallow completion.
        - release enforcement: the default rich live-thread gate in `rich-reasoning-cycle.sh`, `release-gate.sh`, and `release-gate-gui-soak.sh` now points at the stronger v55 deictic-state fixture family.
      - added a freeform memo reasoning gate for less-scaffolded open-ended outputs:
        - created new runner `hosted-web/scripts/freeform-reasoning-cycle.sh` plus v56 regression/holdout fixtures where prompts explicitly ask for a short memo in normal prose, not labeled sections, while still requiring a recommendation, main uncertainty, and reversal evidence.
        - baseline live probing exposed a real behavior gap: those prompts still fell through the rich labeled-contract path and returned large `Outcome:` / `Decision:` / `Fallback Path:` style outputs instead of clean prose.
        - fixed `artificer-api` by detecting freeform-memo preference (`normal prose`, `plain prose`, `not labeled sections`, `without labels`, `no headings`, `no bullets`), forcing that prompt family onto a bounded deterministic quick memo path, and generating domain-specific recommendation / rationale / uncertainty / reversal sentences keyed to the scenario anchor instead of the rich contract scaffolding.
        - early v56 runs then exposed the next real issues: the new scorer had invalid `jq` boolean/pattern bindings, and natural UX / forensics / strategy phrasings still fell back to clause-echo anchors because both anchor extraction and domain scoring were too narrow.
        - fixed `freeform-reasoning-cycle.sh` by correcting the `jq` boolean syntax and regex binding so required/forbidden patterns score against the actual fixture patterns instead of self-matching the assistant text.
        - widened `artificer-api` anchor and classifier coverage for the exact missed natural-language variants, including `trust wants a visible review gate ... honest-user completion falls and backend latency stays volatile`, `one replica keeps diverging ... replayed writes land differently after rejoin`, and `pushing harder into a fast-growing region lifts signups ... counsel flags sanctions exposure`.
        - validation: `freeform-v56-regressions-r3`, `freeform-v56-holdout-r3`, and `freeform-v56-transfer-r3` all pass with `freeform_rate=1.0`, `uncertainty_rate=1.0`, `reversal_rate=1.0`, `avg_required_ratio=1.0`, and zero fail-open / contradiction / shallow completion; the live freeform probe that originally returned a labeled contract now returns the intended short prose memo.
    - 2026-03-17: Extended freeform reasoning into less-scaffolded multi-turn revision behavior.
      - added fixtures:
        - `hosted-web/tests/fixtures/artificer-broad-reasoning-regressions-v57-freeformthreadfit.tsv`
        - `hosted-web/tests/fixtures/artificer-broad-reasoning-holdout-v57-freeformthreadfit.tsv`
      - extended `hosted-web/scripts/freeform-reasoning-cycle.sh` with `followup_prompt` support and promoted v57 into the default freeform gate in `release-gate.sh` and `release-gate-gui-soak.sh`
      - `artificer-api` now detects same-thread freeform follow-up memos, carries prior scenario / prior memo context, and emits revised normal-prose memos keyed to explicit updated conditions instead of repeating the first answer
      - validation: `freeform-v57-regressions-r3`, `freeform-v57-holdout-r3`, and `freeform-v57-transfer-r1` all pass with `freeform_rate=1.0`, `uncertainty_rate=1.0`, `reversal_rate=1.0`, `avg_required_ratio=1.0`, and zero fail-open / contradiction / shallow completion
        - release enforcement: `release-gate.sh` now requires the freeform memo transfer gate, `release-gate-v56-freeform-smoke1` passes end-to-end with reused green artifacts for the unchanged gates, and `release-gate-gui-soak.sh` now supports reusing freeform summaries/transfer JSON as well (`release-gui-soak-v56-freeform-smoke1` passes).

## P2 (Scale)

- INT-008 Multi-run learning loop for controller prompts
  - Goal: Feed accepted proposals into versioned controller prompt variants with rollback.
  - Acceptance:
    - Prompt variants are file-versioned and reversible.
    - A/B telemetry compares quality deltas before promotion.
  - Progress:
    - Added file-backed controller variant store with baseline + proposal-derived candidates, manual promote/rollback gates, and per-variant quality aggregates.
    - Wired run-time variant selection into controller prompt construction and recorded run-level telemetry for before/after quality deltas.
    - Surfaced controller variant state and promote/rollback controls in Mode Runtime settings.
    - Injected runtime learning summaries (latest failure taxonomy signals + quality scorecard trend) into each controller loop prompt so active runs can adapt to recent regressions.
    - Added mode-filtered accepted/applied proposal summaries as an additional runtime learning signal in controller prompts, including legacy title fallback when older proposals are missing explicit `source_mode`.
    - Added runtime adaptation guardrails derived from recent taxonomy/quality patterns, feeding actionable anti-regression instructions directly into controller prompts.
    - Added loop-stagnation detection that records repeated-transition failures and injects anti-repeat guardrails into subsequent controller iterations.

- INT-009 Security specialist modes
  - Goal: Strengthen `pentest` and `security-audit` modes with stricter evidence contracts.
  - Acceptance:
    - Mode policies emit structured finding reports (severity, evidence, remediation).
    - Unsafe offensive behavior remains policy-blocked.
  - Progress:
    - Added post-run security output normalization that enforces structured findings with Severity, Evidence, Remediation, and Status when model output is underspecified.
    - Added stronger synthesis contract prompts for `pentest` and `security-audit` finalization paths.

- INT-010 Quality scorecard automation
  - Goal: Continuously track intelligence and flow quality trends.
  - Acceptance:
    - Scorecard file updates per cycle with before/after deltas.
    - Regressions auto-create proposals tagged to affected failure categories.
  - Progress:
    - Added file-backed quality scorecard entries and markdown summary refresh on each scored run.
    - Added regression-triggered proposal generation from scorecard deltas, tagged to recent failure taxonomy categories.
    - Added sustained-regression gating plus per-mode cooldown suppression to reduce repeated low-signal proposal spam while still escalating severe regressions.
    - Added mode-aware failure attribution and dedupe metadata (`source_mode`) so regression proposals are generated/suppressed per mode without cross-mode category contamination.

## 2026-03-18

- Freeform conversational reassessment
  - Added new packs `artificer-broad-reasoning-regressions-v58-freeformconvofit.tsv` / `artificer-broad-reasoning-holdout-v58-freeformconvofit.tsv` to cover weaker-scaffolded conversational prompts that no longer ask for a memo explicitly.
  - Baseline exposed a real parity gap:
    - `freeform-v58-regressions-r1`: `avg_overall=26.75`, `avg_required_ratio=0.3125`, `fail_open_rate=1.0`
    - `freeform-v58-holdout-r1`: `avg_overall=45.50`, `avg_required_ratio=0.4375`, `fail_open_rate=0.75`
  - Root causes:
    - natural prompts like `What would you recommend?`, `What call do you make?`, and `Do you push harder or change course?` still depended on the older structured-reasoning trigger and often fell into the rich labeled path or generic one-line quick replies
    - follow-up turns that matched the broader conversational freeform detector could skip the freeform thread-context builder, so the memo anchored itself to the delta clause instead of the original scenario
  - Implemented fixes in `artificer-api`:
    - added `prompt_prefers_freeform_reasoning_conversation()` and `prompt_prefers_freeform_reasoning_reply()`
    - allowed strong `reasoning_domain_hint()` classifications to qualify conversational freeform routing even when `prompt_prefers_reasoning_completion()` stays silent
    - changed routing so freeform follow-ups always rebuild `rich_reasoning_context_text` through `reasoning_freeform_context_prompt()`
    - corrected prior-scenario parsing and anchor precedence so freeform follow-up memos stay tied to the original scenario rather than the latest delta sentence
    - tightened mixed update deltas toward the decisive post-`but` clause
  - Promoted v58 into the default freeform gate in `freeform-reasoning-cycle.sh`, `release-gate.sh`, and `release-gate-gui-soak.sh`.
  - Validation:
    - `freeform-v58-regressions-r5`: `avg_overall=100.00`, `avg_required_ratio=1.0`, `freeform_rate=1.0`, `uncertainty_rate=1.0`, `reversal_rate=1.0`, fail-open / contradiction / shallow `0.0`
    - `freeform-v58-holdout-r5`: same metrics, all green
    - `freeform-v58-transfer-r1`: `all_gates_pass=true`, `transfer_risk=low`

- Freeform deictic conversational reassessment
  - Added new packs `artificer-broad-reasoning-regressions-v59-freeformdeicticconvofit.tsv` / `artificer-broad-reasoning-holdout-v59-freeformdeicticconvofit.tsv` to cover even looser first turns such as `How does this strike you?`, `What’s your take?`, `What’s your read?`, `What’s the move?`, and `Thoughts?`, followed by deictic reassessment turns such as `And now?`, `Still yes?`, `Still safe?`, `So?`, and `Where does this leave you?`.
  - Baseline exposed a real next-step gap:
    - `freeform-v59-regressions-r1fast`: `avg_overall=48.75`, `avg_required_ratio=0.1250`, `fail_open_rate=1.0`
    - the tighter-budget baseline showed the actual failure mode clearly: the first-turn detector still missed those looser deictic openings, so the wrong execution path timed out before follow-ups were sent
  - Intermediate diagnostics after widening the first-turn detector:
    - `freeform-v59-regressions-r2`: `avg_overall=90.25`, `avg_required_ratio=0.8125`, but still `fail_open_rate=1.0` under the 15-second submit cap
    - `freeform-v59-regressions-r4`: `avg_overall=87.00`, `avg_required_ratio=0.7500`, with follow-up memos still anchoring updates to short questions or raw semicolon lists instead of the original scenario plus decisive delta
  - Root causes:
    - conversational freeform routing still depended on stronger wording than `How does this strike you?`, `What’s your take?`, `What’s your read?`, `What’s the move?`, and `Thoughts?`
    - deictic short-question follow-ups could recover the wrong prior scenario from recent turns, collapsing the scenario anchor onto the most recent delta fragment
    - semicolon-heavy delta updates were repeated verbatim, which hid the decisive adverse tail the freeform scorer was looking for
  - Implemented fixes in `artificer-api`:
    - widened `prompt_prefers_freeform_reasoning_conversation()` for those weaker deictic first-turn phrasings
    - widened `reasoning_followup_short_question_clause()` for `where does this leave you`, `still safe`, `what’s your take/read/call`, `what’s the move`, `how does this strike you`, and `thoughts`
    - changed `reasoning_followup_prior_scenario_from_turns_block()` so deictic follow-ups recover the original thread scenario rather than the most recent delta-only turn
    - changed `reasoning_freeform_context_prompt()` so a short deictic turn reuses the previous user turn as updated conditions when that previous turn clearly carries the delta
    - added `reasoning_focus_delta_phrase()` so semicolon-heavy updates collapse to the decisive trailing risk phrase in plain prose
  - Promoted v59 into the default freeform gate in `freeform-reasoning-cycle.sh`, `release-gate.sh`, and `release-gate-gui-soak.sh`.
  - Validation:
    - `freeform-v59-regressions-r6`: `avg_overall=100.00`, `avg_required_ratio=1.0`, `freeform_rate=1.0`, `uncertainty_rate=1.0`, `reversal_rate=1.0`, fail-open / contradiction / shallow `0.0`
    - `freeform-v59-holdout-r6`: same metrics, all green
    - `freeform-v59-transfer-r1`: `all_gates_pass=true`, `transfer_risk=low`

- Freeform minimal conversational reassessment
  - Added new packs `artificer-broad-reasoning-regressions-v60-freeformminimalconvofit.tsv` / `artificer-broad-reasoning-holdout-v60-freeformminimalconvofit.tsv` to cover almost content-free first-turn request signals such as `Well?`, `Gut check?`, `Initial take?`, `First instinct?`, `Quick read?`, `Gut reaction?`, `Your instinct?`, and `Thought?`, followed by bare deictic reassessment turns like `Still?` and `And?`.
  - Baseline exposed the next real gap:
    - `freeform-v60-regressions-r1fast`: `done=1/4`, `avg_overall=40.00`, `avg_required_ratio=0.0`, `fail_open_rate=1.0`
  - Root causes:
    - those ultra-weak first-turn asks still fell outside the conversational freeform detector
    - bare follow-up turns like `Still?` and `And?` were too short for the existing short-question matcher, so the system could not infer revision intent from thread state alone
  - Implemented fixes in `artificer-api`:
    - widened `prompt_prefers_freeform_reasoning_conversation()` to include `well`, `gut check`, `gut reaction`, `initial take`, `first instinct`, `quick read`, `your instinct`, and `thought`
    - widened `reasoning_followup_short_question_clause()` to treat `still`, `and`, `well`, `gut check`, `gut reaction`, `initial take`, `first instinct`, `quick read`, `your instinct`, and `thought` as valid deictic clauses
    - widened `reasoning_followup_prior_scenario_from_turns_block()` so those weaker clauses still recover the original scenario anchor from the thread instead of drifting to the latest delta fragment
  - Promoted v60 into the default freeform gate in `freeform-reasoning-cycle.sh`, `release-gate.sh`, and `release-gate-gui-soak.sh`.
  - Validation:
    - `freeform-v60-regressions-r2`: `avg_overall=100.00`, `avg_required_ratio=1.0`, `freeform_rate=1.0`, `uncertainty_rate=1.0`, `reversal_rate=1.0`, fail-open / contradiction / shallow `0.0`
    - `freeform-v60-holdout-r2`: same metrics, all green
    - `freeform-v60-transfer-r1`: `all_gates_pass=true`, `transfer_risk=low`

- Freeform implicit scenario reassessment
  - Added new packs `artificer-broad-reasoning-regressions-v61-freeformimplicitconvofit.tsv` / `artificer-broad-reasoning-holdout-v61-freeformimplicitconvofit.tsv` to cover the next real-use gap: the user gives only a conflicted scenario statement on the first turn, then only an updated situation statement on the second turn, with no overt request wording in either turn.
  - Baseline exposed the next real gap:
    - `freeform-v61-regressions-r1fast`: `done=1/4`, `avg_overall=55.00`, `avg_required_ratio=0.25`, `fail_open_rate=0.75`
  - Root causes:
    - the first-turn detector still depended on overt ask phrasing and would not route pure scenario exposition into the freeform reasoning path
    - freeform routing activation still depended on the older explicit reasoning-completion trigger, so even prompts that were clearly scenario-rich could miss the fast freeform path
  - Implemented fixes in `artificer-api`:
    - added `prompt_prefers_freeform_reasoning_implicit_scenario()` to detect strong domain-specific tradeoff scenarios even without overt request wording
    - updated `prompt_prefers_freeform_reasoning_reply()` to include the new implicit-scenario detector
    - changed run setup so a prompt that qualifies for freeform reasoning activates `rich_reasoning_prompt=1` / `freeform_reasoning_prompt=1` directly, instead of first needing `prompt_prefers_reasoning_completion()`
  - Promoted v61 into the default freeform gate in `freeform-reasoning-cycle.sh`, `release-gate.sh`, and `release-gate-gui-soak.sh`.
  - Validation:
    - `freeform-v61-regressions-r2`: `avg_overall=100.00`, `avg_required_ratio=1.0`, `freeform_rate=1.0`, `uncertainty_rate=1.0`, `reversal_rate=1.0`, fail-open / contradiction / shallow `0.0`
    - `freeform-v61-holdout-r2`: same metrics, all green
    - `freeform-v61-transfer-r1`: `all_gates_pass=true`, `transfer_risk=low`

- Freeform fragment scenario reassessment
  - Added new packs `artificer-broad-reasoning-regressions-v62-freeformfragmentscenefit.tsv` / `artificer-broad-reasoning-holdout-v62-freeformfragmentscenefit.tsv` to cover the next weaker framing style: multiple short factual sentences with no overt ask and no connective tradeoff wording, followed by short factual update sentences in the same style.
  - Baseline exposed two real remaining gaps:
    - `freeform-v62-regressions-r1fast`: `done=2/4`, `avg_overall=40.00`, `avg_required_ratio=0.0`, `fail_open_rate=1.0`
    - `freeform-v62-regressions-r3`: after the initial routing fix, `avg_overall=90.25`, `avg_required_ratio=0.8125`, `fail_open_rate=0.75`
  - Root causes:
    - the implicit freeform route still expected explicit tradeoff markers like `but`, `however`, or comma-heavy structure, so fragment-style multi-sentence exposition could miss the freeform path entirely
    - even when routed correctly, anchor extraction collapsed fragment-style prompts to their first sentence, and dot-separated delta updates were repeated verbatim instead of focused onto the decisive adverse tail
  - Implemented fixes in `artificer-api`:
    - added `prompt_has_implicit_scenario_sentence_shape()` so multi-sentence domain-specific exposition counts as a valid implicit reasoning trigger
    - widened fragment-style scenario anchor extraction across the architecture, forensics, security/compliance, product/UX, metrics/causality, incident, teaching, and strategy families
    - extended `reasoning_focus_delta_phrase()` to collapse dot-separated updates to the last two decisive segments, not just semicolon-delimited lists
  - Promoted v62 into the default freeform gate in `freeform-reasoning-cycle.sh`, `release-gate.sh`, and `release-gate-gui-soak.sh`.
  - Validation:
    - `freeform-v62-regressions-r4`: `avg_overall=100.00`, `avg_required_ratio=1.0`, `freeform_rate=1.0`, `uncertainty_rate=1.0`, `reversal_rate=1.0`, fail-open / contradiction / shallow `0.0`
    - `freeform-v62-holdout-r4`: same metrics, all green
    - `freeform-v62-transfer-r1`: `all_gates_pass=true`, `transfer_risk=low`

- Freeform dash-scene reassessment
  - Added new packs `artificer-broad-reasoning-regressions-v63-freeformdashscene.tsv` / `artificer-broad-reasoning-holdout-v63-freeformdashscene.tsv` to cover the next weaker framing style: dash-separated note scenes like `One shared feed looks simpler - retention differs by merchant - late failover events replay out of order - messy.` followed by equally terse dash-separated update notes.
  - Baseline exposed two real remaining gaps:
    - `freeform-v63-regressions-r1fast`: `done=2/4`, `avg_overall=63.50`, `avg_required_ratio=0.3750`, `fail_open_rate=1.0`
    - `freeform-v63-regressions-r2`: after note-shape routing was fixed, `avg_overall=90.25`, `avg_required_ratio=0.8125`, `fail_open_rate=0.75`
  - Root causes:
    - the implicit freeform route still treated note-style dash fragments as unstructured prose, so architecture and causal rows fell out of the bounded freeform path and timed out under the tighter baseline
    - even when routed correctly, the freeform delta sentence kept quoting the full three-fragment update instead of compressing to the decisive last two conditions, which missed required scenario-specific evidence phrases and still counted as fail-open
  - Implemented fixes in `artificer-api`:
    - widened `prompt_has_implicit_scenario_sentence_shape()` so ` - ` note fragments count as real scenario structure
    - widened `reasoning_followup_fragment_delta_present()` to split on dash-separated note fragments in addition to comma/semicolon/colon fragments
    - tightened `reasoning_focus_delta_phrase()` to normalize dash-separated updates before collapsing them to the decisive tail
    - fed the focused delta back into `reasoning_freeform_context_prompt()` so freeform follow-up memos carry the compressed updated conditions all the way through finalization
  - Promoted v63 into the default freeform gate in `freeform-reasoning-cycle.sh`, `release-gate.sh`, and `release-gate-gui-soak.sh`.
  - Validation:
    - `freeform-v63-regressions-r3`: `avg_overall=100.00`, `avg_required_ratio=1.0`, `freeform_rate=1.0`, `uncertainty_rate=1.0`, `reversal_rate=1.0`, fail-open / contradiction / shallow `0.0`
    - `freeform-v63-holdout-r3`: same metrics, all green
    - `freeform-v63-transfer-r1`: `all_gates_pass=true`, `transfer_risk=low`

- Freeform intent discrimination for ambiguous note scenes
  - Added new mixed-behavior packs `artificer-broad-reasoning-regressions-v64-freeformintentfit.tsv` / `artificer-broad-reasoning-holdout-v64-freeformintentfit.tsv` to cover the next real-use gap: terse domain notes that sometimes clearly imply “give me the call” and sometimes are still ambiguous enough that Artificer should ask one concise clarifying question instead of guessing.
  - Initial regression was green but the fresh holdout exposed two real seams:
    - `freeform-v64-holdout-r1`: `avg_overall=93.25`, `avg_required_ratio=0.9167`, `fail_open_rate=0.5000`, `forbidden_hit_rate=0.2500`
  - Root causes:
    - the new forensics clarify response reused the phrase `main uncertainty`, which made the answer sound like analysis had already been performed and tripped the holdout forbidden-pattern guard
    - the causal holdout prompt shape `Trial starts jumped. Refunds rose later. Queue age climbed. One cohort retained worse after day 21.` did not match an existing anchor rule, so the memo collapsed to the weak anchor `trial starts jumped`
    - a broader latent bug also surfaced during the same pass: `prompt_prefers_freeform_reasoning_followup_memo()` was treating every freeform prompt as a follow-up, so first-turn freeform memos could inject fake `Given the updated conditions ...` prose even with no prior thread state
  - Implemented fixes in `artificer-api`:
    - added `prompt_has_ambiguous_note_fragment_shape()` plus `prompt_prefers_freeform_intent_clarify()` so ambiguous note piles route to a deterministic clarify path instead of the memo path
    - added `reasoning_freeform_clarifying_question_for_prompt()` and `normalize_reasoning_freeform_clarify_response()` for domain-specific one-line clarify prompts
    - removed the unconditional freeform-followup shortcut in `prompt_prefers_freeform_reasoning_followup_memo()` so first-turn memos stop pretending they are revisions
    - tightened `reasoning_freeform_delta_sentence_for_prompt()` so delta text only appears when the prompt truly carries an `Updated conditions:` block
    - widened metrics/causality anchor extraction to recognize the weaker `trial starts jumped / refunds rose later / queue age climbed / day 21 retention` shape as `trial-start gains versus refunds, queue age, and weak cohort retention`
    - softened the forensics clarify wording from `main uncertainty` to `biggest unknown`
  - Implemented fixes in `freeform-reasoning-cycle.sh`:
    - scoring is now mode-aware, so mixed families can require either a freeform memo or a clarify question
    - summaries now report `style_rate`, `freeform_rate`, and `clarify_rate` separately
    - transfer gating now enforces full style-match plus clarify coverage, not just memo coverage
  - Promoted v64 into the default freeform gate in `freeform-reasoning-cycle.sh`, `release-gate.sh`, and `release-gate-gui-soak.sh`.
  - Validation:
    - `freeform-v64-regressions-r3`: `style_rate=1.0`, `freeform_rate=1.0`, `clarify_rate=1.0`, `avg_required_ratio=1.0`, fail-open / contradiction / shallow `0.0`
    - `freeform-v64-holdout-r2`: same metrics, all green
    - `freeform-v64-transfer-r2`: `all_gates_pass=true`, `transfer_risk=low`

- Freeform clarify-followup continuity for ambiguous note scenes
  - Added new mixed-behavior packs `artificer-broad-reasoning-regressions-v65-freeformclarifyfollowupfit.tsv` / `artificer-broad-reasoning-holdout-v65-freeformclarifyfollowupfit.tsv` to cover the next real-use gap: the user gives ambiguous note fragments, Artificer asks a clarifying question, and the user replies tersely with intent like `Give me the recommendation.`, `The safer path.`, `The likely read.`, or `Give me the explanation approach.`
  - Initial state was asymmetric:
    - regression side was already repairable once post-clarify context reconstruction was added
    - fresh holdout `freeform-v65-holdout-r4` still failed on teaching with `avg_overall=75.00`, `avg_required_ratio=0.7500`, `fail_open_rate=0.2500`, and `contradiction_rate=0.2500`
  - Root causes:
    - the post-clarify path only recognized a narrow set of clarify phrasings, so a generic assistant reply like `Please provide the tokenized production snippets and any context needed to assist further.` did not count as clarify state
    - when that clarify state was missed, the terse second turn `Give me the explanation approach.` was treated as a new standalone prompt and fell into the rich labeled contract path anchored on the reply itself
  - Implemented fixes in `artificer-api`:
    - taught `reasoning_recent_user_turns_block_for_prompt()`, `reasoning_prior_scenario_block_for_prompt()`, and `reasoning_freeform_updated_conditions_block_for_prompt()` to stop at `Prior clarify question:` so post-clarify context stays clean
    - added `reasoning_freeform_post_clarify_context_prompt()` so terse intent replies rebuild recent turns, prior scenario, and prior clarify state explicitly
    - widened `assistant_output_is_freeform_clarify_question()` so generic assistant clarifiers that ask the user to provide more details or context still count as clarify state
    - kept the freeform memo anchored to the original scenario instead of the short clarify reply
  - Promoted v65 into the default freeform gate in `freeform-reasoning-cycle.sh`, `release-gate.sh`, and `release-gate-gui-soak.sh`.
  - Validation:
    - `freeform-v65-regressions-r8`: `style_rate=1.0`, `freeform_rate=1.0`, `clarify_rate=1.0`, `avg_required_ratio=1.0`, fail-open / contradiction / shallow `0.0`
    - `freeform-v65-holdout-r5`: same metrics, all green
    - `freeform-v65-transfer-r1`: `all_gates_pass=true`, `transfer_risk=low`

- Freeform recommendation vs clarify vs reflect triage for ambiguous note scenes
  - Added new mixed-behavior packs `artificer-broad-reasoning-regressions-v66-freeformtriagefit.tsv` / `artificer-broad-reasoning-holdout-v66-freeformtriagefit.tsv` to cover the next real-use gap: terse domain notes that sometimes warrant a recommendation, sometimes warrant one clarifying question, and sometimes warrant a short reflective framing instead of either.
  - Initial regression `freeform-v66-regressions-r1` failed materially with `avg_overall=72.00`, `style_rate=0.6667`, and `reflect_rate=0.0000` because reflection prompts were still falling into the clarify path.
  - Root causes:
    - there was no dedicated freeform reflection detector or bounded reflection path, so ambiguous-but-not-actionable scenes were treated as intent ambiguity and routed to clarification
    - `prompt_prefers_freeform_intent_clarify()` contained a real regex bug where `move` matched inside `removed`, which broke teaching-note clarification on prompts like `Raw secrets removed.`
    - product and architecture anchor extraction were too narrow for weaker natural phrasing like `Something feels off.` and `Shared feed is easier to operate. Merchants have different retention rules.`
    - one product holdout fixture still expected a `volatile latency` anchor even though the prompt no longer mentioned latency
  - Implemented fixes in `artificer-api`:
    - added `prompt_has_reflective_ambiguity_cue()` and `prompt_prefers_freeform_reflection()`
    - added deterministic domain-specific reflection generation and normalization with explicit `The tension is ...` / `The unresolved question is ...` sentences
    - added a reflection fail-safe / quick path so reflective notes do not fall into long-loop or clarify behavior
    - tightened clarify detection by changing the loose `move` regex to `move\b`
    - widened product and architecture anchor detection for the weaker prompt shapes found in holdout
  - Validation:
    - `freeform-v66-regressions-r4`: `avg_overall=100.00`, `style_rate=1.0`, `clarify_rate=1.0`, `reflect_rate=1.0`
    - `freeform-v66-holdout-r4`: same metrics, all green
    - `freeform-v66-transfer-r1`: `all_gates_pass=true`, `transfer_risk=low`
  - Promoted v66 into the default freeform gate in `freeform-reasoning-cycle.sh`, `release-gate.sh`, and `release-gate-gui-soak.sh`.

- Freeform narration vs recommendation/clarify/reflect discrimination
  - Added new mixed-behavior packs `artificer-broad-reasoning-regressions-v67-freeformnarrationfit.tsv` / `artificer-broad-reasoning-holdout-v67-freeformnarrationfit.tsv` to cover the next real-use gap: terse status or context notes that should not be treated as a settled request for a decision yet.
  - Added a fourth bounded freeform behavior in `artificer-api`: `frame`, for replies that should briefly orient the user to the situation, name the key moving parts, and offer the next analysis step without forcing either a recommendation or a clarifying question.
  - Initial implementation exposed two real problems:
    - `freeform-reasoning-cycle.sh` was still using pre-v67 summary column indexes, which made the first regression summary undercount `style_rate`
    - once the scorer was fixed, the first clean passes still found real anchor gaps: forensics frame prompts could anchor on `Current picture:` instead of the underlying failover scenario, and security narration prompts with regional outage overrides, analyst-access expansion, residency drift, and weak deletion proof were collapsing to a first-clause anchor
  - Implemented fixes:
    - added `prompt_has_narration_context_cue()` and `prompt_prefers_freeform_frame()`
    - added deterministic domain-specific framing generation with `not a settled decision request yet` and `The key moving parts are ...`
    - extended the freeform runner to score `frame_rate`, `frame_status_rate`, and `moving_parts_rate`
    - taught the security anchor extractor the natural `regional outage override ... analyst access ... residency drift ... deletion proof` shape
    - widened forensics anchor coverage for `replica diverges` and `replay batches arrive twice`
  - Validation:
    - `freeform-v67-regressions-r3`: all tracked rates `1.0`, `avg_overall=100.00`
    - `freeform-v67-holdout-r4`: same metrics, all green
    - `freeform-v67-transfer-r1`: `all_gates_pass=true`, `transfer_risk=low`
  - Promoted v67 into the default freeform gate in `freeform-reasoning-cycle.sh`, `release-gate.sh`, and `release-gate-gui-soak.sh`.

- Freeform continuity from prior framing into recommendation or reflection
  - Added new mixed-behavior packs `artificer-broad-reasoning-regressions-v68-freeformframefollowupfit.tsv` / `artificer-broad-reasoning-holdout-v68-freeformframefollowupfit.tsv` to cover the next real-use gap: the user first provides status/context notes that should trigger a `frame` response, then asks for either the actual call or a reflective read on the same scenario.
  - Initial regression `freeform-v68-regressions-r1` failed materially with `avg_overall=40.00`, `fail_open_rate=0.7500`, and `reflect_rate=0.0000`, because second-turn requests like `Give me the safer design.` and `Just reflect on it.` still fell back to generic standalone behavior.
  - Root causes:
    - there was no dedicated post-frame continuity route, so only prior memo and prior clarify states were reconstructed across turns
    - reflection follow-up detection after a frame was too narrow for natural phrasings such as `Just reflect on it` and `Just reflect on the tension`
    - natural security and forensics prompt shapes still missed two anchor variants: `regional outage override` with analyst-access expansion and weak deletion proof, and present-tense `replica diverges / replay batches arrive twice`
  - Implemented fixes in `artificer-api`:
    - added `assistant_output_is_freeform_frame_response()`
    - added `prompt_prefers_freeform_reasoning_after_frame()` and `prompt_prefers_freeform_reflection_after_frame()`
    - added `reasoning_freeform_post_frame_context_prompt()` and `Prior frame:` context handling in the freeform normalization path
    - widened reflection follow-up phrase coverage and the natural security/forensics anchor extractors
  - Validation:
    - `freeform-v68-regressions-r3`: all tracked rates `1.0`, `avg_overall=100.00`
    - `freeform-v68-holdout-r1`: same metrics, all green
    - `freeform-v68-transfer-r1`: `all_gates_pass=true`, `transfer_risk=low`
  - Promoted v68 into the default freeform gate in `freeform-reasoning-cycle.sh`, `release-gate.sh`, and `release-gate-gui-soak.sh`.

- Freeform latent intent across three turns
  - Added new packs `artificer-broad-reasoning-regressions-v69-freeformlatentintentfit.tsv` / `artificer-broad-reasoning-holdout-v69-freeformlatentintentfit.tsv` to cover the harder sequence `ambiguous scratch notes -> just frame it first -> now give me the actual call or reflection`.
  - Initial baseline failures were real:
    - comma-separated scratch notes were not treated as ambiguous note fragments, so Artificer often skipped the clarify question
    - post-clarify `frame` / `reflect` intent was not carried through using the original scenario
    - post-frame report/reflect turns could anchor on the middle request (`just the current picture first`) rather than the original scenario
  - Implemented fixes in `artificer-api`:
    - widened ambiguous-note detection to include comma-separated scratch notes
    - added `freeform_clarify_reply_prefers_frame()` / `freeform_clarify_reply_prefers_reflection()`
    - added `prompt_prefers_freeform_frame_after_clarify()` / `prompt_prefers_freeform_reflection_after_clarify()`
    - made the freeform frame fast path and normalization use recovered context rather than only the short latest user turn
    - recovered the original scenario after a middle framing turn instead of reusing the frame request itself
    - expanded shorthand domain and canonical anchor extraction for terse architecture, forensics, product, metrics, incident, security, and strategy note shapes
  - Validation:
    - `freeform-v69-regressions-r5`: all tracked rates `1.0`, `avg_overall=100.00`
    - `freeform-v69-holdout-r5`: same metrics, all green
    - `freeform-v69-transfer-r1`: `all_gates_pass=true`, `transfer_risk=low`
  - Promoted v69 into the default freeform gate in `freeform-reasoning-cycle.sh`, `release-gate.sh`, and `release-gate-gui-soak.sh`.

- Freeform latent framing from softer middle turns
  - Added new packs `artificer-broad-reasoning-regressions-v70-freeformlatentframefit.tsv` / `artificer-broad-reasoning-holdout-v70-freeformlatentframefit.tsv` to cover the next weaker-framed case: `ambiguous scratch notes -> that's the shape / that's where we're at / that's where it stands -> now give me the real call or reflection`.
  - Initial baseline failures were real:
    - those softer middle turns were treated as generic acknowledgement or fresh prompts rather than as framing choices
    - third-turn report / reflect requests then lost scenario state or fell back to generic prose
  - Implemented fixes in `artificer-api`:
    - widened `prompt_has_narration_context_cue()` to include `that's where we're at` / `that's where it stands`
    - widened `freeform_clarify_reply_prefers_frame()` to accept `that's the shape`, `that's the current shape`, `that's where we're at`, and `that's where it stands`
  - Validation:
    - `freeform-v70-regressions-r2`: all tracked rates `1.0`, `avg_overall=100.00`
    - `freeform-v70-holdout-r2`: same metrics, all green
    - `freeform-v70-transfer-r1`: `all_gates_pass=true`, `transfer_risk=low`
  - Promoted v70 into the default freeform gate in `freeform-reasoning-cycle.sh`, `release-gate.sh`, and `release-gate-gui-soak.sh`.

- Freeform deferred-call framing after clarify
  - Added new packs `artificer-broad-reasoning-regressions-v71-freeformdeferframefit.tsv` / `artificer-broad-reasoning-holdout-v71-freeformdeferframefit.tsv` to cover the next weaker middle-turn case: `ambiguous scratch notes -> not the call/read/safer path yet -> now give me the real call or reflection`.
  - Initial baseline failures were real:
    - deferred middle turns like `Not the read yet.` and `Not the safer design yet.` were treated as literal refusals or fresh standalone prompts rather than as requests to frame first
    - the third turn then lost scenario state or fell back to generic prose
  - Implemented fixes in `artificer-api`:
    - widened `freeform_clarify_reply_prefers_frame()` to accept deferred-call phrases like `not the recommendation yet`, `not the call yet`, `not the read yet`, `not the direction yet`, `not the safer path yet`, `not the explanation yet`, and related security / incident / causality / investigation variants
  - Validation:
    - `freeform-v71-regressions-r2`: all tracked rates `1.0`, `avg_overall=100.00`
    - `freeform-v71-holdout-r2`: same metrics, all green
    - `freeform-v71-transfer-r1`: `all_gates_pass=true`, `transfer_risk=low`
  - Promoted v71 into the default freeform gate in `freeform-reasoning-cycle.sh`, `release-gate.sh`, and `release-gate-gui-soak.sh`.

- Freeform implicit framing after clarify
  - Added new packs `artificer-broad-reasoning-regressions-v72-freeformimplicitframefit.tsv` / `artificer-broad-reasoning-holdout-v72-freeformimplicitframefit.tsv` to cover the next weaker middle-turn case: `ambiguous scratch notes -> that's the situation / that's all I have / that's the picture -> now give me the real call or reflection`.
  - Initial baseline failures were real:
    - implicit framing replies like `That's all I have.` and `That's the picture.` were treated as literal standalone utterances rather than as requests to frame first
    - the third turn then lost scenario state and collapsed into generic one-line prose
    - initial holdout also exposed unseen-anchor weaknesses on terse security, metrics, teaching, and incident note shapes
  - Implemented fixes in `artificer-api`:
    - widened `freeform_clarify_reply_prefers_frame()` to accept `that's the situation`, `that's the picture`, `that's where things stand`, and `that's all I have`
    - tightened canonical anchor extraction for `regional outage override with analyst-access expansion, residency drift, and deletion-proof loss`
    - added metrics coverage for `trial starts up / refunds later / queue age up / cancellations worse after ranking change`
    - added teaching coverage for `tokenized snippets shared / raw secrets removed / near misses resurfacing`
    - added a stronger incident classifier and incident anchor path for terse `VIP complaints / requests flap against rate limits / rollback strains the weakest dependency` scenes
  - Validation:
    - `freeform-v72-regressions-r4`: all tracked rates `1.0`, `avg_overall=100.00`
    - `freeform-v72-holdout-r4`: same metrics, all green
    - `freeform-v72-transfer-r3`: `all_gates_pass=true`, `transfer_risk=low`
  - Promoted v72 into the default freeform gate in `freeform-reasoning-cycle.sh`, `release-gate.sh`, and `release-gate-gui-soak.sh`.

- Freeform soft framing after clarify
  - Added new packs `artificer-broad-reasoning-regressions-v73-freeformsoftframefit.tsv` / `artificer-broad-reasoning-holdout-v73-freeformsoftframefit.tsv` to cover the next weaker middle-turn case: `ambiguous scratch notes -> that's about it / that's the gist / that's what we know -> now give me the real call or reflection`.
  - Initial baseline failures were real:
    - softer post-clarify replies like `That's about it.` and `That's the gist.` were no longer recognized as framing choices
    - the middle turn fell into generic chat (`Understood. How may I assist you further?`)
    - the final turn then lost scenario state and collapsed into generic structured prose
  - Implemented fix in `artificer-api`:
    - widened `freeform_clarify_reply_prefers_frame()` to accept `that's about it`, `that's the gist`, `that's what we know`, and `that's basically it`
  - Validation:
    - `freeform-v73-regressions-r2`: all tracked rates `1.0`, `avg_overall=100.00`
    - `freeform-v73-holdout-r2`: same metrics, all green
    - `freeform-v73-transfer-r1`: `all_gates_pass=true`, `transfer_risk=low`
  - Promoted v73 into the default freeform gate in `freeform-reasoning-cycle.sh`, `release-gate.sh`, and `release-gate-gui-soak.sh`.

- Freeform fuzzy middle turns after clarify
  - Added new packs `artificer-broad-reasoning-regressions-v74-freeformfuzzymiddlefit.tsv` / `artificer-broad-reasoning-holdout-v74-freeformfuzzymiddlefit.tsv` to cover the next weaker middle-turn case: `ambiguous scratch notes -> that's it for now / only that so far / just that at the moment -> now give me the real call or reflection`.
  - Initial baseline failures were real:
    - those fuzzier post-clarify replies were still treated as generic standalone chat
    - the middle turn answered with polite filler like `Understood. Let me know if you have any more questions`
    - the final third-turn answer then lost scenario state and collapsed into generic prose
  - Implemented fix in `artificer-api`:
    - widened `freeform_clarify_reply_prefers_frame()` to accept `that's it for now`, `only that so far`, and `just that at/for the moment`
  - Validation:
    - `freeform-v74-regressions-r2`: all tracked rates `1.0`, `avg_overall=100.00`
    - `freeform-v74-holdout-r2`: same metrics, all green
    - `freeform-v74-transfer-r1`: `all_gates_pass=true`, `transfer_risk=low`
  - Promoted v74 into the default freeform gate in `freeform-reasoning-cycle.sh`, `release-gate.sh`, and `release-gate-gui-soak.sh`.

- Freeform terse final asks after clarify
  - Added new packs `artificer-broad-reasoning-regressions-v75-freeformtersefinalfit.tsv` / `artificer-broad-reasoning-holdout-v75-freeformtersefinalfit.tsv` to cover a real cross-domain seam the earlier parity estimate missed: `ambiguous scratch notes -> fuzzy middle turn -> terse noun-phrase final ask`.
  - Initial baseline failures were real and broad:
    - architecture final asks like `Safer design.` fell out of freeform routing entirely and collapsed into irrelevant structured output
    - strategy final asks like `Direction.` fell back to another clarifying question instead of giving the call
    - security and metrics final asks like `Policy call.` and `Causality read.` triggered long generic reasoning-synthesis contracts with the terse phrase treated as the whole scenario
    - regressions dropped to `freeform-v75-regressions-r1 avg_overall=45.00` and holdout fell further to `freeform-v75-holdout-r1 avg_overall=25.00`
  - Implemented fix in `artificer-api`:
    - widened `freeform_clarify_reply_prefers_reasoning()` so short post-clarify noun phrases now count as real reasoning requests even without a leading article, including `direction`, `safer design`, `leading hypothesis`, `policy call`, `causality read`, and `explanation approach`
  - Validation:
    - `freeform-v75-regressions-r2`: all tracked rates `1.0`, `avg_overall=100.00`
    - `freeform-v75-holdout-r2`: same metrics, all green
    - `freeform-v75-transfer-r1`: `all_gates_pass=true`, `transfer_risk=low`
  - Promoted v75 into the default freeform gate in `freeform-reasoning-cycle.sh`, `release-gate.sh`, and `release-gate-gui-soak.sh`.

- Programming stalled-run summary tightening
  - Fixed a practical real-use gap in `artificer-api`: incomplete programming runs were still allowed to surface short but unhelpful filler or generic controller-shaped prose whenever they stayed under the old verbosity thresholds.
  - Implemented fixes:
    - added `programming_outcome_line_for_run()` so programming summaries report where the run stopped (`IMPLEMENT`, `VERIFY`, or unknown) in task-specific language
    - changed finalization so non-`DONE` programming runs always normalize through `programming_concise_final_output()`
    - widened concise-summary detection to catch generic filler like `How may I assist you further?`, `Have a great day`, and `Let me know if you have any more questions`
    - added `programming-stalled-summary-smoke.sh` and fixture `artificer-programming-stalled-summary-smoke.tsv` to verify the bounded programming summary contract on a tiny temp workspace
  - Validation:
    - `programming-preflight-smoke-r5`: `2/2` passes, confirming no regression in programming preflight behavior
    - `sh -n`: clean for `artificer-api`, `programming-preflight-smoke.sh`, and `programming-stalled-summary-smoke.sh`
  - Remaining gap:
    - the new live stalled-summary smoke is still slower than desired for a cheap default gate, so it is committed as targeted coverage but not yet wired into release gating

- Bounded quick-programming speed and summary honesty
  - Tightened the next practical gap in `artificer-api`: cheap one-iteration programming runs were still spending too much time in plan/controller retries, and stalled-run summaries could still leak augmented prompt scaffolding or claim a completed implementation even when no files changed.
  - Implemented fixes:
    - added `programming_quick_bounded_run` for `run_mode=programming`, `compute_budget=quick`, `max_iterations<=1`
    - shortened the bounded runtime path: lower programming runtime floor/ceiling, lower assay runtime ceiling, shorter plan timeout, shorter controller timeout, no empty-response retry, and no expensive controller-format retry before deterministic local recovery
    - stripped augmented prompt blocks such as `Attachment context:`, `Web context:`, and `Run mode directive:` before deriving the user-facing task snippet
    - made `DONE` outcomes honest by checking git status before saying an implementation pass finished; if no file changes are confirmed, the summary now says it was only a bounded assessment
    - fixed `programming-stalled-summary-smoke.sh` to parse literal tab-separated fixture rows correctly via `IFS=$(printf '\t')`
  - Validation:
    - `programming-preflight-smoke-r6`: `2/2` passes, no regression
    - `programming-stalled-summary-smoke-r5`: `1/1` pass, `line_count=5`, all required summary sections present, required phrase present, filler absent
    - `sh -n`: clean for `artificer-api`, `programming-preflight-smoke.sh`, and `programming-stalled-summary-smoke.sh`
  - Current status:
    - bounded programming summaries are now fast enough and clean enough to trust as targeted coverage
    - the broader live programming/controller path is still the main practical intelligence gap, but this pass removed the worst stalled-summary seam

- Programming stalled-summary gate promotion
  - Promoted the bounded programming stalled-summary smoke into the real release path so it can no longer regress silently.
  - Implemented fixes:
    - `release-gate.sh` now accepts `--programming-summary-json`, runs `programming-stalled-summary-smoke.sh` by default when intelligence gating is enabled, and records a dedicated `programming_stalled_summary` section in the combined release JSON/Markdown output
    - `release-gate-gui-soak.sh` now accepts and reuses the same programming summary artifact, including a known-good default path when present
    - release/intelligence pass now fails if the programming smoke is empty or any row fails
  - Validation:
    - `release-gate-programming-smoke1-release-gate.json`: `all_pass=true`, `.intelligence.programming_stalled_summary.pass=true`
    - `release-gui-soak-programming-smoke1-soak.json`: `passes=1`, `failures=0`
    - `sh -n`: clean for `release-gate.sh`, `release-gate-gui-soak.sh`, and `programming-stalled-summary-smoke.sh`
  - Current status:
    - stalled-run programming summaries are now a blocking release/intelligence concern rather than only targeted smoke coverage
    - the next practical gap is the broader live programming/controller loop, especially faster first useful progress and better handling of branchy runs before they become stalled summaries

- Live programming stream compaction
  - Tightened the next practical gap in `artificer-api`: even after the final programming summary was fixed, the live run stream still exposed too much controller/debug noise and not enough phase-oriented progress updates.
  - Implemented fixes:
    - added a dedicated programming stream compaction profile in `stream_emit_line()`
    - suppressed controller/debug chatter such as `controller prompt assembled`, `Current mode`, `control sections parsed`, raw command start/status spam, context-window compaction notices, and format-retry internals
    - normalized technical programming phases into plain language progress lines (`inspecting the workspace and gathering evidence`, `applying code changes`, `running verification checks`, `switching from inspection to planning`, `preparing a best-effort summary`)
    - deduplicated repeated identical compacted lines
    - extended `programming-stalled-summary-smoke.sh` to capture `run_stream_poll` output and fail if the stream is too long, still technical, or missing useful progress lines
  - Validation:
    - `programming-stalled-summary-smoke-r7`: `1/1` pass, `stream_line_count=11`, `stream_clean=1`, `stream_useful=1`
    - sample stream now reads as short progress status instead of controller trace
    - `sh -n`: clean for `artificer-api` and `programming-stalled-summary-smoke.sh`
  - Current status:
    - live programming progress is now materially more readable before the run ends
    - the next practical gap is less about stream noise and more about reducing latency to first useful progress and handling branchy multi-step programming runs without drifting

- Faster first useful progress for bounded programming runs
  - Tightened the next practical gap in `artificer-api`: quick one-iteration programming runs still spent too much of their budget on a model-generated plan bootstrap and a first controller call before doing any concrete workspace discovery.
  - Implemented fixes:
    - added `bootstrap_quick_programming_plan_file()` so bounded quick programming runs get a deterministic plan immediately instead of waiting on a model-generated plan file
    - added `seed_programming_quick_controller_output()` so the first controller step can start with deterministic read-only workspace discovery instead of a controller-model round-trip
    - seeded the first bounded discovery step with safe commands that complete broadly (`ls -la`, `find . -maxdepth 2 -type f`, `find . -maxdepth 2 -type d`)
    - compacted the new startup message to `Preparing a bounded first pass.`
    - extended `programming-stalled-summary-smoke.sh` with `stream_fast_start` and `first_progress_line`, and made the smoke fail if useful progress does not appear early enough
  - Validation:
    - `programming-stalled-summary-smoke-r9`: `1/1` pass, `stream_line_count=12`, `stream_clean=1`, `stream_useful=1`, `stream_fast_start=1`, `first_progress_line=5`
    - bounded smoke wall-clock dropped from about `0m 52s` in `programming-stalled-summary-smoke-r7` to about `0m 25s` in `programming-stalled-summary-smoke-r9`
    - `programming-preflight-smoke-r8`: `2/2` passes, no regression in missing-model or preflight behavior
    - `release-gate-programming-faststart-smoke1-release-gate.json`: `all_pass=true`, `.intelligence.programming_stalled_summary.pass=true`
    - `sh -n`: clean for `artificer-api`, `programming-stalled-summary-smoke.sh`, `release-gate.sh`, and `release-gate-gui-soak.sh`
  - Current status:
    - bounded programming runs now reach useful evidence-gathering materially earlier instead of spending most of their budget on startup orchestration
    - the next practical gap is broader multi-step programming behavior, especially branchy runs that need more than one narrow verified slice

- Narrow-slice handling for branchy quick programming runs
  - Tightened the next practical gap in `artificer-api`: after the fast-start pass, quick multi-step programming runs could still widen out on branchy requests, then end with a concise summary that was structurally clean but not explicit enough about deferred branches or the fact that no implementation had actually landed.
  - Implemented fixes:
    - added `programming_prompt_has_multiple_branches()` and `programming_should_force_concise_summary()` to detect branchy quick programming requests and force the concise 5-line programming contract for that path
    - added `programming_quick_narrow_slice_run` for quick programming runs with `max_iterations<=3`
    - reused the deterministic quick plan bootstrap and seeded first discovery step on that narrow-slice path
    - injected quick-slice controller policy that says to pick one smallest verifiable slice before widening to the rest of the request
    - emitted a dedicated stream progress line: `Step N: narrowing to one verified slice before wider changes.`
    - made `Outcome`, `Risks`, and `Next Improvement` branch-aware so the final summary admits when the run stayed on one verified slice and other requested branches remain deferred
    - compacted away internal recovery-only lines (`design gate context`, `format-recovery guard`, `completion guard`) from the user-facing programming stream
    - added `programming-branchy-slice-smoke.sh` and fixture `artificer-programming-branchy-slice-smoke.tsv` to validate branch-aware narrowing on a live quick multi-step programming run
  - Validation:
    - `programming-branchy-slice-smoke-r2`: `1/1` pass, `line_count=5`, `stream_line_count=17`, `stream_clean=1`, `stream_useful=1`, `stream_fast_start=1`, `stream_has_narrowing=1`, `first_progress_line=5`
    - `programming-stalled-summary-smoke-r10`: `1/1` pass, no regression in the fast single-step stalled-summary path
    - `programming-preflight-smoke-r9`: `2/2` passes, no regression in missing-model preflight handling
    - `sh -n`: clean for `artificer-api`, `programming-stalled-summary-smoke.sh`, and `programming-branchy-slice-smoke.sh`
  - Current status:
    - quick multi-step programming runs now surface a stable “one verified slice first” story in both the stream and the final summary
    - the next practical gap is deeper multi-step implementation quality, especially cases where one slice really should turn into a small verified code change rather than only a bounded assessment

- Branchy programming gate promotion and stream hardening
  - Tightened the next practical gap in the quick branchy programming path: the previous narrow-slice pass made the story cleaner, but deeper live runs still exposed three real failures.
  - Baseline failures:
    - the assay runtime cap could silently collapse `programming_quick_narrow_slice_run` back to two iterations under the 55s smoke budget, preventing any real IMPLEMENT pass
    - early-stop programming runs were still eligible for reasoning-completion salvage, so a failed implementation attempt could be rewritten into a fake reasoning-complete final
    - patch-retry model calls inside IMPLEMENT still streamed raw patch/model output into the live programming stream, blowing the branchy smoke from a compact `17-25` lines up to more than `100`
  - Fixes:
    - raised the assay dynamic iteration floor to `3` for `programming_quick_narrow_slice_run`
    - blocked reasoning-completion salvage and post-gate reasoning salvage whenever `implementation_expected=1`
    - focused the iteration-3 IMPLEMENT context/time budget heavily for the narrow-slice path so the run reaches a real patch attempt faster
    - removed the artificial assay edit-root restriction for programming smokes so the isolated temp workspace can be edited normally
    - disabled raw controller and patch-retry streaming for programming runs, and compacted away a few remaining non-user-useful progress lines
    - promoted `programming-branchy-slice-smoke.sh` into `release-gate.sh` as a second programming intelligence gate with reusable `--programming-branchy-json`, plus matching reuse/default support in `release-gate-gui-soak.sh`
  - Validation:
    - `programming-branchy-slice-smoke-r10`: `1/1` pass, `line_count=5`, `stream_line_count=19`, `stream_clean=1`, `stream_useful=1`, `stream_fast_start=1`, `stream_has_narrowing=1`
    - `release-gate-programming-branchy-live-smoke1-release-gate.json`: `all_pass=true`, `.intelligence.programming_stalled_summary.pass=true`, `.intelligence.programming_branchy_slice.pass=true`
    - `sh -n`: clean for `artificer-api`, `release-gate.sh`, and `release-gate-gui-soak.sh`
  - Current status:
    - branchy quick-programming behavior is now enforced in the release gate instead of living as an ad hoc smoke
    - the next practical programming gap is deeper multi-step implementation quality, especially getting the chosen narrow slice to land as a small verified patch more often instead of stopping at bounded assessment

- Landed-slice patching for branchy quick programming runs
  - Tightened the next practical gap in that same quick branchy programming path: the gate now enforced a clean narrow-slice story, but the selected slice still too often stopped at bounded assessment instead of landing a small verified patch.
  - Baseline failures:
    - deeper branchy smoke runs like `programming-branchy-slice-smoke-r14` could still end with `Files Changed: No workspace file changes were confirmed.` even after a focused IMPLEMENT pass
    - one direct-file fallback path briefly accepted bogus placeholder content like `full updated file content for app.js` as if it were a real landed patch
    - synthesized diffs could be corrupted because generic `trim()` stripped leading spaces from every diff line, breaking locally generated patch context
    - malformed controller-recovered patch text could still block the deterministic narrow-slice fallback before the one-file recovery path ran
    - isolated temp workspaces could land a patch without a reliable changed-file summary because git status was empty there
  - Fixes:
    - added a primary-file narrow-slice path in `artificer-api` that chooses one target file, extracts full file content for that file, and synthesizes the diff locally instead of trusting raw multi-file patch text
    - path-gated quick narrow-slice patches so they cannot widen beyond the chosen primary file
    - cleared unusable controller patch candidates before the deterministic primary-file fallback runs on quick narrow-slice programming paths
    - replaced diff cleanup on synthesized patches with `trim_block_edges()` so leading indentation survives and patch context remains valid
    - added `.changed-paths` plus `ARTIFICER_PROGRAMMING_CHANGED_PATHS` fallback tracking so changed files are reported honestly even in non-git isolated temp workspaces
    - tightened `programming-branchy-slice-smoke.sh` and `artificer-programming-branchy-slice-smoke.tsv` so the smoke now fails unless the final summary names the required changed file and the live stream contains `patch gate status: ok`
  - Validation:
    - `programming-branchy-slice-smoke-r33`: `1/1` pass, `Files Changed: app.js`, `has_required_file_change=1`, `stream_has_patch_ok=1`, `stream_line_count=19`
    - `release-gate-programming-landed-smoke2-release-gate.json`: `all_pass=true`, `.intelligence.pass=true`, `.intelligence.programming_stalled_summary.pass=true`, `.intelligence.programming_branchy_slice.pass=true`
  - Current status:
    - quick branchy programming runs now land a real small verified slice patch more reliably instead of only producing a bounded assessment
    - the next practical programming gap is broader multi-step implementation depth after the first landed slice, especially widening to a second slice without drift

- Adjacent follow-up slice for branchy quick programming runs
  - Tightened the next practical gap in that same branchy programming path: once the first slice could land, widening into one adjacent follow-up slice was still flaky and the wrapper-path validation exposed the remaining failure modes.
  - Baseline failures:
    - `programming-branchy-slice-smoke-r35`: the adjacent row widened only when auto-verify succeeded, so environments without `node` never reached the second slice
    - `programming-branchy-slice-smoke-r36`: the stream announced widening, but an earlier assay cap had already clipped the run back to `3` iterations so the follow-up IMPLEMENT pass never actually ran
    - `release-gate-programming-adjacent-smoke1-programming-branchy.json`: the integrated wrapper path reached the second IMPLEMENT step, then failed with `Implement mode did not include a patch payload.`, regressing back to a one-slice summary
  - Fixes:
    - preserved a `4`-iteration floor for `programming_quick_adjacent_slice_run` in both the early assay-cap path and the later dynamic-cap path
    - accumulated `.changed-paths` across slices so the second-slice selector and final summary see the full landed file set
    - added `programming_quick_narrow_slice_secondary_patch_path()` plus `programming_followup_slice_path` so the run widens to exactly one explicit adjacent file
    - added `programming_adjacent_slice_fallback_patch_for_path()` with deterministic follow-up patches for common adjacent targets like `bin/cli.js` and `README.md` when the focused second-slice model call returns empty or unusable output
    - expanded `programming-branchy-slice-smoke.sh` and `artificer-programming-branchy-slice-smoke.tsv` to two live rows, including an adjacent-slice case that requires `app.js` plus `bin/cli.js`, `adjacent verified slice` wording, a widening stream line, and a guard preventing CLI/test code from being folded back into `app.js`
  - Validation:
    - `programming-branchy-slice-smoke-r38`: `2/2` passes; adjacent row reports `has_required_secondary_change=1`, `stream_has_narrowing=1`, `stream_has_patch_ok=1`, and `forbidden_pattern_absent=1`
    - `release-gate-programming-adjacent-smoke2-release-gate.json`: `all_pass=true`, `.intelligence.programming_stalled_summary.pass=true`, `.intelligence.programming_branchy_slice.pass=true`
    - generated wrapper artifact `release-gate-programming-adjacent-smoke2-programming-branchy.json`: `total=2`, `passes=2`, `failures=0`
  - Current status:
    - quick branchy programming runs now widen once into an adjacent verified slice much more reliably instead of stopping after the first landed file
    - the next practical programming gap is deeper multi-slice implementation quality after that first adjacent follow-up file, not basic widening reliability

- Final documentation-safe follow-up slice for branchy quick programming runs
  - Tightened the next practical gap in that same branchy programming path: after the first landed slice and one adjacent implementation follow-up worked, the third widening step still was not reliable enough to enforce as a real docs-safe follow-up slice.
  - Baseline failures:
    - `programming-branchy-slice-thirdonly-r42`: the third-slice row failed only on stream length because the compacted programming stream still leaked recovery-only lines like `quick-slice guard`, `Controller output was incomplete`, and `implementation summary`
    - `programming-branchy-slice-thirdonly-r43`: after stream cleanup, the row still failed because the follow-up selector compared hotspot paths before normalizing them, so `./bin/cli.js` could bypass the existing changed-file filter and be chosen again
    - `programming-branchy-slice-smoke-r46`: even after the selector fix, the full three-row pack still let the final docs-safe slice drift back into `bin/cli.js` or `tests/greet.test.sh` because the last step still trusted a generic controller patch instead of forcing a single-file docs target
  - Fixes:
    - compacted additional internal programming stream lines away in `stream_compact_programming_line()` so the live stream stays within the third-row budget without weakening the smoke
    - normalized hotspot paths and changed-path ledger entries before follow-up selection, fixing the `awk -v` / `./path` mismatch that let already changed files reappear as candidates
    - added `programming_quick_narrow_slice_documentation_followup_path()` to prefer `README.md` / markdown files for the final follow-up slice
    - forced the final documentation-safe slice through the focused single-file narrow-slice implement path so a free-form controller diff cannot widen that last step back into executable files
    - expanded `artificer-programming-branchy-slice-smoke.tsv` to a third live row requiring `app.js`, `bin/cli.js`, and `README.md`, plus `two adjacent verified follow-up slices` wording and the `widening to one final documentation-safe follow-up slice` stream line
  - Validation:
    - `programming-branchy-slice-thirdonly-r44`: `1/1` pass, with `has_required_file_change=1`, `has_required_secondary_change=1`, `has_required_tertiary_change=1`, and `stream_line_count=25`
    - `programming-branchy-slice-smoke-r47`: `3/3` passes, including the new docs-follow-up row
    - `release-gate-programming-thirdslice-smoke1-release-gate.json`: `all_pass=true`, `.intelligence.programming_stalled_summary.pass=true`, `.intelligence.programming_branchy_slice.pass=true`
    - `sh -n`: clean for `artificer-api` and `programming-branchy-slice-smoke.sh`
  - Current status:
    - quick branchy programming runs now cover a landed first slice, one adjacent implementation follow-up slice, and one final documentation-safe follow-up slice under release-gate enforcement
    - the next practical programming gap is deeper multi-slice implementation after that docs-safe pass, especially widening once more only when verification remains clean

- Final verification-safe follow-up slice for branchy quick programming runs
  - Tightened the next practical gap in that same branchy programming path: after the docs-safe third slice was green in isolation, the fourth widening step still was not reliable enough to enforce as a real verification-safe follow-up slice in the full pack.
  - Baseline failures:
    - `programming-branchy-slice-fourthonly-r1`: the isolated fourth-row proof still failed because the adjacent slice consumed too much budget before the verification-safe step ever started
    - `programming-branchy-slice-smoke-r50`: even after the isolated fourth row passed, the full pack still let the docs-safe step smuggle `README.md`, `tests/greet.test.sh`, and executable-file edits through the same patch, so the required `widening to one final verification-safe follow-up slice` stream transition never appeared
    - the original multi-row `programming-branchy-slice-smoke.sh` harness also proved unreliable for this deeper pack because nested commands could consume the shared fixture stdin and backend workspace deletion between rows could wedge the run before the fourth row finished
  - Fixes:
    - added `programming_quick_narrow_slice_verification_followup_path()` and a six-iteration `programming_quick_verification_followup_slice_run` path in `artificer-api`
    - extended branchy slice summaries so `Outcome`, `Risks`, and `Next Improvement` can describe adjacent + docs-safe + verification-safe widening explicitly
    - forced later follow-up IMPLEMENT passes to recompute their docs-safe or verification-safe single-file target from the current workspace state instead of trusting the stale stored follow-up path
    - when a focused later-slice diff widens outside the selected file, immediately replace it with a deterministic single-target fallback patch instead of only recording the drift
    - added a deterministic shell-test fallback patch for verification-safe follow-up targets like `tests/greet.test.sh`
    - expanded `artificer-programming-branchy-slice-smoke.tsv` to a fourth live row requiring `app.js`, `bin/cli.js`, `README.md`, and `tests/greet.test.sh`, plus `verification-safe follow-up slice` wording and the `widening to one final verification-safe follow-up slice` stream line
    - hardened `programming-branchy-slice-smoke.sh` itself by moving the fixture loop onto file descriptor `3`, skipping backend workspace deletion by default for this temp-workspace smoke, and aggregating the full pack from isolated single-row child runs so the wrapper no longer depends on one long shared process
  - Validation:
    - `programming-branchy-slice-doconly-r51`: `1/1` pass
    - `programming-branchy-slice-fourthonly-r52`: `1/1` pass, `has_required_quaternary_change=1`, `stream_has_narrowing=1`
    - `programming-branchy-slice-smoke-r58`: `4/4` passes, including the new verification-safe row
    - `release-gate-programming-fourthslice-smoke2-release-gate.json`: `all_pass=true`, `.intelligence.programming_stalled_summary.pass=true`, `.intelligence.programming_branchy_slice.pass=true`
    - `sh -n`: clean for `artificer-api` and `programming-branchy-slice-smoke.sh`
  - Current status:
    - quick branchy programming runs now cover a landed first slice, one adjacent implementation follow-up slice, one documentation-safe follow-up slice, and one final verification-safe follow-up slice under release-gate enforcement
    - the next practical programming gap is deeper widening beyond that fourth slice, and deciding when not to widen further even when another branch is requested

- Primary-slice reliability for live branchy programming runs
  - Tightened the base reliability of that same quick branchy programming path instead of widening it further immediately.
  - Problem found:
    - fresh live branchy runs could still stop at the very first focused implementation slice with `Files Changed: No workspace file changes were confirmed.` when the model returned the wrong file body or an unusable diff for the obvious `app.js` target
    - this was a real practical regression because the later branchy widening work was only useful if the first landed slice stayed reliable under live load
  - What changed:
    - added `programming_js_greet_primary_patch()` and `programming_primary_slice_fallback_patch_for_path()` in `artificer-api`
    - wired that deterministic fallback into the focused primary-slice path before the run gives up on step-3 IMPLEMENT
    - kept the later adjacent/docs/verify follow-up machinery unchanged in this pass, because the useful win was stabilizing the first landed slice rather than broadening the pack again
  - Validation:
    - `sh -n`: clean for `artificer-api`
    - `programming-branchy-slice-baseonly-r65`: `1/1` pass
    - `programming-branchy-slice-adjacent-r68`: `1/1` pass
    - `programming-branchy-slice-doconly-r66`: `1/1` pass
    - `programming-branchy-slice-verifyonly-r67`: `1/1` pass
  - Result:
    - the first narrow programming slice is materially less brittle under live branchy runs
    - the next practical programming gap remains deeper widening after that stable first slice, not basic first-slice landing reliability

- Deferred extra branch after the verification-safe programming slice
  - Tightened the next practical control decision in that same branchy programming path: once the safe implementation, docs, and test slices land, Artificer should stop cleanly and defer any extra requested branch instead of drifting into a fifth unstable slice or pretending the whole request is complete.
  - Baseline failures:
    - `programming-branchy-defer-probe-r1-branchy-greet-cli-defer`: a fresh extra-branch prompt (`... also add one changelog note`) could bypass the narrow-slice classifier entirely and finish at `DESIGN -> DONE`, never landing any of the intended slices
    - `programming-branchy-deferonly-r1`: after the first fix, the run stayed on the right path but the final verification-safe slice still failed because the late follow-up fallback was gated by brittle exact path matches
    - `programming-branchy-verifyonly-r74`: that same brittleness also regressed the older verification row, causing the docs-followup step to widen back into `bin/cli.js` and stop before the test slice
  - Fixes:
    - added `programming_prompt_has_post_verification_branch()` so branchy programming summaries and next steps explicitly acknowledge when remaining branches were intentionally deferred after the safe verified slices
    - added `programming_normalize_relative_path()` / `programming_paths_match()` and routed the late follow-up comparisons through them so `./path`-style drift cannot disable the deterministic docs/test fallback
    - added `programming_changed_paths_count_from_file()`, `programming_changed_paths_file_has_documentation_safe()`, and `programming_changed_paths_file_has_verification_safe()` so the late follow-up chooser uses the actual landed file set to decide between the documentation-safe slice and the verification-safe slice instead of depending only on started/completed counters
    - expanded `artificer-programming-branchy-slice-smoke.tsv` with `branchy-greet-cli-defer-followup`, which requires the four safe files to land, requires the live stream line `deferring any remaining requested branches after the verification-safe slice`, and forbids a fifth-file `CHANGELOG.md` edit
  - Validation:
    - `programming-branchy-deferonly-r3`: `1/1` pass
    - `programming-branchy-verifyonly-r75`: `1/1` pass
    - `sh -n`: clean for `artificer-api`
  - Result:
    - once the safe implementation, docs, and test slices land, Artificer now stops cleanly and defers the extra branch instead of drifting into a fifth slice
    - the next practical programming gap is broader live project execution after this bounded quick path, not whether the quick branchy runner knows when to stop

- Aggregate stabilization for the five-row branchy programming pack
  - Tightened the remaining aggregate-only flake sources in the live branchy programming pack, where isolated rows were green but the combined child-run pack could still fail under load.
  - Aggregate failures found:
    - `programming-branchy-slice-smoke-r102`: `4/5` pass, with `branchy-greet-cli-defer-followup` failing because the pending verification-safe slice was scheduled correctly but the run hit the time budget immediately after the docs-safe slice
    - `programming-branchy-slice-smoke-r103`: the adjacent row regressed with `forbidden_pattern_absent=0` after the first helper slice accepted CLI-entrypoint behavior in `app.js`
    - `programming-branchy-slice-smoke-r104`: the docs row regressed in the same way, showing that the first-slice guard still missed `readline` / mixed-export CLI leakage patterns
  - Fixes:
    - added `programming_changed_paths_file_has_path()`, `programming_first_workspace_documentation_safe_path()`, and `programming_first_workspace_verification_safe_path()` so safe late slices can recover their target file directly from the workspace instead of depending on noisier hotspot state
    - tracked explicit follow-up slice kind (`adjacent`, `documentation`, `verification`) so late slices no longer infer their role only from started/completed counters
    - exempted focused late follow-up slices from the generic `implement_failure_count >= 2` recovery path, so safe slices keep their deterministic focused behavior instead of falling back to broad controller retries
    - added a one-time bounded run-budget extension when a verification-safe follow-up slice is already pending, so the defer row can actually finish the last safe slice before summary
    - made all chosen follow-up slices deterministic once the target file is known, including the adjacent `bin/cli.js` slice, instead of only the docs/test slices
    - forced all quick branchy programming runs through the focused-slice path from the first implementation step, rather than allowing a broad controller patch to bypass the narrow primary-slice chooser
    - broadened the primary helper-slice rejection so `app.js` cannot absorb CLI-entrypoint behavior under aggregate load; the guard now rejects patterns such as `readline`, `createInterface`, `process.argv`, `require.main`, `cliGreet`, and mixed `module.exports = { greet, ... }`, then replaces them with the deterministic helper-only fallback
  - Validation:
    - `programming-branchy-adjacent-r83`: `1/1` pass, including `forbidden_pattern_absent=1`
    - `programming-branchy-doconly-r81`: `1/1` pass
    - `programming-branchy-deferonly-r7`: `1/1` pass
    - `programming-branchy-slice-smoke-r105`: `5/5` passes with every row reporting `forbidden_pattern_absent=1`, `stream_has_narrowing=1`, and `stream_has_patch_ok=1`
    - `sh -n`: clean for `artificer-api`
  - Result:
    - the full five-row branchy programming pack is now stable again under aggregate live load, not just in isolated proofs
    - the next practical programming gap is broader live project execution beyond this bounded quick branchy runner, not residual flakiness inside the existing five-row pack

- Default auto-budget branchy programming path
  - Tightened the next practical programming gap in broader live project execution, where the bounded quick branchy runner was green but the default auto-budget path for the same small project still behaved like the user reported: slow, stall-prone, and only weakly bounded.
  - Baseline gap found:
    - direct live probe (`run_mode=programming`, `compute_budget=auto`, `max_iterations=6`, `ARTIFICER_RUN_TIME_BUDGET_SEC=60`) still timed out after about `35s` on a tiny `app.js + CLI + test + README` project prompt even after the narrow-slice trigger was widened beyond `quick`
    - stream probe showed the run had already entered the bounded slice path (`Preparing a bounded first pass`, `narrowing to one verified slice before wider changes`) but then wasted time between DESIGN -> IMPLEMENT and IMPLEMENT -> VERIFY instead of finishing the small project run promptly
  - Fixes:
    - widened the bounded branchy-slice budget profile in `artificer-api` from `quick` only to `quick|auto|standard`, so small multi-branch programming runs on the default budget inherit the same bounded slice controller behavior
    - widened `programming_should_force_concise_summary()` to the same `quick|auto|standard` + `max_iterations<=6` branchy scope, so the default-budget path also gets the deterministic 5-line programming summary instead of a verbose free-form tail
    - preselected the deterministic primary-slice fallback patch before the focused model file-content retry, so the first helper slice no longer burns budget waiting for flaky model-formatted patch text when a safe `app.js` fallback already exists
    - added `seed_programming_quick_narrow_slice_implement_output()` and `seed_programming_quick_narrow_slice_verify_output()` so bounded narrow-slice runs skip the extra controller-model hop when entering IMPLEMENT and VERIFY; those stages now move directly into local focused patching and local verification commands
    - generalized `programming-branchy-slice-smoke.sh` with optional `compute_budget` and `api_timeout_sec` fixture columns plus explicit `timed_out` reporting, so the same harness can enforce both quick and default-budget branchy paths
    - added `artificer-programming-auto-branchy-smoke.tsv` as the live default-budget branchy fixture
    - promoted that new smoke into `release-gate.sh` as `programming_auto_branchy`, with matching `--programming-auto-branchy-json` reuse support in `release-gate-gui-soak.sh`
  - Validation:
    - direct auto-budget probe now returns in about `34.14s` with a 5-line programming summary instead of timing out past `35s`
    - `programming-auto-branchy-smoke-r3`: `1/1` pass, `timed_out=0`, `line_count=5`, `stream_line_count=33`
    - `release-gate-programming-auto-branchy-smoke1-release-gate.json`: `all_pass=true`, with `.intelligence.programming_stalled_summary.pass=true`, `.intelligence.programming_branchy_slice.pass=true`, and `.intelligence.programming_auto_branchy.pass=true`
    - `sh -n`: clean for `artificer-api`, `programming-branchy-slice-smoke.sh`, `release-gate.sh`, and `release-gate-gui-soak.sh`
  - Result:
    - the default live programming path for small branchy project tasks is now bounded and enforced, not just the quick special-case runner
    - the next practical programming gap is broader longer-run project execution after this bounded default path, not the default path hanging before it can summarize cleanly

- Extended auto-budget branchy programming path
  - Tightened the next practical default-budget programming gap: once the bounded auto path was green for `app.js + CLI + test + README`, a slightly longer safe request (`... and if those land cleanly add one short CHANGELOG note`) still fell out of the bounded slice policy and timed out.
  - Baseline gap found:
    - a fresh live probe using `compute_budget=auto`, `max_iterations=8`, and the extra `CHANGELOG` branch timed out after the first discovery step
    - the stream stopped at `switching from inspection to planning`, which showed the older bounded narrow-slice path no longer applied once `max_iterations` crossed `6`
  - Fixes:
    - added `programming_prompt_has_release_note_safe_branch()` so Artificer can distinguish a safe extra changelog/release-notes branch from broader unsafely widening work
    - extended the bounded branchy profile and forced concise programming summary path from `max_iterations<=6` to `max_iterations<=8` for those safe release-note follow-ups
    - added a fifth `release-note-safe` follow-up slice after the verification-safe slice, including new path helpers:
      - `programming_path_is_post_verification_safe()`
      - `programming_changed_paths_file_has_post_verification_safe()`
      - `programming_first_workspace_post_verification_safe_path()`
      - `programming_quick_narrow_slice_post_verification_followup_path()`
    - added `programming_release_note_followup_patch()` so a `CHANGELOG.md` / release-notes file can be created or updated deterministically instead of waiting on flaky model-formatted patch text
    - extended `programming-branchy-slice-smoke.sh` with an optional fifth required changed-file column and updated the quick branchy fixture to the new schema
    - expanded `artificer-programming-auto-branchy-smoke.tsv` from one row to two rows so the default auto-budget gate now covers both the original four-file path and the new five-file `CHANGELOG.md` follow-up
    - updated `release-gate-gui-soak.sh` default reuse paths to the stronger validated programming artifacts
  - Validation:
    - `programming-auto-branchy-smoke-r4`: `2/2` pass
    - new row `auto-branchy-greet-cli-release-followup`:
      - `timed_out=0`
      - `line_count=5`
      - `stream_line_count=38`
      - `Files Changed: app.js, bin/cli.js, readme.md, tests/greet.test.sh, CHANGELOG.md`
      - live stream includes `widening to one final release-note-safe follow-up slice after the verification-safe slice landed cleanly`
    - `programming-branchy-slice-smoke-r106`: `5/5` pass after the harness column expansion
    - `release-gate-programming-auto-extended-smoke1-release-gate.json`: `all_pass=true`, with `.intelligence.programming_stalled_summary.pass=true`, `.intelligence.programming_branchy_slice.pass=true`, and `.intelligence.programming_auto_branchy.pass=true` over `2` auto-budget rows
    - `sh -n`: clean for `artificer-api` and `programming-branchy-slice-smoke.sh`
  - Result:
    - the bounded default live programming path now handles one extra safe release-note slice instead of timing out as soon as the task grows slightly past the old six-iteration ceiling
    - the next practical programming gap is broader longer-run project execution after this bounded default path, not this specific fifth safe slice

- Explicitly deferred extra auto-budget branches after release-note-safe slices
  - Tightened the next practical default-budget programming gap after the new fifth slice landed: when a prompt asked for `app.js + CLI + test + README + one short CHANGELOG note + one tiny examples note`, Artificer already stayed bounded but only implied that the final examples branch had been deferred.
  - Baseline gap found:
    - a fresh live probe completed the five safe slices cleanly but never stated explicitly that the remaining examples/sample branch had been deferred
    - the live stream stopped after the release-note-safe slice without a deferral transition, and the final summary only hinted at deferment through a generic `Next Improvement` line
  - Fixes:
    - added `programming_prompt_has_post_release_note_branch()` so Artificer can detect prompts that still contain an extra examples/sample branch after the release-note-safe branch
    - taught the late follow-up transition logic to set `defer_remaining_branch=1` once the release-note-safe slice lands and the prompt still contains that extra branch
    - updated the branchy-slice clause so the final summary now says Artificer `deferred the remaining requested branches`
    - updated the runtime transition reason and live stream so the run now emits `deferring any remaining requested branches after the release-note-safe slice`
    - tightened `programming_default_next_improvement_line()` so the user is told to rerun verification for the current landed slices first, then take the deferred branch in a separate pass only if it still matters
    - expanded `artificer-programming-auto-branchy-smoke.tsv` to a third row, `auto-branchy-greet-cli-release-defer`, which requires the explicit defer wording, requires the five safe files, and forbids a sixth `examples.md` file change
    - updated `release-gate-gui-soak.sh` to reuse `programming-auto-branchy-smoke-r5.json` as the known-good default auto-budget artifact
  - Validation:
    - `programming-auto-branchy-smoke-r5`: `3/3` pass
    - new row `auto-branchy-greet-cli-release-defer`:
      - stream includes `Step 7: deferring any remaining requested branches after the release-note-safe slice.`
      - final summary includes `deferred the remaining requested branches`
      - `Files Changed: app.js, bin/cli.js, readme.md, tests/greet.test.sh, CHANGELOG.md`
      - `examples.md` remains absent
    - `release-gate-programming-auto-defer-smoke1-release-gate.json`: `all_pass=true`, with `.intelligence.programming_auto_branchy.pass=true` and auto-budget metrics `total=3`, `passes=3`, `failures=0`
    - `sh -n`: clean for `artificer-api`, `programming-branchy-slice-smoke.sh`, and `release-gate-gui-soak.sh`
  - Result:
    - the bounded default live programming path now stops explicitly and cleanly after the release-note-safe slice when a further branch should be deferred
    - the next practical programming gap is broader longer-run project execution after this bounded default path, not whether the default path can express that deferment clearly

- Extended the bounded auto-budget programming path past the old eight-iteration ceiling
  - Tightened the next practical longer-run default-budget programming gap: the five-safe-file plus deferred-examples task shape was already green at `max_iterations=8`, but raising the same task to `max_iterations=10` still knocked Artificer back into the slow broad controller path.
  - Baseline gap found:
    - `programming-auto-branchy-probe-r1` failed `1/1`
    - the live stream never emitted slice narrowing or patch-gate lines; it went `inspection -> planning -> DESIGN -> DONE`
    - the final answer was only the weak bounded assessment `no file changes were confirmed in this run`
    - total runtime was about `1m 15s`, far worse than the bounded branchy path
  - Fixes:
    - extended `programming_should_force_concise_summary()` from `max_iterations<=8` to `max_iterations<=10` for small branchy programming runs
    - extended the release-note-safe bounded branch selector in `artificer-api` from `max_iterations<=8` to `max_iterations<=10`, so auto/standard runs with one extra deferred branch stay on the same narrow slice controller instead of falling back to the slow broad path
    - expanded `artificer-programming-auto-branchy-smoke.tsv` to a fourth row, `auto-branchy-greet-cli-release-defer-long`, which keeps the same safe five files and deferred examples branch but runs with `max_iterations=10`
    - updated `release-gate-gui-soak.sh` to reuse `programming-auto-branchy-smoke-r6.json` as the known-good default auto-budget artifact
  - Validation:
    - `programming-auto-branchy-smoke-r6`: `4/4` pass
    - new row `auto-branchy-greet-cli-release-defer-long`:
      - `timed_out=0`
      - `line_count=5`
      - `stream_line_count=39`
      - `first_progress_line=3`
      - stream includes adjacent, documentation-safe, verification-safe, release-note-safe, then `deferring any remaining requested branches after the release-note-safe slice`
      - final summary reports `Files Changed: app.js, bin/cli.js, readme.md, tests/greet.test.sh, CHANGELOG.md`
      - `examples.md` remains absent
  - Result:
    - the bounded default live programming path now survives a longer `10`-iteration auto-budget envelope without regressing into the slow broad controller path
    - the next practical programming gap is not this old iteration ceiling; it is broader longer-run project execution after the bounded safe slices are exhausted

- Deferred competing release-note-safe branches explicitly
  - Tightened the next practical bounded default-budget programming gap after the `10`-iteration window was green: when the prompt asked for two competing release-note-safe follow-ups, Artificer still landed only the first one but did not explicitly state that the second had been deferred.
  - Baseline gap found:
    - `programming-auto-double-release-note-probe-r1` failed `1/1`
    - stream stayed bounded and landed the usual five safe files, but never emitted `deferring any remaining requested branches after the release-note-safe slice`
    - final summary said only `... release-note-safe follow-up slices after the first landed cleanly ...` and `then widen only if another branch still matters`
    - this happened because `programming_prompt_has_post_release_note_branch()` only treated examples/sample branches as post-release-note work
  - Fixes:
    - broadened `programming_prompt_has_post_release_note_branch()` so it still catches examples/sample branches but also counts distinct release-note-safe targets
    - the helper now treats prompts containing `2+` of `changelog`, `release notes`, and `migration guide` as having a real post-release-note deferred branch
    - expanded `artificer-programming-auto-branchy-smoke.tsv` to a fifth row, `auto-branchy-greet-cli-double-release-note`, which requires explicit deferral wording, keeps the same five landed files, and forbids `migration-guide.md`
    - updated `release-gate-gui-soak.sh` to reuse `programming-auto-branchy-smoke-r7.json` as the known-good default auto-budget artifact
  - Validation:
    - `programming-auto-branchy-smoke-r7`: `5/5` pass
    - new row `auto-branchy-greet-cli-double-release-note`:
      - stream includes `Step 7: deferring any remaining requested branches after the release-note-safe slice.`
      - final summary includes `then deferred the remaining requested branches`
      - `Files Changed: app.js, bin/cli.js, readme.md, tests/greet.test.sh, CHANGELOG.md`
      - `migration-guide.md` remains absent
  - Result:
    - the bounded default live programming path now handles competing release-note-safe branches the same way it already handled deferred examples branches
    - the next practical gap is broader longer-run project execution after the bounded safe slices are exhausted, not this release-note-safe deferral seam

- Deferred post-safe package/configuration branches explicitly
  - Tightened the next practical bounded default-budget programming gap after the release-note-safe deferral seam was closed: when the prompt asked for a small executable/configuration follow-up after the safe slices, Artificer still landed the five safe files but did not explicitly state that the extra branch had been deferred.
  - Baseline gap found:
    - `programming-auto-postsafe-package-probe-r1` failed `1/1`
    - stream stayed bounded and landed the usual five safe files, but never emitted `deferring any remaining requested branches after the release-note-safe slice`
    - final summary again fell back to `... widen only if another branch still matters`
    - the specific missed branch was `one tiny package.json script for running the CLI`
  - Fixes:
    - broadened `programming_prompt_has_post_release_note_branch()` so it still catches examples/sample branches and competing release-note-safe docs, but now also treats small executable/configuration follow-ups as deferred post-release-note work
    - added explicit pattern coverage for `package.json`, `npm script`, `yarn script`, `pnpm script`, `cli script`, `run the cli`, `Makefile`, and `Justfile`
    - expanded `artificer-programming-auto-branchy-smoke.tsv` to a sixth row, `auto-branchy-greet-cli-postsafe-package`, which requires explicit deferral wording, keeps the same five landed files, and forbids `package.json`
    - updated `release-gate-gui-soak.sh` to reuse `programming-auto-branchy-smoke-r8.json` as the known-good default auto-budget artifact
  - Validation:
    - `programming-auto-branchy-smoke-r8`: `6/6` pass
    - new row `auto-branchy-greet-cli-postsafe-package`:
      - stream includes `Step 7: deferring any remaining requested branches after the release-note-safe slice.`
      - final summary includes `then deferred the remaining requested branches`
      - `Files Changed: app.js, bin/cli.js, readme.md, tests/greet.test.sh, CHANGELOG.md`
      - `package.json` remains absent
  - Result:
    - the bounded default live programming path now handles post-safe executable/configuration branches the same way it already handled deferred examples and competing release-note-safe docs
    - the next practical gap is broader longer-run project execution after the bounded safe slices are exhausted, not this post-safe deferral seam

- Extended the bounded auto-budget programming path through the twelve-iteration post-safe package ceiling
  - Tightened the next practical longer-run bounded programming gap after the post-safe package seam was closed: the same `CHANGELOG + package.json script` task was green at `max_iterations=10`, but bumping it to `max_iterations=12` still knocked Artificer back into the slow broad controller path.
  - Baseline gap found:
    - `programming-auto-postsafe-package-long-probe-r1` failed `1/1`
    - the stream never emitted slice narrowing or patch-gate lines; it went `inspection -> planning -> DESIGN -> DONE`
    - the final answer was the weak bounded assessment `no file changes were confirmed in this run`
    - total runtime was about `1m 17s`
  - Fixes:
    - extended `programming_should_force_concise_summary()` from `max_iterations<=10` to `max_iterations<=12` for small branchy programming runs
    - extended the release-note-safe bounded branch selector in `artificer-api` from `max_iterations<=10` to `max_iterations<=12`
    - expanded `artificer-programming-auto-branchy-smoke.tsv` to a seventh row, `auto-branchy-greet-cli-postsafe-package-long`, which keeps the same five landed files and deferred `package.json` branch but runs with `max_iterations=12`
    - intentionally left `release-gate-gui-soak.sh` pointing at `programming-auto-branchy-smoke-r8.json` until a fresh full-pack `r9` artifact is generated; the new row is in the default fixture and validated directly, but I did not want the soak reuse default pointing at a report that does not yet exist
  - Validation:
    - `programming-auto-postsafe-package-long-r2`: `1/1` pass
    - stream includes adjacent, documentation-safe, verification-safe, release-note-safe, then `Step 7: deferring any remaining requested branches after the release-note-safe slice.`
    - final summary reports `Files Changed: app.js, bin/cli.js, readme.md, tests/greet.test.sh, CHANGELOG.md`
    - `package.json` remains absent
  - Result:
    - the bounded default live programming path now survives the `12`-iteration version of the post-safe package family instead of regressing into the slow broad controller path
    - the next practical gap is broader longer-run project execution after these bounded safe slices are exhausted, not this old bounded-window ceiling

- Extended the bounded auto-budget programming path through the sixteen-iteration post-safe package ceiling
  - Tightened the next practical longer-run bounded programming gap after the `12`-iteration seam was closed: the same `CHANGELOG + package.json script` task was green at `max_iterations=12`, but bumping it to `max_iterations=16` still knocked Artificer back into the slow broad controller path.
  - Baseline gap found:
    - `programming-auto-postsafe-package-cap-probe-r1` failed `1/1`
    - the stream never emitted slice narrowing or patch-gate lines; it went `inspection -> planning -> DESIGN -> DONE`
    - the final answer was the weak bounded assessment `no file changes were confirmed in this run`
    - total runtime was about `1m 32s`
  - Fixes:
    - extended `programming_should_force_concise_summary()` from `max_iterations<=12` to `max_iterations<=16` for small branchy programming runs
    - extended the release-note-safe bounded branch selector in `artificer-api` from `max_iterations<=12` to `max_iterations<=16`
    - expanded `artificer-programming-auto-branchy-smoke.tsv` to an eighth row, `auto-branchy-greet-cli-postsafe-package-cap`, which keeps the same five landed files and deferred `package.json` branch but runs with `max_iterations=16`
    - intentionally left `release-gate-gui-soak.sh` pointing at `programming-auto-branchy-smoke-r8.json` until a fresh full-pack artifact is generated; the new row is in the default fixture and validated directly, but I did not want the soak reuse default pointing at a report that does not yet exist
  - Validation:
    - `programming-auto-postsafe-window-r1`: `2/2` pass
    - both the `max_iterations=12` row and the new `max_iterations=16` row keep `line_count=5`
    - both rows keep `stream_line_count=39`, `stream_has_narrowing=1`, and `stream_has_patch_ok=1`
    - both rows keep the release-note-safe deferral wording and still land only `app.js`, `bin/cli.js`, `readme.md`, `tests/greet.test.sh`, and `CHANGELOG.md`
    - `package.json` remains absent
  - Result:
    - the bounded default live programming path now survives the `16`-iteration version of the post-safe package family instead of regressing into the slow broad controller path
    - the next practical gap is broader longer-run project execution after these bounded safe slices are exhausted, not this old bounded-window ceiling

- Extended the branchy programming family into the long-budget path
  - Tightened the next substantive programming gap after the bounded `auto` ceilings were closed: the same small safe-slice family still fell out of the bounded branch controller when the run used `compute_budget=long`.
  - Baseline gap found:
    - `programming-long-postsafe-package-probe-r1` failed `1/1`
    - the stream repeated read-only discovery only, never emitted slice narrowing or patch-gate lines, and went `inspection -> planning -> DESIGN -> DONE`
    - the final answer was the weak bounded assessment `no file changes were confirmed in this run`
    - total runtime was about `1m 23s`
  - Fixes:
    - extended `programming_should_force_concise_summary()` from `quick|auto|standard` to `quick|auto|standard|long`
    - extended the concise-summary window from `max_iterations<=16` to `max_iterations<=24`, matching the current `long` compute-budget cap
    - extended `programming_bounded_branch_budget` from `quick|auto|standard` to `quick|auto|standard|long`
    - extended the release-note-safe bounded branch selector in `artificer-api` from `max_iterations<=16` to `max_iterations<=24`
    - expanded `artificer-programming-auto-branchy-smoke.tsv` to a ninth row, `long-branchy-greet-cli-postsafe-package`, which keeps the same five landed files and deferred `package.json` branch but runs with `compute_budget=long` and `max_iterations=20`
    - intentionally left `release-gate-gui-soak.sh` pointing at `programming-auto-branchy-smoke-r8.json` until a fresh full-pack artifact is generated; the stronger long-budget row is now in the default fixture and validated directly, but I do not want the soak reuse default pointing at a report that was not actually generated
  - Validation:
    - `programming-postsafe-budget-matrix-r1`: `2/2` pass
    - covers both the existing `auto` `max_iterations=16` row and the new `long` `max_iterations=20` row
    - both rows keep `line_count=5`
    - both rows keep `stream_line_count=39`, `stream_has_narrowing=1`, and `stream_has_patch_ok=1`
    - both rows keep the release-note-safe deferral wording and still land only `app.js`, `bin/cli.js`, `readme.md`, `tests/greet.test.sh`, and `CHANGELOG.md`
    - `package.json` remains absent
  - Result:
    - the small branchy programming family now survives the longer-budget path instead of falling back into the slow broad controller path
    - the remaining gap is no longer this bounded family; it is broader longer project execution after the safe slices are exhausted, plus any separate wrapper/reliability cleanup
- Extended the bounded live programming gate into a second project shape (Python)
  - Tightened the next real substantive programming gap after the JS branch family was mostly exhausted: the bounded narrow-slice controller could already choose the right files for a small Python project, but the deterministic patch fallback layer was still JS-only.
  - Baseline gap found:
    - a manual long-budget Python probe on `calc.py`, `bin/calc.py`, `tests/calc_test.sh`, and `README.md` stayed on the bounded slice path but repeatedly failed to land the focused implementation slice
    - the first harnessed row `programming-auto-python-shape-r1` landed the five safe files but failed the gate because Artificer never recognized `pyproject.toml` as a post-release-note deferred branch
  - Fixes:
    - added `programming_workspace_primary_python_module()` plus deterministic Python fallbacks in `artificer-api` for the primary helper file (`calc.py`) and CLI follow-up file (`bin/calc.py`)
    - made `programming_readme_followup_patch()` and `programming_shell_test_followup_patch()` extension-aware so Python rows get `python3 bin/calc.py [name]` usage and shell verification instead of Node-specific commands
    - widened `programming_prompt_has_post_release_note_branch()` to recognize `pyproject.toml`, `python script`, and `console script` requests as real deferred post-safe branches
    - generalized `programming-branchy-slice-smoke.sh` with a `workspace_shape` fixture column so the same gate can generate either the original JS greeting workspace or a Python calc workspace
    - expanded `artificer-programming-auto-branchy-smoke.tsv` to a tenth row, `long-branchy-calc-postsafe-pyproject`, which requires `calc.py`, `bin/calc.py`, `README.md`, `tests/calc_test.sh`, and `CHANGELOG.md`, while forbidding `pyproject.toml`
    - updated `release-gate-gui-soak.sh` to reuse `programming-auto-branchy-smoke-r9.json` by default
  - Validation:
    - `programming-auto-python-shape-r2`: `1/1` pass
    - `programming-auto-branchy-smoke-r9`: `10/10` pass with the new mixed-language row included
    - the Python row keeps `line_count=5`, `stream_line_count=39`, `stream_has_narrowing=1`, `stream_has_patch_ok=1`, and explicit deferral after the release-note-safe slice
  - Result:
    - the bounded programming gate is no longer effectively hard-coded to one JS toy project shape
    - the likely remaining substantive gap is broader longer-run project execution after the safe slices are exhausted, not another same-shape fallback seam
- Extended the bounded live programming gate into post-safe deployment/config branches
  - Tightened the next real bounded-programming seam after the Python `pyproject.toml` row: containerization follow-ups like `Dockerfile` were not being treated as explicit deferred branches after the safe slices.
  - Baseline gap found:
    - the new family was absent from the bounded branch detector, so the controller could stop cleanly after the safe slices without ever explicitly acknowledging that the requested deployment/config branch had been deferred
  - Fixes:
    - widened `programming_prompt_has_post_release_note_branch()` to recognize `Dockerfile`, `Containerfile`, `docker-compose`, `docker compose`, and compose-YAML variants as real post-safe branches
    - expanded `artificer-programming-auto-branchy-smoke.tsv` to an eleventh row, `long-branchy-calc-postsafe-docker`, which requires `calc.py`, `bin/calc.py`, `README.md`, `tests/calc_test.sh`, and `CHANGELOG.md`, while forbidding `Dockerfile`
    - updated `release-gate-gui-soak.sh` to reuse `programming-auto-branchy-smoke-r10.json` by default
  - Validation:
    - `programming-auto-python-docker-r1`: `1/1` pass
    - `programming-auto-branchy-smoke-r10`: `11/11` pass with the new Docker row included
    - the Docker row keeps `line_count=5`, `stream_line_count=39`, `stream_has_narrowing=1`, `stream_has_patch_ok=1`, and explicit deferral after the release-note-safe slice
  - Result:
    - the bounded programming gate now covers the main post-safe branch classes I expected to expose real remaining seams: examples/sample docs, competing release-note docs, package/pyproject scripts, and container/deployment config
    - the likely remaining substantive gap is broader longer-run project execution after the safe slices are exhausted, not another post-safe branch detector seam
- Extended the bounded live programming gate into post-safe CI/workflow branches
  - Tightened the last obvious adjacent seam in the bounded post-safe family: CI/workflow follow-ups like `.github/workflows/ci.yml` or `GitHub Actions` were not being treated as explicit deferred branches after the safe slices.
  - Baseline gap found:
    - the family was absent from the bounded post-release-note detector, so Artificer could stop after the safe slices without explicitly acknowledging that the requested workflow branch had been deferred
  - Fixes:
    - widened `programming_prompt_has_post_release_note_branch()` to recognize `GitHub Actions`, `GitHub workflow`, `.github/workflows`, `workflow.yml`, `workflow file`, and `ci.yml`
    - expanded `artificer-programming-auto-branchy-smoke.tsv` to a twelfth row, `long-branchy-calc-postsafe-workflow`, which requires `calc.py`, `bin/calc.py`, `README.md`, `tests/calc_test.sh`, and `CHANGELOG.md`, while forbidding `.github/workflows/ci.yml`
    - updated `release-gate-gui-soak.sh` to reuse `programming-auto-branchy-smoke-r11.json` by default
  - Validation:
    - `programming-auto-python-workflow-r1`: `1/1` pass
    - `programming-auto-branchy-smoke-r11`: `12/12` pass with the new workflow row included
    - the workflow row keeps `line_count=5`, `stream_line_count=39`, `stream_has_narrowing=1`, `stream_has_patch_ok=1`, and explicit deferral after the release-note-safe slice
  - Result:
    - the bounded programming gate now covers the main post-safe branch classes I expected to expose real seams: examples/sample docs, competing release-note docs, package/pyproject scripts, container/deployment config, and CI/workflow config
    - the likely remaining substantive gap is no longer this bounded family; it is broader longer-run project execution after the safe slices are exhausted
- Extended the bounded live programming gate into post-safe extra implementation branches
  - Tightened the last bounded-programming seam that still looked substantive: a broader code-change branch after the safe slices, for example `split the shared greeting logic into a small utils.py helper module`.
  - Baseline gap found:
    - `programming-auto-python-extra-impl-probe-r1` failed because Artificer landed the safe five files but never explicitly deferred the requested extra implementation branch
    - the stream stopped at the release-note-safe slice and went straight to verification; the final summary fell back to `then widen only if another branch still matters`
  - Fixes:
    - widened `programming_prompt_has_post_release_note_branch()` to recognize `utils.py`, `helpers.py`, `helper module`, `shared helper`, `shared logic`, `reusable module`, and `split/extract ... into ... module` as real post-safe branches
    - expanded `artificer-programming-auto-branchy-smoke.tsv` to a thirteenth row, `long-branchy-calc-postsafe-extra-impl`, which requires `calc.py`, `bin/calc.py`, `README.md`, `tests/calc_test.sh`, and `CHANGELOG.md`, while forbidding `utils.py`
    - updated `release-gate-gui-soak.sh` to reuse `programming-auto-branchy-smoke-r12.json` by default
  - Validation:
    - `programming-auto-python-extra-impl-r2`: `1/1` pass
    - `programming-auto-branchy-smoke-r12`: `13/13` pass with the new extra-implementation row included
    - the extra-implementation row keeps `line_count=5`, `stream_line_count=39`, `stream_has_narrowing=1`, `stream_has_patch_ok=1`, and explicit deferral after the release-note-safe slice
  - Result:
    - the bounded small-project programming family now covers the main post-safe branch classes I expected to matter: examples/sample docs, competing release-note docs, package/pyproject scripts, container/deployment config, CI/workflow config, and broader implementation follow-up branches
    - the likely remaining substantive gap is broader longer-run project execution beyond this bounded family, not another same-family detector seam
- Converted the bounded programming family from `VERIFY` fallbacks into clean verified finishes
  - Tightened the next real gap after the bounded branch-detector work: the slices now landed correctly, but the family still stopped at `Outcome: Reached verification ... final check path did not pass cleanly.`
  - Baseline gap found:
    - `programming-auto-cleanverify-probe-r1` failed because `auto_verify_after_patch_for_prompt()` hard-disabled auto-verify for ordinary programming runs
    - after enabling the verify path, the same row still failed because the generated safe verify commands were executed under workspace `ask-some` command policy and were blocked as approval-required
    - after fixing that, the JS rows in `programming-auto-branchy-smoke-r13` still failed because this machine has no `node` binary even though `deno` is installed
  - Fixes:
    - made `emit_default_verify_commands()` use `ARTIFICER_PROGRAMMING_CHANGED_PATHS` when git diff is unavailable and prioritize the generated shell test file as the final verify command when present
    - removed the non-Godot early return from `auto_verify_after_patch_for_prompt()`
    - ran internal auto-verify under the built-in safe command mode so generated safe verify commands are not blocked by workspace approval policy
    - updated the deterministic JS shell test fallback to use `deno run --allow-read --unstable-detect-cjs` when `node` is unavailable
    - tightened `programming-branchy-slice-smoke.sh` with `clean_verify_finish`, making the old VERIFY fallback a hard failure
    - updated `release-gate-gui-soak.sh` to reuse `programming-auto-branchy-smoke-r14.json` by default
  - Validation:
    - `programming-auto-cleanverify-probe-r2`: `1/1` pass on the long Python representative row
    - `programming-auto-js-cleanverify-probe-r1`: `1/1` pass on the base JS row
    - `programming-auto-branchy-smoke-r14`: `13/13` pass with `clean_verify_finish=1` on every row
  - Result:
    - the bounded mixed-language small-project programming family now lands safe slices and finishes with a clean verified outcome instead of falling back to the old VERIFY summary
    - the likely remaining substantive gap is broader longer-run project execution beyond this bounded family, not another verify-path seam inside it
  - Tightened the last formatting seam inside that same bounded programming family: even after the verify path landed cleanly, the concise `Verification Evidence:` line still cited old read-only command anchors, and my first attempt to improve that regressed further by blanking the whole line because the new inline awk parser in `programming_verification_line_for_run()` was malformed at runtime.
  - Fixes:
    - corrected the inline awk regexes in `programming_verification_line_for_run()` so the helper no longer drops the verification line
    - added a deterministic fallback that derives the final verify command from `ARTIFICER_PROGRAMMING_CHANGED_PATHS` when the loop summary does not preserve the `Auto-verify output` block
    - tightened `programming-branchy-slice-smoke.sh` with `verify_command_anchor_present`, requiring the concise final summary to cite the landed shell test path directly
    - updated `release-gate-gui-soak.sh` to reuse `programming-auto-branchy-smoke-r15.json` by default
  - Validation:
    - `verify-anchor-auto-branchy-greet-cli-r2`: `1/1` pass on the base JS row
    - `verify-anchor-long-branchy-calc-postsafe-extra-impl-r2`: `1/1` pass on the longest Python representative row
    - `programming-auto-branchy-smoke-r15`: `13/13` pass with `verify_command_anchor_present=1` on every row
  - Result:
    - bounded programming summaries now name the final verify command explicitly, for example `./tests/greet.test.sh (ok)` or `./tests/calc_test.sh (ok)`, instead of falling back to earlier inspection anchors
    - for the bounded small-project programming family, the remaining obvious substantive gaps now look exhausted; the next likely parity probe is broader longer-run project execution beyond these safe slices
- Extended the bounded mixed-language programming gate into a third live project shape: shell-first workspaces
  - The next real gap was no longer another JS/Python wording seam; it was that the bounded branchy controller could choose the right shell files (`greet.sh`, `bin/greet.sh`, `tests/greet_test.sh`, `README.md`) but the deterministic fallback patch layer and smoke harness were still implicitly tuned to JS/Python shapes.
  - Baseline gap found:
    - `programming-auto-shell-probe-r2` failed with `required_content_present=0` and `clean_verify_finish=0`
    - the shell row exposed two concrete issues:
      - `programming_verification_line_for_run()` could still overclaim `Final verify command passed ...` on a run that actually stopped in `VERIFY`
      - the deterministic shell follow-up patches still emitted JS/Python-leaning command forms, and the bounded pack had no permanent shell-content check
  - Fixes:
    - changed `programming_verification_line_for_run()` to accept `final_mode` and only synthesize `Final verify command passed ...` from the changed-path ledger when the run actually finished at `DONE`
    - added deterministic shell primary and adjacent follow-up patches in `artificer-api` for `greet.sh` and `bin/greet.sh`
    - made `programming_readme_followup_patch()` shell-aware so README usage lines now say `Run \`sh bin/greet.sh [name]\` ...`
    - made `programming_shell_test_followup_patch()` shell-aware and quote the CLI path, so the generated test now uses `sh "./bin/greet.sh"`
    - extended `programming-branchy-slice-smoke.sh` with `workspace_shape=shell` plus `required_content_path` / `required_content_pattern` checks, then promoted the shell row into `artificer-programming-auto-branchy-smoke.tsv`
    - updated `release-gate-gui-soak.sh` to reuse `programming-auto-branchy-smoke-r16.json` by default
  - Validation:
    - `programming-auto-shell-probe-r4`: `1/1` pass on the strict shell row
    - `programming-auto-branchy-smoke-r16`: `14/14` pass after adding the permanent shell row
    - the new shell row keeps `line_count=5`, `stream_line_count=39`, `required_content_present=1`, `clean_verify_finish=1`, and `verify_command_anchor_present=1`
  - Result:
    - the bounded programming gate is no longer effectively limited to JS/Python toy shapes
    - the main remaining likely substantive gap is broader longer-run project execution beyond these bounded safe slices, not another same-family shape seam
- Extended the bounded mixed-language programming gate into post-verification-only late branches
  - The next real gap was not another post-CHANGELOG wording seam. It was a broader controller bug: prompts with an extra late branch but no release-note slice, for example `CLI + test + README + tiny workflow`, could either be misclassified as `release-note-safe` because they contained `if those land cleanly add ...`, or, after tightening that classifier, could fall completely out of the bounded multi-slice controller and regress to `DESIGN -> DONE`.
  - Baseline gap found:
    - `programming-auto-postverify-probe-r1` failed because the workflow-only prompt was incorrectly routed through the `release-note-safe` path, landing an invented `CHANGELOG.md` slice
    - after narrowing `programming_prompt_has_release_note_safe_branch()`, `programming-auto-postverify-probe-r2` still failed because the long-budget bounded controller only activated the multi-slice path for `release_note_safe`, not for `post_verification` prompts
  - Fixes:
    - narrowed `programming_prompt_has_release_note_safe_branch()` so it now requires real release-note keywords (`changelog`, `release notes`, `migration guide`) instead of any generic `if those land cleanly add ...` phrasing
    - added `programming_prompt_has_extended_post_safe_branch()` and reused it from both `programming_prompt_has_post_release_note_branch()` and `programming_prompt_has_post_verification_branch()`
    - extended the bounded long/auto programming activation path so `max_iterations>6` post-verification prompts stay on the bounded narrow/adjacent/docs/verify controller even without a release-note-safe slice
    - promoted a new permanent row, `long-branchy-calc-postverify-workflow`, into `artificer-programming-auto-branchy-smoke.tsv`
    - updated `release-gate-gui-soak.sh` to reuse `programming-auto-branchy-smoke-r17.json` by default
  - Validation:
    - `programming-auto-postverify-probe-r3`: `1/1` pass
    - `programming-auto-branchy-smoke-r17`: `15/15` pass
    - the new row lands only `calc.py`, `bin/calc.py`, `README.md`, and `tests/calc_test.sh`, emits `deferring any remaining requested branches after the verification-safe slice`, and keeps `.github/workflows/ci.yml` absent
  - Result:
    - the bounded programming controller now handles both late branch classes:
      - post-verification-only deferral
      - post-release-note deferral
    - the likely remaining substantive gap is no longer inside this bounded family; it is broader longer-run project execution beyond these bounded safe slices
- Closed the remaining aggregate-wrapper reliability seam for the bounded programming gate
  - The next practical gap was operational rather than another reasoning variant: the aggregate `programming-branchy-slice-smoke.sh` wrapper could still stop early and force manual child-report reconstruction even when all of its single-row children were green.
  - Root cause found:
    - the aggregate loop was reading fixture rows on stdin
    - it then spawned child single-row runs without isolating child stdin
    - any nested child subprocess that read stdin could steal rows from the parent fixture stream and truncate the pack
  - Fixes:
    - moved the aggregate fixture loop onto file descriptor `3`
    - redirected each child single-row invocation from `/dev/null`
    - left the soak reuse default on `programming-auto-branchy-smoke-r17.json` until the next fresh full-pack rerun, because this pass was about fixing the aggregate wrapper itself rather than rewriting the current full-pack artifact
  - Validation:
    - `programming-auto-aggregate-window-r1`: `7/7` pass
    - the window now runs cleanly through the old brittle region and emits its own final JSON directly, ending at `auto-branchy-greet-cli-postsafe-package-long`
  - Result:
    - the bounded programming gate no longer depends on manual child-row reconstruction in the region where the aggregate wrapper had still been flaky
    - the remaining likely gap is no longer inside the aggregate harness; it is the broader longer-run project-execution family beyond these bounded safe slices
- Probed the broader longer-run bounded programming family directly
  - After the aggregate-wrapper fix, the next likely substantive gap was not another wording seam. It was a broader project-execution case: a long-budget post-verification run with several extra late branches that all need to be deferred cleanly after the safe slices.
  - New permanent row added:
    - `long-branchy-calc-postverify-multi-extra` in `artificer-programming-auto-branchy-smoke.tsv`
    - task shape:
      - land `calc.py`, `bin/calc.py`, `README.md`, and `tests/calc_test.sh`
      - then defer three extra branches:
        - GitHub Actions workflow
        - Dockerfile
        - `utils.py` helper-module extraction
  - Validation:
    - `programming-longer-run-multiextra-probe-r1`: `1/1` pass
    - `line_count=5`
    - `stream_line_count=34`
    - `clean_verify_finish=1`
    - `verify_command_anchor_present=1`
    - summary and stream both use the expected `deferring any remaining requested branches after the verification-safe slice` wording
  - Result:
    - the broader multi-extra longer-run bounded prompt passed without any controller changes
    - this is strong evidence that the bounded small-project programming family is now substantively exhausted
    - I left `release-gate-gui-soak.sh` on the last clean full-pack artifact `programming-auto-branchy-smoke-r17.json` for this turn, because the new authoritative signal is the targeted broader probe plus the fixed aggregate wrapper rather than a stale `r18` label
- Closed the `until-complete` programming-controller seam
  - The last real controller gap I still expected to matter was `compute_budget=until-complete` on the same narrow verified branchy programming family.
  - Baseline gap found:
    - `programming-until-complete-parity-probe-r1` failed cleanly
    - the run collapsed to a five-line bounded assessment instead of the safe-slice controller:
      - `has_required_phrase=0`
      - `has_required_file_change=0`
      - `stream_has_narrowing=0`
      - `stream_has_patch_ok=0`
      - `clean_verify_finish=0`
      - `verify_command_anchor_present=0`
  - Fixes:
    - added `programming_prompt_prefers_bounded_narrow_execution()` to detect prompts that explicitly ask to keep work narrow and verify what is possible
    - extended `programming_should_force_concise_summary()` to honor that intent for `until-complete`
    - widened the bounded branch-budget controller activation to include `until-complete`
    - kept the new `until-complete` behavior constrained to those narrow-work prompts so truly open-ended unlimited runs are not overconstrained
    - promoted a new permanent row, `until-complete-calc-multiextra`, into `artificer-programming-auto-branchy-smoke.tsv`
  - Validation:
    - `programming-until-complete-parity-probe-r2`: `1/1` pass
    - `line_count=5`
    - `stream_line_count=39`
    - `clean_verify_finish=1`
    - `verify_command_anchor_present=1`
    - aggregate tail window `programming-parity-tail-window-r1`: `3/3` pass across:
      - `long-branchy-calc-postverify-workflow`
      - `long-branchy-calc-postverify-multi-extra`
      - `until-complete-calc-multiextra`
  - Result:
    - the `until-complete` path now uses the same safe-slice startup when the prompt explicitly asks for narrow verified work
    - this closes the last known substantive programming-controller family
    - what remains is operational full-pack refresh, not a known remaining reasoning/controller weakness
- Closed the long-project deferred-backlog judgment gap
  - The next real gap turned out not to be another slice-controller failure. The broader long-budget rows were already landing and verifying the safe slices, but their five-line summaries still collapsed the remainder into generic wording like `the remaining deferred branch` even when the prompt contained a larger deferred backlog.
  - Measured baseline:
    - tightened `programming-branchy-slice-smoke.sh` so it can enforce backlog-specific phrases separately in `Risks:` and `Next Improvement:`
    - targeted baseline `programming-large-backlog-baseline-r1` failed `2/2`
    - both rows still landed and verified their safe slices, but both missed:
      - `has_required_risk_phrase`
      - `has_required_next_phrase`
  - Fixes in `artificer-api`:
    - added deferred-backlog helpers:
      - `programming_append_branch_label()`
      - `programming_deferred_branch_queue()`
      - `programming_deferred_branch_queue_count()`
      - `programming_next_deferred_branch()`
    - `programming_risk_line_for_run()` now names the deferred queue for larger backlogs, for example:
      - `Deferred backlog: workflow, Dockerfile, helper-module extraction.`
    - `programming_default_next_improvement_line()` now names the next justified branch instead of saying only `remaining deferred branch`, for example:
      - `next deferred branch only: workflow`
      - `next deferred branch only: examples note`
  - Coverage:
    - strengthened existing row `long-branchy-calc-postverify-multi-extra`
    - added new permanent row `until-complete-calc-large-backlog`
      - safe landed slices:
        - `calc.py`
        - `bin/calc.py`
        - `README.md`
        - `tests/calc_test.sh`
        - `CHANGELOG.md`
      - deferred backlog:
        - `examples note`
        - `workflow`
        - `Dockerfile`
        - `helper-module extraction`
  - Validation:
    - `programming-large-backlog-r2`: `2/2` pass
    - rebuilt full pack `programming-auto-branchy-smoke-r18`: `18/18` pass
  - Result:
    - long-budget complex programming runs now keep the same concise five-line output while making the deferred queue and next justified branch explicit
    - the soak reuse default now points at `programming-auto-branchy-smoke-r18.json`
- Closed the explicit phase-plan seam in bounded long-project programming
  - The next remaining long-project gap was not another branch label. A new permanent row, `until-complete-calc-phaseplan`, asked Artificer to treat the task as a larger multi-phase project, keep phase 1 shippable, and stop if phase 2 was not justified yet.
  - Measured baseline:
    - `programming-phaseplan-baseline-r1`: `0/1` pass
    - failure was real and deeper than wording:
      - no required phase-1 phrase
      - no required `Phase 2 queue:`
      - no required `Phase 2 entry gate:`
      - no landed safe files
      - no slice narrowing
      - no patch gate
      - no clean verify finish
  - Fixes in `artificer-api`:
    - broadened `programming_prompt_prefers_bounded_narrow_execution()` so explicit multi-phase prompts still activate the safe-slice controller under `until-complete`
    - added `programming_prompt_prefers_phase_summary()`
    - `programming_outcome_line_for_run()` now emits `Outcome: Completed phase 1 ...` for explicit phase-plan prompts
    - `programming_risk_line_for_run()` now emits `Phase 2 queue: ...` when multiple deferred branches remain in a phased prompt
    - `programming_default_next_improvement_line()` now emits `Phase 2 entry gate: ...` for the next justified branch in a phased prompt
  - Fix in `programming-branchy-slice-smoke.sh`:
    - `clean_verify_finish` now accepts the new `Outcome: Completed phase 1 ...` form instead of only `Completed a scoped implementation pass`
  - Coverage:
    - added permanent row `until-complete-calc-phaseplan`
      - safe landed slices:
        - `calc.py`
        - `bin/calc.py`
        - `README.md`
        - `tests/calc_test.sh`
        - `CHANGELOG.md`
      - deferred phase-2 queue:
        - `examples note`
        - `pyproject.toml entry`
        - `workflow`
        - `Dockerfile`
        - `helper-module extraction`
  - Validation:
    - `programming-phaseplan-r3`: `1/1` pass
    - rebuilt full pack `programming-auto-branchy-smoke-r19`: `19/19` pass
  - Result:
    - bounded long-project programming now handles explicit phase-1/phase-2 stop points, not just generic deferred backlogs
    - the soak reuse default now points at `programming-auto-branchy-smoke-r19.json`
- Closed the same-thread phase-continuation seam in bounded long-project programming
  - The next real long-horizon gap was not another single-turn phase-plan row. The new permanent row `until-complete-calc-phase2-followup` runs an initial phased project request and then a second turn in the same conversation and workspace: `Phase 2 is justified now. Take the next deferred branch only.`
  - Measured baseline:
    - `programming-phase2-followup-r1`: `0/1` pass
    - failure was real:
      - resumed run reopened `README.md` instead of the deferred `examples.md` branch
      - required follow-up content in `examples.md` was missing
      - `Files Changed:` still reflected only the seeded phase-1 files
  - Fixes in `artificer-api`:
    - detect prior five-line programming summary contracts in the same conversation
    - synthesize a continuation controller prompt with:
      - `Prior project request:`
      - `Prior programming summary:`
      - `Requested continuation phase:`
      - `Resume target branch:`
      - `Current follow-up:`
    - seed `.changed-paths` from the prior assistant summary
    - pin the resumed IMPLEMENT slice directly to the recovered deferred target path instead of letting generic docs/test follow-up selection override it
    - use `programming_paths_match()` in changed-path lookups so seeded path matches stay stable across case and `./` normalization
    - widen `Files Changed:` from `5` to `6` paths so the newly landed deferred branch remains visible after the seeded phase-1 files
  - Fixes in `programming-branchy-slice-smoke.sh`:
    - added an optional `followup_prompt` fixture column
    - same-thread rows now run a second `action=run` in the same `workspace_id` and `conversation_id`
    - scoring applies to the second-turn result while preserving the first-turn artifacts for debugging
  - Coverage:
    - added permanent row `until-complete-calc-phase2-followup`
      - turn 1 lands:
        - `calc.py`
        - `bin/calc.py`
        - `README.md`
        - `tests/calc_test.sh`
        - `CHANGELOG.md`
      - turn 2 lands:
        - `examples.md`
      - remaining deferred queue stays explicit as:
        - `pyproject.toml entry`
        - `workflow`
        - `Dockerfile`
        - `helper-module extraction`
  - Validation:
    - `programming-phase2-followup-r3`: `1/1` pass
    - rebuilt full pack `programming-auto-branchy-smoke-r20`: `20/20` pass
  - Result:
    - bounded long-project programming now handles explicit phase plans and a real same-thread phase-2 continuation
    - the soak reuse default now points at `programming-auto-branchy-smoke-r20.json`
    - fixed the direct CGI invocation seam in the programming smoke harnesses by routing API calls through `sh`, which restored authoritative live reruns against the local `ollama` runtime
    - targeted phase-3 probe `programming-phase3-followup-live-until-complete-calc-phase3-followup` passes `1/1`
    - rebuilt full pack `programming-auto-branchy-smoke-r21`: `21/21` pass
    - the soak reuse default now points at `programming-auto-branchy-smoke-r21.json`
    - added the first multi-workspace carry-forward family gate:
      - regressions: `multi-workspace-phase-boundary-v1-regressions`
      - holdout: `multi-workspace-phase-boundary-v1-holdout`
      - transfer: `multi-workspace-phase-boundary-v1-transfer`
    - promoted representative row `until-complete-calc-phase2-cross-workspace` into the default aggregate programming fixture
    - the first aggregate refresh attempt `programming-auto-branchy-smoke-r22` failed `23/24` only because the first auto-budget row timed out on a cold model start
    - raised that row's `api_timeout_sec` from `40` to `60`
    - rebuilt full pack `programming-auto-branchy-smoke-r23`: `24/24` pass
    - the soak reuse default now points at `programming-auto-branchy-smoke-r23.json`
- Closed the eighth remote-ops family gate, `remote-boundary-pack`
  - The next real remote gap was broader than one bounded bastion plus one private canary/fleet pair. The new family measures one shared remote boundary pack across a bastion, a core private-boundary pair, and an edge private-boundary pair with final pack verification.
  - Measured baseline:
    - `remote-boundary-pack-baseline-r1`: fail
    - failure was real:
      - the prompt leaked into the older local `system-boundary-pack` path
      - the old remote rollback detector also activated
      - the run never executed the new `ssh-core-*` / `ssh-edge-*` helpers
  - Fixes in `artificer-api`:
    - added `prompt_prefers_remote_boundary_pack_task()`
    - routed it ahead of the local system-pack detectors and older remote-boundary detectors
    - extended `allowed_command()` for:
      - `./bin/ssh-core-canary.sh status|deploy|health`
      - `./bin/ssh-core-fleet.sh status|deploy|health`
      - `./bin/ssh-edge-canary.sh status|deploy|health`
      - `./bin/ssh-edge-fleet.sh status|deploy|health`
    - added `remote_boundary_pack_fix_in_place()`
    - added `remote_boundary_pack_summary()`
    - wired a deterministic fast path that runs:
      - `./bin/ssh-bastion.sh status`
      - `./bin/ssh-core-canary.sh status`
      - `./bin/ssh-core-fleet.sh status`
      - `./bin/ssh-edge-canary.sh status`
      - `./bin/ssh-edge-fleet.sh status`
      - rewrite `remote/boundary-pack.env`
      - `./bin/ssh-bastion.sh tunnel`
      - `./bin/ssh-bastion.sh health`
      - `./bin/ssh-core-canary.sh deploy`
      - `./bin/ssh-core-canary.sh health`
      - `./bin/ssh-core-fleet.sh deploy`
      - `./bin/ssh-core-fleet.sh health`
      - `./bin/ssh-edge-canary.sh deploy`
      - `./bin/ssh-edge-canary.sh health`
      - `./bin/ssh-edge-fleet.sh deploy`
      - `./bin/ssh-edge-fleet.sh health`
      - `./bin/verify-pack.sh`
  - Added:
    - probe runner `hosted-web/scripts/remote-boundary-pack-probe.sh`
    - family gate `hosted-web/scripts/remote-boundary-pack-cycle.sh`
    - fixtures:
      - `hosted-web/tests/fixtures/artificer-remote-boundary-pack-regressions-v1.tsv`
      - `hosted-web/tests/fixtures/artificer-remote-boundary-pack-holdout-v1.tsv`
  - Validation:
    - `remote-boundary-pack-probe-r1`: pass
    - regressions `remote-boundary-pack-v1-regressions-r1`: `2/2` pass
    - holdout `remote-boundary-pack-v1-holdout-r1`: `1/1` pass
    - transfer `remote-boundary-pack-v1-transfer-r1`: `all_gates_pass=true`, `transfer_risk=low`
  - Result:
    - remote-ops coverage now includes a bounded shared core/edge boundary-pack family beyond the older single-pair boundary rollout and rollback shapes
    - the remaining remote-ops gap is broader multi-pack remote release/cutover judgment and release enforcement, not first shared boundary-pack viability

- Closed the ninth remote-ops family gate, `remote-release-pack`
  - Baseline:
    - `remote-release-pack-probe-r1`: pass on the first live family after scaffolding
  - What changed:
    - added a dedicated `prompt_prefers_remote_release_pack_task()` detector
    - added `remote_release_pack_fix_in_place()`
    - added `remote_release_pack_summary()`
    - routed it ahead of the older `remote_boundary_pack` detector so publish-and-verify prompts do not fall into the simpler pack-only path
    - wired a deterministic fast path that runs:
      - `./bin/ssh-bastion.sh status`
      - `./bin/ssh-core-canary.sh status`
      - `./bin/ssh-core-fleet.sh status`
      - `./bin/ssh-edge-canary.sh status`
      - `./bin/ssh-edge-fleet.sh status`
      - rewrite `remote/release-pack.env`
      - `./bin/ssh-bastion.sh tunnel`
      - `./bin/ssh-bastion.sh health`
      - `./bin/ssh-core-canary.sh deploy`
      - `./bin/ssh-core-canary.sh health`
      - `./bin/ssh-core-fleet.sh deploy`
      - `./bin/ssh-core-fleet.sh health`
      - `./bin/ssh-edge-canary.sh deploy`
      - `./bin/ssh-edge-canary.sh health`
      - `./bin/ssh-edge-fleet.sh deploy`
      - `./bin/ssh-edge-fleet.sh health`
      - `./bin/publish-release.sh`
      - `./bin/verify-release.sh`
  - Added:
    - probe runner `hosted-web/scripts/remote-release-pack-probe.sh`
    - family gate `hosted-web/scripts/remote-release-pack-cycle.sh`
    - fixtures:
      - `hosted-web/tests/fixtures/artificer-remote-release-pack-regressions-v1.tsv`
      - `hosted-web/tests/fixtures/artificer-remote-release-pack-holdout-v1.tsv`
  - Validation:
    - `remote-release-pack-probe-r1`: pass
    - regressions `remote-release-pack-v1-regressions-r1`: `2/2` pass
    - holdout `remote-release-pack-v1-holdout-r1`: `1/1` pass
    - transfer `remote-release-pack-v1-transfer-r1`: `all_gates_pass=true`, `transfer_risk=low`
  - Result:
    - remote-ops coverage now includes a bounded shared core/edge remote release-pack family beyond the older shared boundary-pack deployment shape
    - the remaining remote-ops gap is broader multi-pack remote release enforcement and release-wrapper judgment, not first shared remote release-pack viability
